# ユーザー確認事項

**開発者（AI）が環境上の制約で実施できず、利用者が実機で確認する必要がある項目**を集約する。

作業ごとに項目が追加される。確認が済んだ項目は削除せず、結果を記入して残す。

---

## この文書の位置づけ

### 載せるもの

**自動化は可能だが、AI 側の環境では実行できない項目。**

| 制約の例 | 該当する項目 |
|---|---|
| 開発環境に実データがあり、破壊的操作を試せない | アンインストールの残留確認 |
| セッションから対象マシンへ接続できない | GPU 非搭載環境での挙動 |
| クリーンな環境が必要 | 上書きインストール、downgrade |
| 実際の音声で確認したい | 成功したジョブの表示 |

### 載せないもの

**操作の性質上、そもそも自動化できない項目**は `docs/archive/受入試験_手動確認手順書.md` に記載する。
対話メニューの選択、Explorer の起動、確認プロンプトでの中断など。

こちらは Phase 3d 以来の恒久的なリストであり、本文書とは役割が異なる。

### 記入の方法

- 未確認: `- [ ]`
- 確認済み: `- [x]` とし、**日付・環境・結果**を項目の下に追記する
- 不合格だった場合、**チェックを入れず**に結果だけ記入し、対応する課題番号を記す

---

## 確認軸ごとの実施方針

**確認できる環境が限られる項目に加え、0.1.24から配布形態そのものを比較する項目が生じたため、
「実行環境」と「配布形態」の2軸で章を分ける。** ポータブル版はSandbox、開発機、USB接続機の
いずれでも試せるため、従来の「環境を問わない」へ混ぜるとinstallerとの比較対象が不明確になる。

| 環境 | 用途 |
|---|---|
| Windows Sandbox | インストーラーの挙動。**GPU 演算は検証できない** |
| Core i7 機 | GPU 非搭載環境固有の挙動 |
| いずれか | 上記に該当しないもの |
| 配布形態／起動経路 | installer／portable固有の配置、移動、削除、token方針、Python直起動 |

**Windows Sandbox で済む項目を先に消化すると、Core i7 機を使う回数を減らせる。**

---

# 未確認の項目

## A. Windows Sandbox で確認するもの

インストーラーの挙動を確認する。GPU を使う処理は含めない。

### A-2. アンインストール時の利用者データ削除（0.1.22 / ac-2）

**手順**: A-1 と同じ状態を作り、アンインストール時に**すべて削除**を選択する。

**合格条件**

- [ ] 選択した対象がすべて削除される
- [ ] **キーリングのトークンが実際に削除される**（削除を選択した場合）
- [ ] トークン削除に失敗した場合、**その旨が利用者に伝わる**
- [ ] 利用者が指定した出力ディレクトリは走査も削除もされない

**注意**: トークンの削除は `utteran-gui.exe --delete-keyring-token` で行われる。
アプリ本体が消える前に実行される設計だが、**失敗時の扱いが未検証**である。

### A-3. 実行中の上書きインストール（0.1.22 / ac-2）

**手順**: 0.1.22 を起動したまま、同じ 0.1.22 をインストールする。

**合格条件**

- [ ] 実行中である旨が利用者に示される
- [ ] GUI が安全に終了してから更新される
- [ ] **profile の子プロセスも停止する**
- [ ] 更新後、旧 GUI が自動再起動しない

### A-4. 古いプロファイルを残した状態での更新（0.1.22 / ac-2）

**背景**: 0.1.19 で OpenVINO GenAI の依存が追加され、プロファイルの再構築が必要になった。
利用者が変更履歴を読まずに更新した場合の挙動を確認する。

**手順**: 0.1.21 でプロファイルを作成し、0.1.22 へ更新する。

**合格条件**

- [ ] `.utteran-profile.json` の不一致（lock 変更、extras 変更）が検出される
- [ ] **再構築の案内が表示される**
- [ ] 既存プロファイルが即座に FAILED 扱いにならない

### A-5. バージョンを下げるインストール（0.1.22 / ac-2）

**手順**: 0.1.22 がインストールされた状態で、0.1.21 をインストールする。
対話形式と silent の両方を試す。

**合格条件**

- [ ] 対話時: 現行版と導入版が示され、**承認を求められる**
- [ ] 対話時: 承認しなければ中止される
- [ ] silent 時: **開始前に拒否される**

---

## B. Core i7 機で確認するもの

**i7-1165G7 / Iris Xe / NVIDIA なし / RAM 16GB**

GPU 非搭載環境に固有の挙動を確認する。この環境では過去に複数の不具合が発見されている。

### B-1. デバイス検出のタイムアウト（0.1.21 / ac-1）

**背景**: 初回起動時にプローブがタイムアウトしていた（A-8）。
ウィザードの外側だけ 180秒で、環境画面の 200秒と不一致だった。
**この機での実測 94.7〜105.7秒が、200秒という値の根拠になっている。**

**手順**

1. デバイスキャッシュを削除する
2. GUI を起動し、初回の環境検出を行う
3. 完了後、「再検出」を1回実行する

**合格条件**

- [ ] 初回検出が **200秒以内**に終わる
- [ ] タイムアウトしない
- [ ] 再検出も正常に完了する
- [ ] 実行中・成功・タイムアウト・エラー・不明が**正しく区別して表示される**
- [ ] **成功が FAILED と表示されない**
- [ ] **進行中が失敗と表示されない**
- [ ] GPU 非搭載環境に適した選択肢のみが表示される

### B-2. プロセスの残留（0.1.22 / ac-2）

**背景**: Job Object による子孫プロセスの回収を実装した。
**GPU 非搭載環境では起動するプロセスの構成が異なる可能性がある。**

**手順**: 以下の各状態で GUI を閉じ、プロセスを確認する。

1. アイドル状態
2. ジョブ実行中
3. キャンセル操作の直後
4. デバイスプローブの実行中
5. **GUI を強制終了**（タスクマネージャーから）

**合格条件**

- [ ] 各終了後、起動したプロセスが**タイムアウト内に0件**になる
- [ ] GUI 本体、profile の `utteran`、プローブ、WebView2 のいずれも残らない
- [ ] 終了を待っている状態が**失敗として表示されない**
- [ ] 失敗した場合のみ診断情報が残る

### B-3. 実際の音声での成功表示（0.1.21 / ac-1）

**背景**: A-6（成功が FAILED と表示される）の修正は、自動テストで検証した。
競合はタイミング依存のため、**実際に1本通した確認に別の価値がある。**

**手順**: 短い音声を1本処理する。
**機密データは不要。**`tests/fixtures/benchmark/japanese_reference.wav`（25.825秒）で足りる。

**合格条件**

- [ ] 処理が完了し、**成功として表示される**
- [ ] ジョブ履歴に正しい状態が保存される
- [ ] GUI を再起動しても状態が正しい

---

## C. 環境を問わないもの

### C-1. 出力形式チップを含む画面のレイアウト（0.1.23 / ac-3）

**背景**: `.format-chip` 内の透明な入力要素が外側を基準に絶対配置され、横スクロールを起こしていた。
CSSの静的契約は自動テストしたが、WebView2の実描画、幅変更時の折り返し、他要素の見た目は
画面上での確認が必要である。

**手順**

1. 0.1.23 のGUIを起動し、「文字起こし」画面を開く
2. 横幅を広い状態から、操作可能な範囲で狭い状態まで段階的に変更する
3. 出力形式チップを全てON/OFFし、設定・履歴・モデル管理・処理キュー画面にも切り替える
4. 結果履歴がある場合は結果表示と再出力画面も開く

**合格条件**

- [ ] 各画面の外枠に意図しない横スクロールが発生しない
- [ ] 幅を変更しても出力形式チップが領域内で折り返され、欠けたり重なったりしない
- [ ] switch、wizard card、speaker filterなど他の絶対配置要素が意図しない位置へ移動していない

### C-2. 配布版のバージョン表示（0.1.23 / ac-3）

**背景**: source上のAPIとメタデータ整合は自動テストできるが、PyInstallerで凍結したexeが同梱metadataを
読み、Windowsのversion resourceと同じ値を表示することは配布版の実起動で確認する必要がある。

**手順**

1. `utteran-setup-0.1.23.exe`をインストールする
2. インストールしたGUIを起動し、画面左下のversionを確認する
3. `utteran-gui.exe`のプロパティを開き、「詳細」のProductVersionとFileVersionを確認する

**合格条件**

- [ ] GUIに`v0.1.23`と表示される
- [ ] ProductVersionとFileVersionがともに`0.1.23`である
- [ ] installer名、README冒頭、GUI表示、exeのversionが一致する

### C-3. 入出力ディレクトリの独立記憶（0.1.23 / ac-3）

**背景**: 設定の独立更新と入力ファイル名を保存しないことは自動テストした。native dialog、実ジョブ投入、
GUI再起動をまたぐ提示値は実際のデスクトップ操作で確認する必要がある。

**手順**

1. 入力ファイルを`C:\ac3-input-a\sample.wav`、出力先を`C:\ac3-output-a`としてジョブを投入する
2. GUIを終了して再起動し、入力と出力の初期値を確認する
3. 入力だけを`C:\ac3-input-b\sample.wav`へ変更してジョブを投入し、再起動する
4. 出力だけを`C:\ac3-output-b`へ変更してジョブを投入し、再起動する
5. GUI設定の`settings.json`を開き、入力ファイル名が記録されていないことを確認する

**合格条件**

- [ ] 初回再起動後、入力は`C:\ac3-input-a`、出力は`C:\ac3-output-a`が提示される
- [ ] 入力だけの変更後も、出力は`C:\ac3-output-a`のままである
- [ ] 出力だけの変更後も、入力は`C:\ac3-input-b`のままである
- [ ] `settings.json`に`sample.wav`や入力ファイル履歴が保存されていない

### C-4. アプリ内モーダルの描画とキーボード操作（0.1.26 / ac-6）

**背景**: DOM構造と状態遷移は自動testできるが、WebView2でのfocus表示、背景との重なり、実際の
keyboard eventとresize時の描画はdesktop上の目視・操作が必要である。

**手順**

1. GUIで存在しないpathを選んでerror modalを表示し、Enterで閉じる
2. model取得の確認を開き、背景clickとEscでそれぞれ取り消す
3. ジョブ、model、OpenVINO IRの削除確認を開き、初期focusとTab／Shift+Tabを確認する
4. 確認表示中に同じ操作buttonを連打し、閉じた後の処理回数とfocus位置を確認する
5. light／dark themeと最小幅で同じ操作を行い、出力形式chipを含む背景layoutも確認する

**合格条件**

- [ ] Enterで確定、Escと背景clickで取消でき、Tab focusがmodal外へ出ない
- [ ] 破壊的操作は取消buttonが初期focusで、取消時にAPI処理が始まらない
- [ ] modalは1つだけ表示され、連打しても処理が重複しない
- [ ] 閉じると呼出元へfocusが戻り、theme／幅によらず読める
- [ ] modal表示で既存layoutや`.format-chip`の配置が変わらない

---

## D. 配布形態・起動経路を確認するもの

### D-1. ZIP起動・データ局所化・削除（0.1.24 / ac-4）

**背景**: build成果物の構成と隔離rootの自動試験はできるが、クリーンなWindowsで初回構築を通した後、
既存のユーザー領域と比較して残留がないことは別環境での確認が必要である。

**手順**

1. クリーンな環境で`%LocalAppData%`、`%AppData%`、`%UserProfile%\.utteran`のutteran関連pathを記録する
2. `utteran-portable-0.1.24.zip`を新しいfolderへ展開し、`utteran-gui.exe`を起動する
3. CPU profileを構築し、公開fixtureで短い処理を行ってGUIを終了する
4. 展開先の`data`と、手順1で記録したユーザー領域を比較する
5. 必要な出力を別folderへ移し、展開folder全体を削除する

**合格条件**

- [ ] ZIP直下のGUIが起動し、初回setupを完了できる
- [ ] `.venvs`、model、cache、設定、履歴、log、ffmpeg、native buildが`data`配下だけに作られる
- [ ] utteran自身の新しい永続dataがユーザー領域に作られない（OS／外部softwareの記録は区別する）
- [ ] 展開folder削除後、utteran管理dataが残らない
- [ ] installer版の既存配置と動作が変わっていない

### D-2. 展開先の移動とprofile再構築案内（0.1.24 / ac-4）

**背景**: venvには絶対pathが埋まる。manifest比較は自動試験済みだが、driveをまたぐ実移動後のGUI表示と
再構築完了はWindows上で確認する必要がある。

**手順**

1. D-1でprofile構築済みの展開folderを、別driveまたはdrive letterの異なる場所へ移動する
2. GUIを起動し、環境表示を確認する
3. 案内に従って同じprofileを再構築する

**合格条件**

- [ ] 保存場所が変わったことと再構築が必要なことが表示される
- [ ] 検出直後に既存profileが削除またはFAILED扱いにならない
- [ ] 再構築後、新しい場所から処理を開始できる

### D-3. USBメモリ上の実用速度（0.1.24 / ac-4）

**背景**: USBの媒体・接続規格でI/O性能が大きく変わるため、開発環境の内蔵driveでは判断できない。

**手順**: USBメモリへZIPを展開し、CPU profile構築、GUI再起動、model読込、公開fixtureの短い処理を行い、
各所要時間とUSB規格を記録する。

**合格条件**

- [ ] 処理がI/O errorなく完了する
- [ ] 構築・起動・model読込・処理の各所要時間が記録される
- [ ] 遅くなる可能性が同梱READMEとGUIから事前に分かる

### D-4. ポータブルtokenの非永続化（0.1.24 / ac-4）

**背景**: memory-only storeは自動試験済みだが、Windows資格情報マネージャーと再起動後GUIを含む
end-to-endの非永続化は実機確認が必要である。

**手順**

1. ポータブルGUIのtoken画面でread tokenを入力し、同じ起動中にpreflightを完了する
2. GUIを終了し、Windows資格情報マネージャーと展開先のfileを確認する
3. GUIを再起動してtoken状態を確認する

**合格条件**

- [ ] 入力時に「この起動中だけ使用し、終了時に破棄」と表示される
- [ ] 同じ起動中はprofile processがtokenを利用できる
- [ ] keyringにも設定／log／別fileにもtokenが保存されない
- [ ] 再起動後は未設定となり、再入力が必要である

### D-5. Python直起動の基本動作と不足案内（0.1.25 / ac-5）

**背景**: 起動scriptの静的契約と開発環境での起動は自動確認できるが、前提条件の異なる利用者環境で
表示される案内と、通常のデスクトップ操作を含む一連の動作は各環境で確認する必要がある。

**手順**

1. Python 3.11/3.12、uv、ffmpegを導入したsource checkoutで`launch-python.bat -CheckOnly`を実行する
2. `launch-python.bat`を実行し、GUIを起動してCPU profileを構築する
3. 公開fixtureで短い処理を行い、GUIを終了する
4. 隔離環境でPython、uv、ffmpegを1つずつPATHから外し、`-CheckOnly`の案内を記録する
5. 表示された`python-launch-*.log`を開き、実行段階と失敗理由を確認する

**合格条件**

- [ ] bat経由でGUIとfaster-whisper CPU処理が完了する
- [ ] GUIとprofile CLIがそれぞれvenvの`python.exe -m`で起動する
- [ ] 実行ポリシーが恒久的に変更されていない
- [ ] Python、uv、ffmpegの各不足が名称と導入方法を含めて個別に表示される
- [ ] 前提条件が自動導入されない
- [ ] venv構築中は進行中と表示され、完了前に失敗扱いにならない
- [ ] 失敗理由、次の操作、診断logの場所が表示される
- [ ] venvはrepository配下、その他の管理dataは従来のユーザー領域に作られる

### D-6. Smart App Controlでの段階判定（0.1.25 / ac-5）

**背景**: 開発環境にはSmart App Controlが有効な機体がない。公式Pythonが起動できてもnative DLLの
検査で後段だけ阻止される可能性があるため、成功／失敗を一括せず段階ごとに記録する。

**手順**: Smart App Controlが有効な環境で`launch-python.bat`を実行し、次を順番に試す。

1. venvの`python.exe`そのもの
2. GUIの起動と表示
3. faster-whisper CPUでの公開fixture処理
4. OpenVINOおよびOpenVINO GenAIでの公開fixture処理
5. whisper.cppのnative buildと実行（最も阻止されやすい想定）

**段階記録**

- [ ] `python.exe`: 成功 / 阻止 / 不明（結果・表示: ）
- [ ] GUI: 成功 / 阻止 / 不明（結果・表示: ）
- [ ] faster-whisper CPU: 成功 / 阻止 / 不明（結果・表示: ）
- [ ] OpenVINO: 成功 / 阻止 / 不明（結果・表示: ）
- [ ] OpenVINO GenAI: 成功 / 阻止 / 不明（結果・表示: ）
- [ ] whisper.cpp build: 成功 / 阻止 / 不明（結果・表示: ）
- [ ] whisper.cpp実行: 成功 / 阻止 / 不明（結果・表示: ）

**最終判定**

- [ ] 判定A: 全経路が動作した。完全な回避策として文書を更新する
- [ ] 判定B: 一部構成のみ動作した。動作した構成だけを明示して案内する
- [ ] 判定C: `python.exe`自体が阻止された。ac-5の回避効果を否定して記録する

判定Cでも、source利用やapp exeを禁止する別policy環境向けの起動経路として成果物は維持する。

### D-7. フルパスdrag-and-dropの配布形態一致（0.1.26 / ac-6）

**背景**: 通常browserはdropしたfileのフルpathを公開しない。実装はpywebview 6.2.1とWebView2のnative
file object連携に依存し、source、installer、portableでbundle／起動条件が異なるため、Explorerからの
実操作を各形態で比較する必要がある。

**手順**

1. `launch-python.bat`、installer版、portable版を順に起動する
2. 各GUIの入力欄へ、対応file 1件、folder 1件、対応file 2件、非対応file、`.lnk`をExplorerからdropする
3. 受理した対応fileで出力先を指定してjobを投入し、終了後にGUIを再起動する
4. 各形態の`settings.json`とapp logを調べ、dropしたfile名を検索する

**合格条件**

- [ ] 3形態すべてで1 file／1 folderのフルpathが入力欄へ入り、folderはbatch入力として使える
- [ ] 複数項目、非対応形式、linkは理由をmodal表示して全件拒否し、入力値を部分更新しない
- [ ] job投入後の再起動では入力directoryだけが既定となり、file名や入力履歴を保存しない
- [ ] settingsとlogにdropしたfile名がなく、source／installer／portableで挙動が一致する
- [ ] dropできたように見えてfile名だけが入る状態や、黙って無視する状態がない

---

# 確認済みの項目

確認が完了した項目を、結果とともにここへ移す。**削除しない。**

記入例:

```
### （項目名）（版 / フェーズ）

- [x] 合格条件1
- [x] 合格条件2

**確認日**: 2026-XX-XX
**環境**: Core i7-1165G7 / Iris Xe / Windows 11
**結果**: 合格。所要 XX 秒。
**備考**: （気づいた点があれば）
```

### A-1. クリーンインストール後のアンインストール（0.1.27 / bugfix-i）

- [x] アプリ本体のディレクトリが残らない
- [x] `.venvs` が残らない
- [x] モデル、OpenVINO IR、GenAIコンパイルキャッシュが残らない
- [x] デバイスプローブのキャッシュが残らない
- [x] ネイティブビルド（`~/.utteran/native`）が残らない
- [x] ジョブ履歴、出力した文字起こし、設定が残っている
- [x] 残ったものの場所が利用者に示されている

**確認日**: 2026-09-03

**環境**: Windows 11 Business 10.0.26200 / Inno Setup 6.7.3 /
`scripts/verify_uninstaller.ps1`の隔離AppId・隔離user root

**結果**: 合格。0.1.27をclean installし、実際の凍結GUIを起動・終了した後、silentと対話の両方で
runtime errorなく完了した。対話では利用者データ4項目をすべて「いいえ」とし、残留場所の一覧と
完了画面も確認した。

**備考**: 開発環境の実データを保護するため、profile、model、OpenVINO／GenAI／device cache、native
build、job、設定、logは隔離rootに置いた代表markerで確認した。削除対象の内容ではなくpath単位の
`DelTree`／`DeleteFile`を試す検証であり、数GBの実modelは使用していない。共有`uv.exe`と利用者データは
保持され、ffmpegだけが削除された。

### A-6. 壊れた版への上書きインストールによる復旧（0.1.27 / bugfix-i）

- [x] 0.1.26が入った同じ場所へ0.1.27を上書きできる
- [x] `unins000.exe`／`unins000.dat`が修正版へ差し替わる
- [x] 上書き後のアンインストールがruntime errorなく完了する
- [x] 再生成可能dataを削除し、利用者データと共有uvを保持する

**確認日**: 2026-09-03

**環境**: Windows 11 Business 10.0.26200 / Inno Setup 6.7.3 / 隔離AppId・隔離user root

**結果**: 合格。Git上の0.1.26 `.iss`から再現した壊れた版をinstallし、同じAppId・directoryへ
0.1.27を上書きした後、silent uninstallがexit 0で完了した。手作業のregistry削除は不要。

---

# 更新の手順

## 項目を追加するとき

新しい作業で実機確認が必要な項目が生じた場合、**その作業と同じ単位で本文書へ追記する。**
後回しにしない。

追記する内容:

- **背景**（なぜ AI 側で確認できないのか）
- **手順**（実機の前で見て実行できる粒度）
- **合格条件**（チェックボックス形式）
- **どの版・フェーズで追加されたか**

## 項目を完了させるとき

1. 合格条件のチェックボックスを `- [x]` にする
2. 確認日、環境、結果を記入する
3. 「確認済みの項目」節へ移動する

**不合格だった場合はチェックを入れず**、結果と対応する課題番号を記入して未確認のまま残す。

## 関連文書

| 文書 | 役割 |
|---|---|
| `docs/archive/受入試験_手動確認手順書.md` | 操作の性質上、自動化できない項目の恒久リスト |
| `AISTATE.md` | 作業の詳細な記録。各項目の技術的背景 |
| `変更履歴.md` | 版ごとの変更内容 |
