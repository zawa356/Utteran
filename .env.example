# Hugging Face の Settings > Access Tokens で発行した読み取りトークンを指定します。
# このファイルを .env にコピーし、実際の値を設定してください。
HF_TOKEN=

# 設定値は UTTERAN_<SECTION>__<KEY> 形式で上書きできます。
# UTTERAN_ASR__MODEL=large-v3-turbo
# UTTERAN_DIARIZATION__NUM_SPEAKERS=2
