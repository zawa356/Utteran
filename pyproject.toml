[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "utteran"
version = "0.1.19"
description = "Local speaker-aware transcription CLI for audio and video files"
readme = "README.md"
requires-python = ">=3.11,<3.13"
license = { file = "LICENSE" }
authors = [{ name = "utteran contributors" }]
dependencies = [
  "huggingface-hub>=0.27,<2",
  "keyring>=25,<26",
  "platformdirs>=4,<5",
  "pydantic>=2.9,<3",
  "pydantic-settings>=2.5,<3",
  "python-dotenv>=1,<2",
  "rich>=13.9,<15",
  "typer>=0.12,<1",
]

[project.optional-dependencies]
# torch build-selecting extras: exactly one of these three may be installed at
# once (see [tool.uv] conflicts below). Each pulls pyannote.audio plus the
# matching torch/torchaudio wheel from an explicit per-build index.
cpu = [
  "faster-whisper>=1.1,<2",
  "pyannote.audio>=4,<5",
  "torch>=2.8,<2.12",
  "torchaudio>=2.8,<2.12",
]
cuda = [
  "faster-whisper>=1.1,<2",
  "pyannote.audio>=4,<5",
  "torch>=2.8,<2.12",
  "torchaudio>=2.8,<2.12",
]
xpu = [
  "faster-whisper>=1.1,<2; sys_platform == 'win32'",
  "pyannote.audio>=4,<5; sys_platform == 'win32'",
  "torch>=2.8,<2.12; sys_platform == 'win32'",
  "torchaudio>=2.8,<2.12; sys_platform == 'win32'",
  # torch's xpu wheel depends transitively on triton-xpu, but uv only
  # resolves indexed sources (see [tool.uv.sources] below) for packages
  # listed directly in a conflicting extra's own optional-dependencies -
  # a transitive-only requirement is rejected with "Source entry ... was
  # not found under the project.optional-dependencies section for that
  # extra". Windows-only: no non-Windows torch+xpu combination has been
  # verified (see AISTATE.md I-4/I-5).
  "triton-xpu; sys_platform == 'win32'",
]
# Feature-adding extras: composable with any of the above, and with each
# other. Not part of the torch conflict set.
whisper-cpp = ["cmake>=3.28,<5"]
openvino = ["openvino>=2025,<2027", "openai-whisper>=20250625", "onnxscript>=0.2,<1"]
openvino-genai = ["openvino-genai>=2026.1,<2027"]
onnx = ["onnxruntime>=1.19,<2"]
gui = [
  "fastapi>=0.115,<1",
  "pywebview>=5,<7",
  "uvicorn>=0.30,<1",
]
# Japanese linguistic boundary snapping. Setup adds this to inference profiles only;
# the isolated GUI environment intentionally remains free of inference dependencies.
japanese = [
  "SudachiPy>=0.6,<1",
  "SudachiDict-core>=20250515,<20270101",
]
# Windows-only, used solely by build.ps1 to package src/utteran_gui into the
# Phase 5d installer. Never combined with an inference profile extra - the
# distributable must not embed torch/faster-whisper/pyannote (see
# packaging/gui.spec's forbidden-module check).
build = [
  "pyinstaller>=6.11,<7; sys_platform == 'win32'",
]
dev = [
  "faster-whisper>=1.1,<2",
  "httpx>=0.27,<1",
  "mypy>=1.11,<2",
  "pytest>=8.3,<9",
  "pytest-cov>=5,<7",
  "ruff>=0.7,<1",
  "SudachiPy>=0.6,<1",
  "SudachiDict-core>=20250515,<20270101",
]

[tool.uv]
conflicts = [[{ extra = "cpu" }, { extra = "cuda" }, { extra = "xpu" }]]

[tool.uv.sources]
torch = [
  { index = "pytorch-cpu", extra = "cpu" },
  { index = "pytorch-cu126", extra = "cuda" },
  { index = "pytorch-xpu", extra = "xpu" },
]
torchaudio = [
  { index = "pytorch-cpu", extra = "cpu" },
  { index = "pytorch-cu126", extra = "cuda" },
  { index = "pytorch-xpu", extra = "xpu" },
]
triton-xpu = [{ index = "pytorch-xpu", extra = "xpu" }]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[[tool.uv.index]]
name = "pytorch-cu126"
url = "https://download.pytorch.org/whl/cu126"
explicit = true

[[tool.uv.index]]
name = "pytorch-xpu"
url = "https://download.pytorch.org/whl/xpu"
explicit = true

[project.scripts]
utteran = "utteran.cli:app"
utteran-gui = "utteran_gui.app:main"

[tool.hatch.build.targets.wheel]
packages = ["src/utteran", "src/utteran_gui"]

[tool.pytest.ini_options]
addopts = "-ra --capture=sys"
testpaths = ["tests"]
markers = [
  "requires_model: tests that require downloaded inference models",
]

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.11"
strict = true
mypy_path = "src"
packages = ["utteran", "utteran_gui"]
warn_unused_configs = true

[[tool.mypy.overrides]]
module = [
  "ctranslate2",
  "faster_whisper",
  "huggingface_hub",
  "huggingface_hub.*",
  "keyring",
  "numpy",
  "onnxruntime",
  "openvino",
  "openvino_genai",
  "pyannote.*",
  "sudachipy",
  "torch",
  "webview",
]
ignore_missing_imports = true
follow_imports = "skip"
