# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 形式に準拠します。

## [未リリース]

### 追加

- Phase 1/2受入試験指示書、専用ブランチ、環境記録付き受入試験報告書を追加。
- ID付きケース、JSONL逐次記録、resume/rerun、timeout時のprocess tree停止、peak memory、
  秘密マスク済み出力抄録に対応する受入試験ハーネスと、本文を出さない構造／統計validatorを追加。
- 実会議原本から音量統計で複数話者候補区間を選び、機能／異常系／バッチ用の派生素材を
  `output/_testdata`へ生成するスクリプトを追加。
- 受入G1で30秒MP4のASR-only CPU処理、5形式出力、JSON／字幕／UTF-8／統計検証を実施し、
  2ケース合格を報告書へ記録。
- 受入G2で3分MP4/WAV/M4Aの基本文字起こし、5形式、出力先、衝突連番、改行、BOM、
  言語指定／自動判定を検証し、16ケース合格を報告書へ記録。
- 受入G3の実pyannote初回試験で、進捗hookのpyannote 4.x互換不良により15ケース中14ケースが
  失敗した結果と再現原因を、修正前の記録として追加。
- 進捗hook修正後の受入G3再試験で、自動／固定／範囲話者数、exclusive区間、モデル明示、
  表示名置換の全15ケース合格を記録。
- 受入G4途中でレジューム段階8ケースと中断再開1ケースの合格、SIGINT終了コードとWindows
  control event伝播による2件の失敗、未実施5件を修正前の記録として追加。
- Windows control event隔離後のG4-09再試験で、実ASR処理開始付近のCtrl+Cへ60秒応答せず
  強制停止となる製品側中断問題を確認し、修正前の結果として記録。
- 受入G4ロック競合試験で、Windowsの`os.kill(pid, 0)`による所有PID生存確認がSystemErrorとなる
  失敗と原因を、修正前の結果として記録。
- 受入G5初回で、壊れたmediaをskip扱いして一部失敗exit5にならない結果と仕様差を、修正前の
  記録として追加。
- 修正後の受入G4でレジューム、段階無効化、force/no-resume、SIGINT 130、中断再開、競合／stale／
  force-unlock、corrupt、jobs clean、JSONログ秘匿の全16ケース合格を記録。
- Phase 2 実装指示書を追加し、実装計画を作業状態へ記録。
- Phase 1 の必須文書（README、変更履歴、要件定義、AI 作業状態）を作成。
- uv / src レイアウトのプロジェクト定義、MIT ライセンス、依存物の注意書き、
  `.gitignore`、`.env.example`、パッケージ骨格を追加。
- 共通データモデル、キャンセルトークン、設定モデルと優先順位、HF トークンプロバイダー、
  専用例外、トークンをマスクするコンソール／JSON ログ設定を追加。
- 設定、シリアライズ、キャンセル、ログ秘匿のモデル不要テストを追加。
- 設定、PATH、アプリケーションデータ同梱先の順で行う ffmpeg 探索と、動画を含む入力から
  16 kHz / mono / PCM 16bit WAV を安全に生成する音声正規化処理を追加。
- ffmpeg 未検出、入力未検出、デコード失敗を個別エラーとして扱い、音声処理のテストを追加。
- `ASRBackend` 抽象基底、ASR レジストリ、CPU/CUDA 対応 faster-whisper バックエンドを追加。
- faster-whisper の遅延結果を共通モデルへ変換し、単語タイムスタンプ、進捗、キャンセル、
  モデル未取得・CUDA 依存・VRAM 不足のエラー変換に対応。
- `DiarizationBackend` 抽象基底、レジストリ、pyannote.audio 4.x バックエンドを追加。
- 通常／exclusive 話者区間、話者数制約、CPU/CUDA、ローカルモデル、HF トークン・利用条件・
  キャッシュ未取得エラー、進捗、キャンセルに対応。
- 正規化済み PCM WAV をテンソルとして pyannote に渡し、TorchCodec の共有ライブラリ依存を回避。
- 設計書7章に基づく単語中心時刻の話者割当、重なり／最近傍フォールバック、話者切替分割、
  極短話者島の吸収、同一話者連結、登場順リナンバーを `align.py` に追加。
- exclusive 優先、重複話者、未割当、極短区間、結合を含むモデル不要テストを追加。
- SRT、WebVTT、schema version 1 JSON、TXT、Markdown exporter とレジストリを追加。
- 話者表示名、SRT BOM、LF/CRLF、ミリ秒桁上がり、5形式共通の衝突回避連番に対応し、
  モデル不要の書式テストを追加。
- 抽象バックエンドのみを利用する単一ファイル pipeline と、Typer の `transcribe`
  サブコマンドを追加。
- CLI 設定上書き、5形式選択、話者数制約、話者分離無効化、Rich 進捗、終了コード、
  期待済みエラーの traceback 抑制を実装し、結合テストを追加。
- ffmpeg 未検出の CLI 終了コードと、faster-whisper のキャッシュ限定ロード／モデル未取得案内の
  回帰テストを追加。
- CLI・環境変数・dotenv・TOML・既定値の全設定優先段階と、HF トークン優先順位のテストを追加。
- README を Phase 1 の実装内容に更新し、uv extras、モデル事前取得、HF 利用条件・トークン、
  ffmpeg、CLI、設定優先順位、入出力形式、検査手順、ライセンスを記載。
- 入力フィンガープリントだけで識別するジョブストア、原子的 manifest／中間 JSON、
  ステージ別 config hash、依存無効化、PID ロック、一覧・削除候補 API を追加。
- manifest の破損・実行中断・中間スキーマ不一致・ロック競合／古いロックを含む
  モデル不要のジョブ管理テストを追加。
- pipeline を永続ジョブ上の audio / asr / diarization / merge / export 段階実行へ移行し、
  完了段階の再利用、export-only 再実行、`--force` 相当の全再計算、中断再開を追加。
- 名前順のフォルダ探索、再帰、include/exclude glob、ファイル単位の失敗継続、集計、
  部分失敗終了コード 5、バッチ全体でのモデル再利用を行う逐次バッチ層を追加。
- レジューム、設定差分、強制再実行、中断、バッチ選別・集計・モデル再利用のテストを追加。
- CPU コア／AVX、CTranslate2 CUDA と compute type、cuDNN/cuBLAS、PyTorch CUDA、
  OpenVINO、ONNX Runtime、ffmpeg、バックエンド導入状況を検出する `devices.py` を追加。
- 検出結果から JSON 化可能なレポートと実際の auto 選択を導出し、環境プローブを注入できる
  モデル不要テストを追加。
- 設計書10.2の backend 別モデルカタログと、`UTTERAN_MODEL_DIR` 対応のモデル管理層を追加。
- utteran 管理領域と既存 Hugging Face キャッシュからの導入検出、明示ダウンロード、削除、
  必須ファイル／実サイズ検証、進捗・キャンセル境界を追加。
- gated モデル取得失敗をトークン未設定、未同意／権限不足、無効トークンに分類し、
  faster-whisper／pyannote が管理済みモデルパスを利用できるよう変更。
- カタログ曖昧性、保存先優先順位、検出／検証／削除、模擬取得、Hub エラー分類のテストを追加。
- `transcribe` にフォルダ入力、recursive、include/exclude、resume/no-resume、force、
  force-unlock、dry-run、yes を追加し、バッチ集計と終了コードを接続。
- `devices` と JSON 出力、`models list/download/remove/verify/path`、
  `jobs list/show/clean`、`config init/show/path` の各 CLI を追加。
- config.toml の安全な雛形作成、モデル取得前確認、ジョブ／モデル削除前の対象表示と確認、
  各サブコマンドの期待済みエラー処理とトークンマスクを追加。
- 各ジョブの `utteran.log` にステージ開始／再利用／完了／中断／失敗をマスク済みJSON Linesで
  記録し、設定したジョブ／出力ディレクトリ内の生成物を再帰バッチ候補から除外。
- quiet 時もコンソールとは独立してジョブログへ段階遷移を記録する handler と回帰テストを追加。
- devices JSON、jobs 一覧／詳細／削除、config 初期化／表示、バッチ終了コードの CLI テストを追加。
- 管理者権限不要で冪等な Windows `setup.ps1` を追加。Python/uv、profile 別 sync、ffmpeg、
  `.env`、モデル、CUDA 依存、最終 devices 診断を順に実行し、オフライン時は可能な範囲を継続。
- FFmpeg 公式ダウンロードページが案内する gyan.dev release essentials ZIP をユーザーデータへ
  配置し、配布ライセンス表示と SHA-256 検証を行うセットアップ処理を追加。
- Intel 検出／将来 backend 準備用の `intel` extra (`openvino-genai`) を追加。
- `cpu` / `cuda` / `intel` のprofile別PyTorch配布元と、CUDAカーネルprobeの回帰テストを追加。
- モデルカタログへ人間向け表示名と用途説明を追加し、`models list --available` とID省略時の
  `models download` で、番号／IDをカンマ区切り選択できる縦型一覧を追加。
- モデル選択の番号範囲、重複排除、明示ID、対話的複数取得、非対話時の取得拒否を検証する
  モデル不要テストを追加。
- `start.ps1` を追加し、文字起こし設定、モデル、デバイス、ジョブ、設定、setup、input/outputを
  番号メニューから操作できるWindows対話フロントを追加。
- 文字起こしウィザードへ入力／出力、ASR backend／モデル、デバイス、言語、話者分離モデル、
  人数、5出力形式、バッチglob、resume／force、config、ログ、dry-runの選択を追加。
- `input/.gitkeep` と `output/.gitkeep` を追加し、空フォルダを維持するGit運用を追加。
- CLIへ `--diarization-model` と `--language auto` を追加し、対話フロントから話者分離モデルと
  言語自動判定を明示できるようにした。
- pyannoteの部分取得検出・欠落ファイル表示・再取得と、Windows extended-length path変換の
  モデル不要回帰テストを追加。

### 変更

- Phase 2 の訂正仕様に従い、`job_id` を入力ハッシュのみから算出するよう要件を変更。
- ステージ別 `config_hash` の算出対象と決定的な正規化方式を要件へ明記。
- `--output-dir` 変更時に新しい保存先へ再出力されるよう、export hash の対象へ実効出力先を追加。
- バッチ一部失敗の終了コード 5、ジョブロック、`devices --json` を要件へ追加。
- faster-whisper の auto デバイス／compute type を CTranslate2 の実初期化結果に基づく選択へ変更。
  float16 非対応時は `int8_float16` / `int8`、CUDA 初期化不可時は CPU へ情報付きで退避し、
  明示指定時はフォールバックしない。
- pyannote の CUDA 選択も最小テンソル確保による初期化確認を行うよう変更。
- CTranslate2 CUDA の auto 判定は compute type に加え cuDNN / cuBLAS の解決も必須とし、
  GPU 列挙だけ可能な環境を CPU 選択と明確な警告に変更。
- README を Phase 2 の Windows setup、extras、モデル／ジョブ管理、レジューム、フォルダ処理、
  devices、config、終了コード、保存先とライセンス注意へ全面更新。
- 第三者ライセンス表へ Kotoba Whisper、OpenVINO runtime／モデル、setup が取得する
  gyan.dev FFmpeg GPLv3 build を追記。
- 利用不能な CUDA の CPU フォールバックを含め、Phase 1 の導入・実行・検証内容へ文書を同期。
- Phase 1 初回コミットに向け、秘密設定、入力メディア、生成出力、モデル、ジョブ、ログ、
  ビルド・検査キャッシュ、OS・IDE 生成物を除外するよう `.gitignore` を整理。
- Windows setupのprofileを、CPU版PyTorch、CUDA 12.6版PyTorch、CPU版PyTorch＋OpenVINOへ分離。
  CUDA wheel内のcuDNN/cuBLASをCTranslate2からも利用できるようDLL探索範囲を変更。
- PyTorch CUDA判定を、メモリ確保だけでなくCUDAカーネル実行・結果転送・同期まで行うprobeへ変更。
- Windows setupを環境構築専用とし、モデルの一覧・選択・取得を `utteran models` CLIへ分離。
  `-SkipModels`、`-Models`、`-ModelDir` と通常実行中のモデルID入力プロンプトを削除。

### 修正

- CLIの長時間pipeline／batch処理を割り込み可能なworker経由にし、CTranslate2ネイティブ推論中でも
  メインスレッドがCtrl+Cへ即応し、実CLIでは残存native threadを待たず終了コード130を返すよう修正。
- Windowsの生成済み`utteran.exe`ランチャー配下で子Pythonが継承するCtrl+C無視状態を、
  `transcribe`開始時に明示解除するよう修正。
- Windowsのジョブlock所有PID確認を`os.kill(pid, 0)`からWin32の非破壊なprocess handle／exit code
  確認へ変更し、競合時のSystemErrorを修正。
- バッチ中のデコード失敗をskipではなくfailedとして集計し、正常ファイルとの混在時はexit 5、
  全件デコード失敗時はexit 1になるよう最新受入仕様へ修正。
- バッチ全体でのASR／話者分離backendの初回loadと後続reuseを、モデルID・device付きの
  非秘密ジョブログで確認できるようにした。
- 受入試験G5に、バッチ集計・全件失敗・再帰・glob・dry-run・backend再利用・生成先除外・
  SIGINTの実環境検証を追加した。初回状態検証で、既存job作成日時と同一fingerprint入力を
  考慮していない受入検証器の不具合を記録した。
- G5受入検証器のジョブ探索をfingerprint由来IDへ合わせ、実ジョブ更新順と衝突回避を考慮した
  出力件数で検証するよう修正した。
- Windowsモデル不要試験で、導入済みpyannoteモデルに依存してトークン不足試験が失敗する
  環境依存性を記録した。
- pyannoteのトークン不足単体試験でローカルモデル探索を隔離し、導入済みモデルの有無に
  かかわらずremote model経路を検証できるようにした。
- Phase 2受入試験G5（バッチ処理）の全11観点をWindows CPU実環境で完了した。
- Phase 2受入試験G6（モデル管理）を完了し、一覧・検証・エラー分類・非対話案内を
  実環境で確認した。登録済み小容量モデルがないため破壊的な削除再取得は該当なしとした。
- Phase 2受入試験G7（デバイス・プロファイル）の全7観点をWindows CPU環境で完了した。
- Phase 2受入試験G8（設定）の全7観点を隔離した設定・dotenvで完了した。
- Phase 2受入試験G9（エラー処理・終了コード）を完了し、終了コード0/2/3/4/5/130と
  verbose時だけの予期しない例外詳細を確認した。
- Windows受入試験のCtrl+C送信先を独立consoleへ隔離し、対象プロセスの中断イベントが親の
  受入ハーネスまで伝播する問題を修正。
- 中断受入試験が前回強制停止の`asr=running`を今回の開始と誤認しないよう、manifest更新と
  新規ASR処理ログの双方を待ってからCtrl+Cを送るよう修正。
- pyannote.audio 4.xが進捗hookへ渡す`file=`キーワードを受けられるようにし、実モデルでの
  話者分離がTypeErrorになる問題を修正。正規化音声をメモリ渡しする構成では不要な
  TorchCodecデコード警告も抑制。
- `input`／`output`ディレクトリ全体の除外を、内容を除外しながら各`.gitkeep`だけ追跡する規則へ変更。

- 受入ハーネスのpeak memoryがWindows console launcherだけを測定していた問題を、全子孫processの
  working set合計をpollする方式へ修正し、PID木追跡の回帰テストを追加。
- pyannote community-1が設定ファイルしかない約1 MiBの部分取得でも「導入済み」「正常」と
  誤判定される問題を修正。必須のembedding／segmentation重みとPLDAファイルを検証し、欠落名の
  表示、同一保存先への再取得、部分取得の削除に対応。
- pyannote community-1のカタログ概算サイズを、現行スナップショットに合わせて34 MiBへ修正。
- Windowsの既定モデル保存先でHugging Faceの一時パスが260文字に達し、大きな重みだけ取得に
  失敗する問題をextended-length pathで修正。
- Typer の単一コマンド自動昇格を抑止し、`utteran transcribe` のサブコマンド構造を固定。
- config.toml の空文字 `job_dir` / ffmpeg `path` を仕様どおり自動探索として扱うよう修正。
- バックエンド例外と CLI の想定外エラーから機密値が漏れないよう、詳細の抑制とマスクを強化。
- `auto` が利用不能な CUDA を検出した場合に CPU へフォールバックし、明示的な CUDA 指定では
  依存エラーを返すよう修正。
- WSL で Windows 側 TEMP を使う場合も pytest が安定するよう、既定キャプチャ方式を `sys` に変更。
- 管理済み pyannote モデルをトークンなしの完全ローカル環境でも解決できるよう、token 必須判定の
  順序を修正。
- `config init` 雛形と組み込み既定値が一致するよう、ASR initial prompt の既定値を空文字へ修正。
- 破損 manifest を持つジョブも `corrupt` 状態で一覧表示・削除できるよう修正。
- Ctrl+C をバッチの通常失敗として継続せず、キャンセルとして即時伝播するよう修正。
- Phase 2 の最終検証として Python 3.11／3.12 のモデル不要72テスト、ruff、format、mypy、
  lock 整合性を確認し、実装と文書の状態を同期。
- WSLで作成した `.venv/lib64` をWindows版uvが削除できずsetupが失敗する問題を、既存環境を
  保持した `.venv-windows` 分離で修正。
- 依存同期失敗後にモデル／devicesを再実行し、最後に成功表示していたsetupを修正。不完全時は
  後続処理をスキップして終了コード1を返す。
- WindowsのCPU物理コア数とAVX2／AVX-512が未検出だった問題をWin32 APIによる取得で修正。
- CLIのトークン不足テストが利用者の実 `.env` に依存しないよう、一時作業ディレクトリへ隔離。
- CLIのトークン不足テストが利用者の実モデルキャッシュに依存しないよう、未取得状態を明示固定。
- Windows 3.12実機でcpu／cuda／intelの初回・冪等再実行、各profile＋devのモデル不要74テスト、
  CUDA 12.6版PyTorchによるGTX 1070 Ti上の実CUDAカーネルprobeを確認。

### 削除
