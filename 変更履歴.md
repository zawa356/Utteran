# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 形式に準拠します。

## [未リリース]

## [0.1.5] - 2026-08-24

### 修正

- Inno Setup完了画面から直接GUIを起動すると、installerのRedirectionGuardを子processが継承し、
  `uv sync`がmanaged Pythonの検査時にWindows error 448で失敗する問題を修正した。installerからの
  自動起動を廃止し、Setup終了後にStart menu／desktop shortcutから通常processとして起動する。
- 配布版だけで発生する起動元依存の再発を防ぐため、installerへ`[Run]`／`postinstall` GUI起動を
  追加できない回帰testを追加した。

## [0.1.4] - 2026-08-24

### 修正

- Windowsのクリーンインストール直後、native DLLの展開中にscanner、cache、通信などの一時的要因で
  `uv sync`が失敗すると、即座にセットアップ全体が失敗していた。locked環境への同じ同期処理を
  2秒／4秒のbackoff付きで最大3回自動再試行し、途中まで作成されたvenvをそのまま再利用する。
- 3回すべて失敗した場合はffmpeg確認や一般的なToken案内へ進まず、その場でfail-fastするようにした。
  uvの本来のエラーと各試行の終了コードがGUIの診断ログ末尾に残る。

## [0.1.3] - 2026-08-24

### 修正

- profile venv構築の失敗ログに含まれる一般的なHugging Face Token案内を、Token認証失敗と
  誤分類する問題を修正した。venv構築ではToken／model分類を無効化し、frontend側もToken画面へ
  戻す対象をmodel取得・smoke testに限定する。
- ウィザードのsubprocessが失敗した際、一般的な案内だけでなく、秘密値をmask済みの末尾ログを
  error画面へ表示するようにした。環境構築や外部commandの本来の失敗原因を画面から確認できる。

## [0.1.2] - 2026-08-24

### 追加

- セットアップウィザードをprofile→話者分離→Token→モデル→確認の入力フェーズと、venv構築→
  preflight→pyannote→ASR→smoke testの無人実行フェーズへ分離した。確認画面に所要時間・概算
  ダウンロード量・席を外せる旨を表示し、各完了段階を永続化して再起動後に続きから再開できる。
- 話者分離の選択を独立ステップにし、既定ON、必要条件、OFF時に話者ラベルが得られないこと、
  設定画面から後で有効化できることを明示した。
- 選択profileのCLIから実効Hugging Face credentialの取得元とpyannote gated modelへの
  アクセス可否を、Token本文を返さず確認する`utteran config token-status --json`とGUI preflightを
  追加した。

### 修正

- Windows配布版GUIが`utteran.exe`、`powershell.exe`、`taskkill`、`explorer.exe`を起動する際に
  黒いコンソールウィンドウが一瞬表示される問題を修正した。GUI配下の全subprocessへ共通helperから
  `CREATE_NO_WINDOW`を適用し、長時間jobでは既存の`CREATE_NEW_PROCESS_GROUP`もbit ORで維持する。
- project、CLI、GUI、installerのversionを0.1.2へ統一した。配布版GUIのサイドバーとversion APIへ
  versionを表示し、exeのProductVersion埋め込みとinstaller成果物名をbuild時に厳密検証することで、
  同じversionの古いinstallerを最新版と誤認できないようにした。
- profile venvをまだ構築していない新規実行で、保存済みの古いToken errorを再表示する問題を
  frontend側でも防止した。Token保存成功後は入力欄と永続化済みerrorを消去する。
- 中断状態が`execution`なのにprofileが未選択の場合、422応答を`[object Object]`と表示して再試行を
  繰り返す問題を修正した。不整合状態はCPU/GPU自動検出・おすすめ構成画面へ戻し、構造化API errorも
  読める文面へ整形する。profile変更時は以前のprofileに属する完了段階を破棄する。
- preflightがgated repositoryの公開メタデータだけを確認し、無効Tokenでも`available`となる
  false positiveを修正した。Token本人確認に続いて`config.yaml`をdry-runし、モデル本体を
  取得せず401（無効）とgated利用条件未同意を分類する。
- Intel profile検証で`devices --json`とtorch XPU probeを別processで重複実行していた処理を
  1回へ統合し、初回に時間がかかる旨、専用進捗stage、実測秒数を表示するようにした。
- セットアップウィザードで話者分離のON/OFFにかかわらずHF Token準備画面へ到達するよう修正し、
  話者分離ONではprofile CLIによるpreflightが成功するまでモデル取得へ進まないようにした。
- Token未設定・無効・モデル利用条件未同意・ネットワーク／profile境界の失敗をToken画面内で
  区別し、入力欄と利用条件リンクを残したまま再確認できるようにした。`HF_TOKEN`／`.env`が
  profileの実効Tokenである場合もkeyringと同じpreflightで認識する。
- ウィザード開始時刻とsmoke test成功時の完了時刻を永続化し、途中終了後は再開対象、成功後は
  完了画面のボタンを押す前に完了済みとなるよう修正した。旧版由来のwizard fieldなし既存環境は
  従来どおり自動表示しない。

## [0.1.1] - 2026-08-24

### 追加

- セットアップウィザード内の共通トークン入力を、アカウント作成・モデル利用条件同意・read token
  発行の案内、マスク入力、保存後の再取得確認、後回し経路付きで改善した。
- GUIテーマにWindows設定へ追従する`system`を追加して新しい既定とし、既存の`dark`／`light`
  明示設定は移行時も維持する。
- 配布版exeへ、実credentialを変更せず診断用資格情報でkeyringのimport/backend/get/set/deleteを
  確認する`--diagnose-keyring <json-path>`診断を追加した。

### 修正

- OS keyring利用不能をトークン未設定と誤表示していた例外握りつぶしを廃止し、利用不能状態と
  対処（Windows資格情報マネージャー、`HF_TOKEN`、`.env`）を明示するよう修正した。保存時は
  同じslotからの再取得まで成功した場合だけ設定済みとする。
- PyInstaller specでkeyring全submoduleとentry-point metadataを明示収集し、同梱欠落を検出する
  回帰テストを追加した。
- GUI設定を原子的な部分更新へ変更し、言語変更やトークン操作による再描画が入力途中の別項目を
  古い値へ戻す問題と、複数更新の競合を修正した。
- Windows CIの非対話subprocessで日本語がUnicode escapeへ変換される環境差を避けるため、
  中断終了codeの回帰テストへUTF-8入出力契約を明示した。
- Windows CIのPowerShell起動検査を、安全な非対話モードを持つscriptに限定した。全scriptの
  Windows PowerShell 5.1構文検査は維持し、native GUIとInno Setup buildは実機検証へ委ねる。
- クリーン環境で作成済みプロファイルが0件の場合、PowerShellの空配列展開により`start.ps1`が
  `.Count`参照で停止する問題を修正し、0件でもメインメニューから正常終了できるようにした。

## [0.1.0] - 2026-08-19

### 追加

- Phase 5d Windowsインストーラーを追加。`packaging/gui.spec`（PyInstaller onedir）で
  `src/utteran_gui`とFastAPI／uvicorn／pywebviewだけをビルドし、推論コア（`utteran`
  パッケージ、torch、faster-whisper、pyannote、ctranslate2）を含めないことをビルド時点と
  実ファイル走査の2重で保証する。`packaging/installer.iss`（Inno Setup、管理者権限不要、
  日本語／英語対応）が、このGUI本体と、プロファイルvenv構築に必要な最小限のソース
  （`pyproject.toml`、`uv.lock`、`src/utteran`、`src/utteran_gui`、`setup.ps1`、`run.ps1`）を
  同一ディレクトリへ配置する。アンインストール時はアプリ本体を常に削除し、GUI設定・
  プロファイル実行環境・ダウンロード済みモデル・ジョブ履歴・ffmpegは個別に削除するか
  対話確認する（既定は削除しない、uvは削除対象にしない）。署名は行わず、SHA-256の公開と
  README記載の回避手順で代替する。`build.ps1`が1コマンドでビルド・SHA-256算出まで実行する。
  推論／GUIのPythonコードは変更していない。
- Phase 5c GUI初回セットアップウィザードを追加。ハードウェア検出（GPUベンダー・メモリ・
  ディスク空き容量、推論依存を追加しない）から推奨profileの提示、venv構築、モデル取得、
  Hugging Faceトークン設定、合成音声による実動作確認までを1本の画面で案内する。
- `setup.ps1`にuvの自動導入（`Install-Uv`）を追加。GitHub最新releaseのアセットと公開
  `.sha256`をffmpegと同じ手順で検証してから展開し、ユーザー領域へ配置する。
- `setup.ps1`の`Write-Step`へ任意の`-Stage`パラメータを追加し、GUIが進捗ステージを
  正確にパースできる機械可読マーカー行を主要ステップへ付与した。
- GUIのエラー案内に`license`カテゴリを追加し、モデル利用条件未同意（`ModelAgreementError`）
  とトークン自体が無効（`HuggingFaceAuthenticationError`）を区別して表示できるようにした。
- レジューム挙動をCUDA実機で再調査し、同一条件の全stage再利用、欠落成果物／出力先変更時の
  export-only、ASR model変更時のASR以降再実行を受入ケースと調査報告へ追加。
- CLI／GUIの完了表示と進捗JSONLへ、実際のASR model・device、実行stage、cache再利用stageを追加。
- `.gitattributes`を追加し、通常textをLF、UTF-8 BOM付きPowerShellをCRLF、media／model／実行形式を
  binaryとして固定して、WindowsとUnix間の改行差分を解消。
- Phase 5b GUIへ仮想スクロール結果viewer、全文検索と一致移動／highlight、IME入力保護、
  話者／時間filter、model・device情報、話者別発話統計を追加。
- 入力名、日時、状態、model、話者数、容量のjob履歴と、状態filter、並び替え、結果／folder表示、
  容量確認付き個別削除を追加。
- `jobs list --json`／`jobs show --json`、`jobs clean --job-id --json`、`jobs export --json`を追加。
  `jobs export`は`merged.json`から形式・話者表示名・出力先を変えてexportだけを再生成する。
- 話者表示名と直近の出力指定を関連job内のschema付き`presentation.json`へ保存し、GUI全体設定へ
  参加者名を残さずjobと同時に削除できるようにした。
- Phase 5a Windows desktop GUIを追加。独立した`utteran_gui` package、FastAPIのloopback API、
  pywebview、dark/light theme、日本語/English、動的profile／model／device選択、drag and drop、
  stage進捗、SSE、詳細log、process tree中断、生成file表示を実装した。
- `setup.ps1 -Profile gui`と`gui.ps1`を追加。GUI専用`.venvs/win-gui`はPyTorchと
  faster-whisperを含まず、選択した推論profileのCLIだけをsubprocess起動する。
- `transcribe --progress-json`を追加。job/file/stage/output/error/doneをschema version 1の
  UTF-8 JSONLとしてstderrへ出し、秘密をmaskし、認識segment／word／本文を含めない契約とした。
- `profiles list --json`と`native status --json`を追加し、GUIが`devices --json`、
  `models list --json`と合わせて実行可能な選択肢を動的に構成できるようにした。
- GUI local APIへ起動ごとのsession key、全API認証、HttpOnly SameSite cookie、CSP、
  loopbackのOS割当port、CORS無効化を導入。tokenはOS keyringだけへ保存し値をAPIへ返さない。
- GUI／進捗契約のモデル不要回帰テストを追加し、Linux／Windows CIへ`gui` extraを導入した。
- `input/`のfile名とWindows user名から値を保存せずhash patternを生成し、全ref、
  Git object、commit message、差分、現在treeを照合する`private_history_match.py`を追加。
- 履歴書き換え前のbundle/mirror/復元cloneを検証する`history_backup.py`、email付きcommit
  trailerを使わない規約、安全なforce-with-lease/新repository移行手順を追加。
- 公開向けにREADMEを短く再構成し、英語short README、SemVer/release手順、現在のrelease状態、
  model利用条件の明示を追加。第三者noticeへCTranslate2、Hugging Face Hub、GGML/Silero model、
  Vulkan/shadercを追加し、codeとmodelの条件を分離して説明。
- `benchmark --durations 180,900,full`による複数長の1コマンド測定、15分未満のPhase 3d順位逆転警告、
  schema version 2 JSONを追加。既定は指定WAV全体を測り、`--apply`は最長結果と測定秒数を記録する。
- Phase 4a CIを追加。Linuxでruff/mypy/lockfile/モデル不要pytest/PowerShell BOM byte検査/
  公開履歴監査、Windowsでモデル不要pytest/全PowerShell構文検査/Windows PowerShell 5.1実起動を
  実行する。CIの保証範囲と、実質的なrelease品質保証を統合受入ハーネスが担うことを文書化。

- Phase 4a公開履歴監査として、全ref、全commit、全差分、全Git object、到達不能objectを
  一致値非保存で走査する`tools/public_history_scan.py`と回帰テストを追加。`.gitignore`が
  `.env`、input/output、venv、model、job、log、mediaを実際に除外するテストも追加。
- GitHub上のIssues/PR/comments、Actions logs、release/assets、Wiki、Discussionsを確認し、
  値を含まない監査結果を`docs/公開履歴監査_Phase4a.md`へ記録。

- Phase 3d R-4: 受入試験ハーネスの`Case`へ`requires`（profile/backends/native_variants/models/
  cuda/xpu）・`destructive`・`estimated_seconds`を追加。`devices --json`/`models list --json`の
  スナップショットと突き合わせて実行可否を判定し、不能なケースは失敗でなく理由付きskipとして
  記録する。`destructive`ケースは既定除外し明示指定でのみ実行する。`run_selected()`/`RunSummary`
  でPython APIと機械可読サマリーJSON出力を追加。P0〜P11から74ケースを新規移植し、G13へ
  `requires: {cuda: true}`を付与した。自動化困難な項目は`docs/受入試験_手動確認手順書.md`へ記録。

### 修正

- `faster-whisper:large-v3`のようなbackend修飾付きASR model IDを同backendのcanonical IDへ
  正規化し、`large-v3`との記法差だけでASR cacheが無効化される問題を修正。
- GUIの全API応答へ`Cache-Control: no-store`を追加し、結果本文や検索語をbrowserの永続storageへ
  保存しないことを回帰検査。未対応／破損した`merged.json`は結果を推測表示せず、
  破損jobからもviewerを開いて対応／検出schema versionを確認できるようにした。
- `faster-whisper`をbase依存から`cpu`／`cuda`／`xpu` hardware extrasへ移し、GUIの軽量性を保証。
  core test用の`dev` extraには明示的に含め、XPU推論依存はWindows限定としてlock解決を安定化した。
- GUIの終了案内で`MemoryBudgetError`を一般的な依存不足とせず、GPUメモリ不足とCPU切替を
  具体的に案内するようにした。
- 承認済みPhase 4a履歴cleanupで、commit messageのemail形式と過去file内のuser絶対pathを
  一般化。全branch/tagを書き換え、reflog失効・aggressive GC後の到達不能objectを0件にした。
- Git checkout内でGitが追跡しうる出力先へJSON/TXT/Markdownを作る穴を塞ぎ、
  `output`/`transcripts`/`utteran-output`の全5形式を`git check-ignore`で回帰検査。
- Linux CIで検出した、Windows専用`subprocess.CREATE_NEW_PROCESS_GROUP`をmypyが解決できない問題を
  OS条件付き`getattr`へ変更。profile directoryをWindows固定していたテストと、受入ハーネスの
  実行ファイルoverrideがLinuxで無視される問題もクロスプラットフォーム化。
- Phase 5d事前準備: `setup.ps1`自身の`Write-Host`/`Write-Step`出力が、GUIセットアップウィザードの
  ようにstdoutがpipe接続（非console）の状態で実行されると、cp932ロケールでは`[Console]::
  OutputEncoding`がOEMコードページへ既定化し、日本語進捗行が文字化けする不具合を発見・修正。
  `[Console]::IsOutputRedirected`のときだけ起動時にUTF-8を強制し、対話console実行には影響しない。
  ステージマーカー自体はASCIIのため気付かれていなかった。
- Phase 5d事前準備: `utteran_gui/processes.py`の`kill_process_tree`が、Windows上でmypyを実行すると
  `os.killpg`/`signal.SIGKILL`（POSIX専用）を解決できず失敗する問題を`getattr`ベースの解決へ修正。
  CIのmypyはLinuxでのみ実行されるため気付かれていなかった。
- Phase 5d事前準備: セットアップウィザードのプロファイル選択カードの見出しが、翻訳済み文言では
  なく生のプロファイル識別子（`cpu`/`intel`等）をそのまま表示していた問題を修正し、
  ja/en両方に翻訳ラベルを追加。真に空の`.venvs`からの初回フロー通し確認で発見。

- Phase 3d R-7: `sys.stdout`/`sys.stderr`がコンソール非接続時（パイプ・subprocess capture）に
  cp932へ既定化し、`devices --json`等の日本語を含むJSON出力が破損する（無効なJSONになる場合も
  ある）不具合を修正。CLI起動時にUTF-8へ`reconfigure`する。
- Phase 3d R-4: `setup.ps1`/`run.ps1`にUTF-8 BOMがなく、Windows PowerShell 5.1から実行すると
  構文エラーで即座に失敗する重大な不具合を修正。`start.ps1`と同じくBOM付きで追跡する。
- Phase 3d R-4最終実行: Windows PowerShell 5.1で`setup.ps1`がパイプされたUTF-8 JSONを
  cp932として復号する問題、複数行Python probeの引用符が失われる問題、`start.ps1`の3引数
  `Join-Path`非互換を修正。受入シナリオの非対話stdin、UTF-8読取り、同一job IDの直近batchログ
  集計、任意結果パスの`{results}`展開も是正した。
- G/P統合192ケースへ`requires`/`destructive`/`estimated_seconds`を明示し、G14 CUDA依存を
  NVIDIA不在時の理由付きskipへ変更。長時間・破壊的ケースを含む最終最新結果は177合格・
  15 CUDA理由付きskip・0失敗（P系77/77合格）。

### 追加

- 文字起こし完了時に音声抽出、ASR、話者分離、話者割当、出力生成と実行フェーズ合計の所要時間を
  表示する処理時間集計を追加。resume時は今回実行したフェーズだけ、一括処理時は成功ジョブの
  フェーズ別合計を表示。
- ASRと話者分離の実行先を独立指定する`--asr-device`／`--diarization-device`を追加し、
  従来の`--device`は両方へ適用する互換オプションとして維持。

- Phase 3c Step 0としてcommunity-1実モデルをArc 140TのXPUへ載せ、CPU比較とXPU同一条件2回の
  実推論で話者数・区間の一致を確認し、XPU対応可能な分岐Aと確定。
- pyannote話者分離へ`xpu`/`xpu:N`、CUDA→XPU→CPUのauto選択、実kernel probe、auto時だけの
  CPUフォールバック、intel profile不整合案内、共有RAMを考慮したOOM表示とcache解放を追加。
- `devices`の既存JSON構造を維持したままPyTorch XPUの版、デバイス名、利用可能メモリ、probe結果と
  話者分離auto選択理由を追加。

### 修正

- 長時間whisper.cpp推論で稀に生じる`start == end`のsegment/wordを共通型変換時に除外し、
  単語がなくなったsegmentはsegment単位の話者割当に退避するよう修正。
- 長い無音区間などでwhisper.cppが同一textを5回以上連続生成した場合、5回目以降を
  ハルシネーションとして除外し、連続反復を最大4segmentへ制限。
- 未ビルドのwhisper.cpp構成を明示したエラーに、対象構成を復旧する
  `utteran native build --variant <構成>`コマンドを追加。
- native manifestからprofile固有の絶対`OpenVINO_DIR`を除去し、移植可能なplaceholderとして記録。
  部分的な`native build --variant`が未指定構成のmanifest entryを消す問題も修正し、既存構成を保持する。
- `setup.ps1`がPython CLIの日本語JSONや非ASCIIユーザーパスをWindowsの異なる文字コードで
  誤デコードし、profile検証や既定profile設定に失敗する問題を修正。対象の子process入出力を
  UTF-8へ固定し、呼出元の環境変数は復元する。
- Windows対話フロントでwhisper.cppの`openvino_vulkan`等のASR専用構成がpyannoteにも渡り、
  モデル読み込み失敗になる問題を修正。話者分離デバイスはPyTorchの利用可能デバイスから別途選ぶ。
- pyannoteへ非対応デバイスを指定した際、具体的な「対応していないデバイス」エラーが一般的な
  モデル読み込み失敗へ置き換わる問題を修正。

- Phase 3b Step 1として、whisper.cppのDTWトークンを特殊トークン除外、UTF-8断片復元、
  言語別境界判定、10ミリ秒tick変換、offsets退避、segment範囲clamp付きで共通Wordへ変換する
  基盤とモデル不要試験を追加。
- `[asr].word_timestamps`の`auto`/`always`/`never`と`[asr.whisper_cpp]`設定を追加。
- Hugging Face APIで実在とサイズを確認したwhisper.cpp GGML 33件をカタログへ追加し、
  推奨6件の既定表示、`--all`全件表示、単一ファイル取得・検証に対応。
- `models prepare-openvino` / `list-openvino` / `remove-openvino`と、モデルサイズ単位IR、
  量子化GGML向けalias、IR整合性検証、PyTorchキャッシュ削除を追加。
- upstream v1.9.1に残るOpenVINO import互換性とWindows一時ONNX削除失敗を直した変換scriptを
  vendor同梱。
- manifest解決、構成別v1.9.1引数、動的OpenVINO DLL、full JSON変換、進捗、秘密値マスク、
  process treeキャンセルに対応するwhisper.cpp ASR backendを追加。
- CUDA→OpenVINO+Vulkan→Vulkan→OpenVINO→faster-whisper CPUのauto選択と、auto時だけの
  GPU初期化失敗1回フォールバックを追加。
- `models list --json`を追加し、start.ps1のASR backend・導入済みモデル・device/native構成を
  `devices --json`と組み合わせて現在profileから動的生成するよう変更。
- Windowsの非ASCIIモデルパスでwhisper.cpp v1.9.1がネイティブ終了する問題を、推論中だけ
  GGML/IRをASCII-safe一時パスへハードリンクする方式で修正。
- Windows cp932環境でPyTorch ONNX exporterのUnicode進捗表示がIR変換を失敗させる問題を、
  converter subprocessの`PYTHONIOENCODING=utf-8`固定で修正。
- Intel Arc 140T実機でnative 4構成、large-v3-turbo-q5_0、OpenVINO IR、DTW、auto選択、
  3分TS有無性能を検証。

- Phase 1/2受入試験指示書、専用ブランチ、環境記録付き受入試験報告書を追加。
- ID付きケース、JSONL逐次記録、resume/rerun、timeout時のprocess tree停止、peak memory、
  秘密マスク済み出力抄録に対応する受入試験ハーネスと、本文を出さない構造／統計validatorを追加。
- 実会議原本から音量統計で複数話者候補区間を選び、機能／異常系／バッチ用の派生素材を
  `output/_testdata`へ生成するスクリプトを追加。
- 受入G1で30秒MP4のASR-only CPU処理、5形式出力、JSON／字幕／UTF-8／統計検証を実施し、
  2ケース合格を報告書へ記録。
- 受入G2で3分MP4/WAV/M4Aの基本文字起こし、5形式、出力先、衝突連番、改行、BOM、
  言語指定／自動判定を検証し、16ケース合格を報告書へ記録。
- 受入G3の実pyannote初回試験で、進捗hookのpyannote 4.x互換不良により15ケース中14ケースが
  失敗した結果と再現原因を、修正前の記録として追加。
- 進捗hook修正後の受入G3再試験で、自動／固定／範囲話者数、exclusive区間、モデル明示、
  表示名置換の全15ケース合格を記録。
- 受入G4途中でレジューム段階8ケースと中断再開1ケースの合格、SIGINT終了コードとWindows
  control event伝播による2件の失敗、未実施5件を修正前の記録として追加。
- Windows control event隔離後のG4-09再試験で、実ASR処理開始付近のCtrl+Cへ60秒応答せず
  強制停止となる製品側中断問題を確認し、修正前の結果として記録。
- 受入G4ロック競合試験で、Windowsの`os.kill(pid, 0)`による所有PID生存確認がSystemErrorとなる
  失敗と原因を、修正前の結果として記録。
- 受入G5初回で、壊れたmediaをskip扱いして一部失敗exit5にならない結果と仕様差を、修正前の
  記録として追加。
- 修正後の受入G4でレジューム、段階無効化、force/no-resume、SIGINT 130、中断再開、競合／stale／
  force-unlock、corrupt、jobs clean、JSONログ秘匿の全16ケース合格を記録。
- 受入G14へ3分CUDA性能測定、全結果集約、2時間ジョブ保持、fixture容量、CUDA自動選択、
  最終秘密値走査を追加。初回は成果物検証器へ未定義引数を渡した試験基盤不具合により、
  9ケース中8ケース合格・1ケース失敗として修正前の結果を記録。
- Phase 1/2受入試験G0〜G14を完了し、各IDの最新115ケース全件合格、実行環境、不具合対応、
  未実施理由、3分／約2時間19分のCPU/CUDA性能を`docs/受入試験報告.md`へ集約。
- READMEへGTX 1070 Ti実機での3分／約2時間19分の処理時間、実時間比、ピークRAMの目安を追加。
- 最終品質ゲートとしてWindows 92件、WSL 90件（Windows専用2件skip）のモデル不要試験、
  両OSのruff／format／mypy、lock整合性、CUDA自動選択、秘密値走査を確認。
- 受入試験報告へ試験終了コミット`982c5f5`を記録し、セッション再開情報を最終同期。
- Phase 2 実装指示書を追加し、実装計画を作業状態へ記録。
- Phase 1 の必須文書（README、変更履歴、要件定義、AI 作業状態）を作成。
- uv / src レイアウトのプロジェクト定義、MIT ライセンス、依存物の注意書き、
  `.gitignore`、`.env.example`、パッケージ骨格を追加。
- 共通データモデル、キャンセルトークン、設定モデルと優先順位、HF トークンプロバイダー、
  専用例外、トークンをマスクするコンソール／JSON ログ設定を追加。
- 設定、シリアライズ、キャンセル、ログ秘匿のモデル不要テストを追加。
- 設定、PATH、アプリケーションデータ同梱先の順で行う ffmpeg 探索と、動画を含む入力から
  16 kHz / mono / PCM 16bit WAV を安全に生成する音声正規化処理を追加。
- ffmpeg 未検出、入力未検出、デコード失敗を個別エラーとして扱い、音声処理のテストを追加。
- `ASRBackend` 抽象基底、ASR レジストリ、CPU/CUDA 対応 faster-whisper バックエンドを追加。
- faster-whisper の遅延結果を共通モデルへ変換し、単語タイムスタンプ、進捗、キャンセル、
  モデル未取得・CUDA 依存・VRAM 不足のエラー変換に対応。
- `DiarizationBackend` 抽象基底、レジストリ、pyannote.audio 4.x バックエンドを追加。
- 通常／exclusive 話者区間、話者数制約、CPU/CUDA、ローカルモデル、HF トークン・利用条件・
  キャッシュ未取得エラー、進捗、キャンセルに対応。
- 正規化済み PCM WAV をテンソルとして pyannote に渡し、TorchCodec の共有ライブラリ依存を回避。
- 設計書7章に基づく単語中心時刻の話者割当、重なり／最近傍フォールバック、話者切替分割、
  極短話者島の吸収、同一話者連結、登場順リナンバーを `align.py` に追加。
- exclusive 優先、重複話者、未割当、極短区間、結合を含むモデル不要テストを追加。
- SRT、WebVTT、schema version 1 JSON、TXT、Markdown exporter とレジストリを追加。
- 話者表示名、SRT BOM、LF/CRLF、ミリ秒桁上がり、5形式共通の衝突回避連番に対応し、
  モデル不要の書式テストを追加。
- 抽象バックエンドのみを利用する単一ファイル pipeline と、Typer の `transcribe`
  サブコマンドを追加。
- CLI 設定上書き、5形式選択、話者数制約、話者分離無効化、Rich 進捗、終了コード、
  期待済みエラーの traceback 抑制を実装し、結合テストを追加。
- ffmpeg 未検出の CLI 終了コードと、faster-whisper のキャッシュ限定ロード／モデル未取得案内の
  回帰テストを追加。
- CLI・環境変数・dotenv・TOML・既定値の全設定優先段階と、HF トークン優先順位のテストを追加。
- README を Phase 1 の実装内容に更新し、uv extras、モデル事前取得、HF 利用条件・トークン、
  ffmpeg、CLI、設定優先順位、入出力形式、検査手順、ライセンスを記載。
- 入力フィンガープリントだけで識別するジョブストア、原子的 manifest／中間 JSON、
  ステージ別 config hash、依存無効化、PID ロック、一覧・削除候補 API を追加。
- manifest の破損・実行中断・中間スキーマ不一致・ロック競合／古いロックを含む
  モデル不要のジョブ管理テストを追加。
- pipeline を永続ジョブ上の audio / asr / diarization / merge / export 段階実行へ移行し、
  完了段階の再利用、export-only 再実行、`--force` 相当の全再計算、中断再開を追加。
- 名前順のフォルダ探索、再帰、include/exclude glob、ファイル単位の失敗継続、集計、
  部分失敗終了コード 5、バッチ全体でのモデル再利用を行う逐次バッチ層を追加。
- レジューム、設定差分、強制再実行、中断、バッチ選別・集計・モデル再利用のテストを追加。
- CPU コア／AVX、CTranslate2 CUDA と compute type、cuDNN/cuBLAS、PyTorch CUDA、
  OpenVINO、ONNX Runtime、ffmpeg、バックエンド導入状況を検出する `devices.py` を追加。
- 検出結果から JSON 化可能なレポートと実際の auto 選択を導出し、環境プローブを注入できる
  モデル不要テストを追加。
- 設計書10.2の backend 別モデルカタログと、`UTTERAN_MODEL_DIR` 対応のモデル管理層を追加。
- utteran 管理領域と既存 Hugging Face キャッシュからの導入検出、明示ダウンロード、削除、
  必須ファイル／実サイズ検証、進捗・キャンセル境界を追加。
- gated モデル取得失敗をトークン未設定、未同意／権限不足、無効トークンに分類し、
  faster-whisper／pyannote が管理済みモデルパスを利用できるよう変更。
- カタログ曖昧性、保存先優先順位、検出／検証／削除、模擬取得、Hub エラー分類のテストを追加。
- `transcribe` にフォルダ入力、recursive、include/exclude、resume/no-resume、force、
  force-unlock、dry-run、yes を追加し、バッチ集計と終了コードを接続。
- `devices` と JSON 出力、`models list/download/remove/verify/path`、
  `jobs list/show/clean`、`config init/show/path` の各 CLI を追加。
- config.toml の安全な雛形作成、モデル取得前確認、ジョブ／モデル削除前の対象表示と確認、
  各サブコマンドの期待済みエラー処理とトークンマスクを追加。
- 各ジョブの `utteran.log` にステージ開始／再利用／完了／中断／失敗をマスク済みJSON Linesで
  記録し、設定したジョブ／出力ディレクトリ内の生成物を再帰バッチ候補から除外。
- quiet 時もコンソールとは独立してジョブログへ段階遷移を記録する handler と回帰テストを追加。
- devices JSON、jobs 一覧／詳細／削除、config 初期化／表示、バッチ終了コードの CLI テストを追加。
- 管理者権限不要で冪等な Windows `setup.ps1` を追加。Python/uv、profile 別 sync、ffmpeg、
  `.env`、モデル、CUDA 依存、最終 devices 診断を順に実行し、オフライン時は可能な範囲を継続。
- FFmpeg 公式ダウンロードページが案内する gyan.dev release essentials ZIP をユーザーデータへ
  配置し、配布ライセンス表示と SHA-256 検証を行うセットアップ処理を追加。
- Intel 検出／将来 backend 準備用の `intel` extra (`openvino-genai`) を追加。
- `cpu` / `cuda` / `intel` のprofile別PyTorch配布元と、CUDAカーネルprobeの回帰テストを追加。
- モデルカタログへ人間向け表示名と用途説明を追加し、`models list --available` とID省略時の
  `models download` で、番号／IDをカンマ区切り選択できる縦型一覧を追加。
- モデル選択の番号範囲、重複排除、明示ID、対話的複数取得、非対話時の取得拒否を検証する
  モデル不要テストを追加。
- `start.ps1` を追加し、文字起こし設定、モデル、デバイス、ジョブ、設定、setup、input/outputを
  番号メニューから操作できるWindows対話フロントを追加。
- 文字起こしウィザードへ入力／出力、ASR backend／モデル、デバイス、言語、話者分離モデル、
  人数、5出力形式、バッチglob、resume／force、config、ログ、dry-runの選択を追加。
- `input/.gitkeep` と `output/.gitkeep` を追加し、空フォルダを維持するGit運用を追加。
- CLIへ `--diarization-model` と `--language auto` を追加し、対話フロントから話者分離モデルと
  言語自動判定を明示できるようにした。
- pyannoteの部分取得検出・欠落ファイル表示・再取得と、Windows extended-length path変換の
  モデル不要回帰テストを追加。
- Phase 3a事前調査（Step 0）を実施し、whisper.cppバージョン選定、単語タイムスタンプ取得可否、
  Vulkanビルド前提、uv conflicting extras/explicit indexの実挙動、torch XPUとopenai-whisperの
  依存衝突、pyannote 4.0.7のXPU動作可否、ディスク使用量を実機検証してAISTATE.mdへ記録した。
- `pyproject.toml` のextrasを、torchビルドを選ぶ排他extras（`cpu`/`cuda`/`xpu`）と機能を
  追加する非排他extras（`whisper-cpp`/`openvino`/`onnx`）へ再編し、`[tool.uv] conflicts`と
  explicit indexを設定した。`xpu`extraには推移的依存の`triton-xpu`をWindows限定で追加した。
- `src/utteran/profiles.py` を追加し、プロファイル定義（`cpu`/`cuda`/`intel`/`vulkan`）、
  `<venv_root>/<os>-<profile>` のvenvレイアウト、既定プロファイル解決、現在プロファイル取得
  （`UTTERAN_PROFILE`）を実装した。
- `src/utteran/native.py` を追加し、whisper.cpp（`v1.9.1`固定）の取得と`cpu`/`openvino`/
  `vulkan`/`openvino_vulkan`構成のビルド、前提条件判定、manifest記録、実行時ライブラリ
  ディレクトリの動的解決を実装した。
- `config.toml` へ `[general].venv_dir` / `native_dir` / `default_profile` を追加した。
- CLIへ `utteran profiles list/current/path` と `utteran native build/status/clean` を追加した。
- `setup.ps1` を全面改修し、`-Profile cpu|cuda|intel|vulkan` によるプロファイル別venv作成・
  更新、`-List`/`-Remove`/`-SetDefault`/`-VenvDir` を追加した。
- `run.ps1` を新規追加し、指定または既定プロファイルのvenv内`utteran`を実行できるようにした。
- `start.ps1` に「プロファイル管理」メニュー（一覧・セッション内選択・作成／更新・既定設定・
  削除）を追加し、旧`.venv`/`.venv-windows`前提の起動解決をプロファイルモデルへ置き換えた。
- `utteran devices` に現在のプロファイルと他プロファイル一覧、Vulkanのビルド前提／ランタイム
  検出、ネイティブビルド状態を追加した（既存のJSON構造は変更せず追加のみ）。
- 要件定義に19章「実行環境の分離」と20章「ネイティブビルド」を新設し、14章の依存関係表を
  新extras構成へ更新した。

### 変更

- Phase 2 の訂正仕様に従い、`job_id` を入力ハッシュのみから算出するよう要件を変更。
- ステージ別 `config_hash` の算出対象と決定的な正規化方式を要件へ明記。
- `--output-dir` 変更時に新しい保存先へ再出力されるよう、export hash の対象へ実効出力先を追加。
- バッチ一部失敗の終了コード 5、ジョブロック、`devices --json` を要件へ追加。
- faster-whisper の auto デバイス／compute type を CTranslate2 の実初期化結果に基づく選択へ変更。
  float16 非対応時は `int8_float16` / `int8`、CUDA 初期化不可時は CPU へ情報付きで退避し、
  明示指定時はフォールバックしない。
- pyannote の CUDA 選択も最小テンソル確保による初期化確認を行うよう変更。
- CTranslate2 CUDA の auto 判定は compute type に加え cuDNN / cuBLAS の解決も必須とし、
  GPU 列挙だけ可能な環境を CPU 選択と明確な警告に変更。
- README を Phase 2 の Windows setup、extras、モデル／ジョブ管理、レジューム、フォルダ処理、
  devices、config、終了コード、保存先とライセンス注意へ全面更新。
- 第三者ライセンス表へ Kotoba Whisper、OpenVINO runtime／モデル、setup が取得する
  gyan.dev FFmpeg GPLv3 build を追記。
- 利用不能な CUDA の CPU フォールバックを含め、Phase 1 の導入・実行・検証内容へ文書を同期。
- Phase 1 初回コミットに向け、秘密設定、入力メディア、生成出力、モデル、ジョブ、ログ、
  ビルド・検査キャッシュ、OS・IDE 生成物を除外するよう `.gitignore` を整理。
- Windows setupのprofileを、CPU版PyTorch、CUDA 12.6版PyTorch、CPU版PyTorch＋OpenVINOへ分離。
  CUDA wheel内のcuDNN/cuBLASをCTranslate2からも利用できるようDLL探索範囲を変更。
- PyTorch CUDA判定を、メモリ確保だけでなくCUDAカーネル実行・結果転送・同期まで行うprobeへ変更。
- Windows setupを環境構築専用とし、モデルの一覧・選択・取得を `utteran models` CLIへ分離。
  `-SkipModels`、`-Models`、`-ModelDir` と通常実行中のモデルID入力プロンプトを削除。
- `pyannote`/`intel` extrasを廃止し、`cpu`/`cuda`/`xpu`（排他）と`whisper-cpp`/`openvino`/
  `onnx`（非排他）へ再編した。READMEの「`cpu`、`cuda`、`intel`は相互に切り替えて使用します」
  という単一venv前提の記述を、プロファイル別venvモデルの説明へ全面的に更新した。
- 単一`.venv`/`.venv-windows`前提だった`setup.ps1`/`start.ps1`の実行環境解決を、
  `.venvs/<os>-<profile>`のプロファイル別venvモデルへ変更した。既存の`.venv`/`.venv-windows`は
  削除・変更せず、検出時に手動削除の案内のみ表示する。

### 修正

- 利用者の実運用で`kotoba-whisper-v2.0`使用時、単語タイムスタンプ計算中にCTranslate2が
  ネイティブクラッシュ（Windows終了コード`-1073741819`、Pythonから捕捉不能）する不具合を修正。
  原因は当該モデル（distil-whisper系、decoder 2層）の`alignment_heads`が、蒸留前の深いdecoder
  向けの値（layer 7〜25参照）のまま上流で配布されており、実在しないdecoder層への範囲外
  アクセスを引き起こしていたこと。`models download`完了後と`models verify`実行時に、
  CTranslate2形式モデルへ隔離subprocessで単語タイムスタンプ計算を1回試行し、ネイティブ
  クラッシュを検知した場合のみ`alignment_heads`を安全な既定値へ自動修正するようにした。
  Python例外による検証不能時は誤って書き換えない。モデル不要回帰テストを追加。
- G14のJSON単独成果物検証を`formats`の未定義引数から`json` validatorへ修正し、CUDA話者付き
  成果物の時間範囲・segment数・話者数を正常に検証できるようにした。
- CLIの長時間pipeline／batch処理を割り込み可能なworker経由にし、CTranslate2ネイティブ推論中でも
  メインスレッドがCtrl+Cへ即応し、実CLIでは残存native threadを待たず終了コード130を返すよう修正。
- Windowsの生成済み`utteran.exe`ランチャー配下で子Pythonが継承するCtrl+C無視状態を、
  `transcribe`開始時に明示解除するよう修正。
- Windowsのジョブlock所有PID確認を`os.kill(pid, 0)`からWin32の非破壊なprocess handle／exit code
  確認へ変更し、競合時のSystemErrorを修正。
- バッチ中のデコード失敗をskipではなくfailedとして集計し、正常ファイルとの混在時はexit 5、
  全件デコード失敗時はexit 1になるよう最新受入仕様へ修正。
- バッチ全体でのASR／話者分離backendの初回loadと後続reuseを、モデルID・device付きの
  非秘密ジョブログで確認できるようにした。
- 受入試験G5に、バッチ集計・全件失敗・再帰・glob・dry-run・backend再利用・生成先除外・
  SIGINTの実環境検証を追加した。初回状態検証で、既存job作成日時と同一fingerprint入力を
  考慮していない受入検証器の不具合を記録した。
- G5受入検証器のジョブ探索をfingerprint由来IDへ合わせ、実ジョブ更新順と衝突回避を考慮した
  出力件数で検証するよう修正した。
- Windowsモデル不要試験で、導入済みpyannoteモデルに依存してトークン不足試験が失敗する
  環境依存性を記録した。
- pyannoteのトークン不足単体試験でローカルモデル探索を隔離し、導入済みモデルの有無に
  かかわらずremote model経路を検証できるようにした。
- Phase 2受入試験G5（バッチ処理）の全11観点をWindows CPU実環境で完了した。
- Phase 2受入試験G6（モデル管理）を完了し、一覧・検証・エラー分類・非対話案内を
  実環境で確認した。登録済み小容量モデルがないため破壊的な削除再取得は該当なしとした。
- Phase 2受入試験G7（デバイス・プロファイル）の全7観点をWindows CPU環境で完了した。
- Phase 2受入試験G8（設定）の全7観点を隔離した設定・dotenvで完了した。
- Phase 2受入試験G9（エラー処理・終了コード）を完了し、終了コード0/2/3/4/5/130と
  verbose時だけの予期しない例外詳細を確認した。
- G10セキュリティ受入に、ダミートークンの通常・verbose・例外・ジョブログでのマスクと
  生成物／Git ignore走査を追加し、初回走査で設定キー名を値と誤認する偽陽性を記録した。
- 受入セキュリティ走査を実トークン相当の長さに限定し、`hf_token`設定キー名の偽陽性を修正した。
- Phase 2受入試験G10（セキュリティ）を完了し、ダミートークン全経路マスク、生成物走査、
  private pathのGit ignoreを確認した。
- G11文書整合性受入にREADME CLI例の安全な実行とコード契約照合を追加し、初回に位置引数を
  optionと誤認する受入器不具合と例番号不足を記録した。
- G11再試験で、要件定義のconfig例からoutput 2項目とalignment 5項目が欠落している
  文書不一致、およびWindows受入器のglob引用符欠落を記録した。
- 要件定義のconfig例へ実装済みoutput/alignment設定を補い、READMEへ一般設定とトークンの
  優先経路の区別および完全な終了コード表を追加した。
- Phase 2受入試験G11（文書整合性）を完了し、README例27件と全CLI／設定契約を確認した。
- Phase 2受入試験G12（start.ps1）を完了し、構文・コマンド構築・read-onlyメニュー・
  文字起こしdry-runをWindows PowerShellで確認した。
- G13耐久試験用ハーネスへWDDM対応のGPU全体VRAM baseline/peak/delta測定と、構造化ログからの
  ASR／話者分離モデルロード時間抽出を追加した。
- Phase 2受入試験G13をCUDA実機の約2時間19分ファイルで完了し、話者分離なし／あり、
  全5形式、品質統計、RAM/VRAM、モデルロード時間、全stage再利用を確認した。
- Windows受入試験のCtrl+C送信先を独立consoleへ隔離し、対象プロセスの中断イベントが親の
  受入ハーネスまで伝播する問題を修正。
- 中断受入試験が前回強制停止の`asr=running`を今回の開始と誤認しないよう、manifest更新と
  新規ASR処理ログの双方を待ってからCtrl+Cを送るよう修正。
- pyannote.audio 4.xが進捗hookへ渡す`file=`キーワードを受けられるようにし、実モデルでの
  話者分離がTypeErrorになる問題を修正。正規化音声をメモリ渡しする構成では不要な
  TorchCodecデコード警告も抑制。
- `input`／`output`ディレクトリ全体の除外を、内容を除外しながら各`.gitkeep`だけ追跡する規則へ変更。

- 受入ハーネスのpeak memoryがWindows console launcherだけを測定していた問題を、全子孫processの
  working set合計をpollする方式へ修正し、PID木追跡の回帰テストを追加。
- pyannote community-1が設定ファイルしかない約1 MiBの部分取得でも「導入済み」「正常」と
  誤判定される問題を修正。必須のembedding／segmentation重みとPLDAファイルを検証し、欠落名の
  表示、同一保存先への再取得、部分取得の削除に対応。
- pyannote community-1のカタログ概算サイズを、現行スナップショットに合わせて34 MiBへ修正。
- Windowsの既定モデル保存先でHugging Faceの一時パスが260文字に達し、大きな重みだけ取得に
  失敗する問題をextended-length pathで修正。
- Typer の単一コマンド自動昇格を抑止し、`utteran transcribe` のサブコマンド構造を固定。
- config.toml の空文字 `job_dir` / ffmpeg `path` を仕様どおり自動探索として扱うよう修正。
- バックエンド例外と CLI の想定外エラーから機密値が漏れないよう、詳細の抑制とマスクを強化。
- `auto` が利用不能な CUDA を検出した場合に CPU へフォールバックし、明示的な CUDA 指定では
  依存エラーを返すよう修正。
- WSL で Windows 側 TEMP を使う場合も pytest が安定するよう、既定キャプチャ方式を `sys` に変更。
- 管理済み pyannote モデルをトークンなしの完全ローカル環境でも解決できるよう、token 必須判定の
  順序を修正。
- `config init` 雛形と組み込み既定値が一致するよう、ASR initial prompt の既定値を空文字へ修正。
- 破損 manifest を持つジョブも `corrupt` 状態で一覧表示・削除できるよう修正。
- Ctrl+C をバッチの通常失敗として継続せず、キャンセルとして即時伝播するよう修正。
- Phase 2 の最終検証として Python 3.11／3.12 のモデル不要72テスト、ruff、format、mypy、
  lock 整合性を確認し、実装と文書の状態を同期。
- WSLで作成した `.venv/lib64` をWindows版uvが削除できずsetupが失敗する問題を、既存環境を
  保持した `.venv-windows` 分離で修正。
- 依存同期失敗後にモデル／devicesを再実行し、最後に成功表示していたsetupを修正。不完全時は
  後続処理をスキップして終了コード1を返す。
- WindowsのCPU物理コア数とAVX2／AVX-512が未検出だった問題をWin32 APIによる取得で修正。
- CLIのトークン不足テストが利用者の実 `.env` に依存しないよう、一時作業ディレクトリへ隔離。
- CLIのトークン不足テストが利用者の実モデルキャッシュに依存しないよう、未取得状態を明示固定。
- Windows 3.12実機でcpu／cuda／intelの初回・冪等再実行、各profile＋devのモデル不要74テスト、
  CUDA 12.6版PyTorchによるGTX 1070 Ti上の実CUDAカーネルprobeを確認。
- Phase 3a実装中、`native.py`の`ProcessRunner`が`subprocess.run(text=True)`のロケール依存
  デコードを使っており、日本語（cp932）Windows環境でcmake/gitの出力デコード中に
  `UnicodeDecodeError`がsubprocess読み取りスレッド内で発生し、出力が失われる問題を実機で発見。
  `encoding="utf-8", errors="replace"`明示指定で修正した（リリース前に自己検出・修正）。
- Phase 3a実装中、`setup.ps1`のプロファイル別`uv sync`呼び出しが`sync`引数を二重指定しており
  `uv`がexit 2で失敗する問題を実機検証で発見し修正した（リリース前に自己検出・修正）。
- Phase 3a最終回帰確認中、`run.ps1`が`.ps1`起動特有のPowerShell引数解釈により、
  引用符なしのカンマ区切り値（`--format srt,vtt,json,txt,md`等）を空白区切りへ変えてしまい
  設定エラーになる問題を実モデルでの実機文字起こしテストで発見。配列型要素を`,`で
  再結合する処理を追加して修正した（リリース前に自己検出・修正）。
- Phase 3a最終回帰確認中、`run.ps1`／`start.ps1`がプロファイル起動時に`UTTERAN_PROFILE`
  環境変数を設定しておらず、`utteran profiles current`／`devices`の現在プロファイル表示が
  常に「不明」になる問題を発見し、起動直前に設定するよう修正した
  （リリース前に自己検出・修正）。

### 削除
# 2026-08-04 Phase 3d 是正（作業中）

- whisper.cpp v1.9.1の確認済み対策を設定化し、反復抑制の閾値・無効化と警告を追加。
- ジョブと認識本文を保存しない`benchmark`を追加（warmup、反復中央値、JSON、apply）。
- Intel実測に基づきwhisper.cpp autoをVulkan優先へ変更。
- Phase 3報告の構成間一致度placeholderとPython実行版の説明を是正。
- 24分46秒実音声でflash/DTW切り分けとC0〜C6個別効果測定を実施し、前文脈遮断とVADの
  単独効果を確認。反復保険既定を10へ緩和。
- Silero VAD v6.2.0をモデルカタログ、download、verify、自動解決へ統合。非ASCIIパスのnative
  crashと不正UTF-8 JSONを実測中に検出し修正。
- 25/50/100分連結音声でVulkan ASRとXPU話者分離、25/50分でCPU話者分離の時間・peak RAMを測定。
- Phase 1/2 G系とPhase 3 P14耐久ケースを単一ハーネスへ統合し、group実行と環境動的解決を整備。
# 2026-08-05 Phase 4b Step 0

- pyannote.audio 4.0.7 の segmentation、embedding、clustering、通常／exclusive再構成を
  実コードと合成音声で調査した。
- 分割処理には非公開属性と pipeline 本体ロジックへの強い依存が必要なため分岐Bとし、
  Phase 4b での分割実装を見送った。根拠と将来実装の規模・リスクを調査報告へ記録した。

# 2026-08-05 Phase 4b Step 1

- 専用VRAM、共有メモリ、システムRAMを区別するメモリ予算と安全率、R-5実測に基づく
  stage別ピーク推定、3点以上で切り替わる外れ値耐性付きキャリブレーションを追加した。
- profile共通のplatformdirs領域へ内容・パスを含まない実測点を保存し、`utteran memory show/reset`
  とprocess tree working set測定を追加した。

# 2026-08-05 Phase 4b Step 2

- 話者分離前のsafe/danger/impossible/unknown判定、auto限定の安全なCPU退避、明示deviceを
  変更しない警告・停止、理解可能な対処案を実装した。
- GPU／共有RAM／システムRAMのOOMを捕捉し、auto時だけCPUへ最大1回再試行する保険を追加した。
  退避と実測peakは中間結果、最終JSON、ジョブログへ保存し、音声・ASRのresumeを維持する。
- 検証用に`UTTERAN_DEBUG_MEMORY_BUDGET_GIB`で予算を上書きできるようにした。

# 2026-08-05 Phase 4b Step 4・文書

- 3分実機fixtureと人工予算でsafe、danger auto退避、danger明示継続、impossible事前停止を確認した。
  XPU allocator制限による実OOMも捕捉し、CPUへの1回再試行、ASR resume、全stage完走を確認した。
- R-5の25/50/100分byte実測に対する推定誤差と、新規3分点によるローカル補正改善を記録した。
- OOM後CPU測定にはGPU working setが混ざるため、job peakには残してもキャリブレーションから
  除外するよう是正した。
- 受入P9で同じ3分付近の点だけから回帰した傾きが暴走する不具合を検出し、ローカル式への切替に
  音声長span 5分以上を追加した。修正後の失敗ケース再実行を含むID別最新7/7件が合格した。
- READMEのメモリ要件・動作、要件定義13章の予算・式・補正・判定・退避・分岐B仕様、AISTATE、
  検証報告を更新した。
