# utteran 設計書 / 要件定義

> このドキュメントは utteran リポジトリの `要件定義.md` として配置する。
> 仕様に変更が生じた場合、実装と同じ作業単位で必ず本書を更新すること。

---

## 1. 概要

### 1.1 目的

音声・動画ファイルから、話者を区別した文字起こしを生成するローカル実行ツール。
会議・インタビュー・講演の記録作成を主用途とする。

### 1.2 スコープ

**対象とするもの**

- ファイル入力によるバッチ処理（単一ファイル / フォルダ再帰 / キュー）
- 文字起こし（ASR）と話者分離（Speaker Diarization）の統合
- 複数の推論バックエンド（CPU / NVIDIA / Intel、将来 AMD）の切り替え
- CLI インターフェース
- WindowsデスクトップGUI（ローカルWebView、Phase 5a）

**対象としないもの（現時点）**

- リアルタイム（マイク入力）処理
- 文字起こし結果の編集エディタ
- クラウド API への送信（完全ローカル実行を前提とする）

### 1.3 想定音源

| 種別 | 話者数 | 想定長 |
|---|---|---|
| 会議・打ち合わせ | 2〜6人 | 30分〜3時間 |
| インタビュー・対談 | 2人 | 30分〜2時間 |
| 講演・セミナー | 1人＋質疑 | 1〜3時間 |

3時間を超える音声も処理可能であること。

### 1.4 プロジェクト情報

| 項目 | 値 |
|---|---|
| 名称 | utteran |
| 言語 | Python 3.11 / 3.12 |
| パッケージ管理 | uv（`pyproject.toml`） |
| ライセンス | MIT |
| 主対象 OS | Windows 10/11 |
| 副対象 OS | Linux（動作可能な実装とする） |

---

## 2. アーキテクチャ

### 2.1 設計原則

1. **コアはライブラリとして実装し、CLI はその薄いラッパーとする。**
   GUIは推論コアをimportせず、profileごとのCLIをsubprocessとして利用する。
2. **すべての長時間処理は進捗コールバックとキャンセルに対応する。**
   GUI のプログレスバーと中断ボタンに直結できるようにするため。
3. **設定は単一の Config オブジェクトに集約する。**
   GUI の設定画面がこれをバインドするだけで済むようにするため。
4. **バックエンドはプラグイン的なインターフェースで抽象化する。**
   モデル固有の構造を上位層に漏らさない。
5. **重い処理の結果は必ず中間ファイルに保存する。**
   再実行時に不要な再計算を発生させない。

### 2.2 ディレクトリ構成

```
utteran/
├── README.md                  # 必須ドキュメント
├── 変更履歴.md                # 必須ドキュメント
├── 要件定義.md                # 必須ドキュメント（本書）
├── AISTATE.md                 # 必須ドキュメント
├── LICENSE                    # MIT
├── THIRD_PARTY_NOTICES.md     # 依存ライブラリ・モデルのライセンス表記
├── pyproject.toml
├── setup.ps1                  # Windows 向けセットアップスクリプト
├── start.ps1                  # Windows 向け対話フロント
├── gui.ps1                    # Windows GUI起動スクリプト
├── input/
│   └── .gitkeep               # 利用者入力は除外し、フォルダだけ維持
├── output/
│   └── .gitkeep               # 生成出力は除外し、フォルダだけ維持
├── .env.example
├── src/
│   ├── utteran/
│       ├── __init__.py
│       ├── cli.py             # typer によるCLI定義
│       ├── config.py          # pydantic-settings による設定
│       ├── types.py           # 共通データモデル
│       ├── pipeline.py        # 全体オーケストレーション
│       ├── jobs.py            # ジョブ管理・レジューム
│       ├── audio.py           # ffmpeg 経由の音声抽出・正規化
│       ├── devices.py         # デバイス・バックエンド検出
│       ├── align.py           # ASR結果と話者区間の突き合わせ
│       ├── logging.py         # ログ設定（トークンマスク含む）
│       ├── errors.py          # 例外定義
│       ├── asr/
│       │   ├── base.py        # ASRBackend 抽象基底
│       │   ├── faster_whisper.py
│       │   ├── openvino.py    # Phase 3
│       │   └── registry.py
│       ├── diarization/
│       │   ├── base.py        # DiarizationBackend 抽象基底
│       │   ├── pyannote.py
│       │   ├── sherpa_onnx.py # Phase 3
│       │   └── registry.py
│       ├── exporters/
│       │   ├── base.py
│       │   ├── srt.py
│       │   ├── vtt.py
│       │   ├── json_exporter.py
│       │   ├── text.py
│       │   └── markdown.py
│       └── models/
│           ├── catalog.py     # モデル定義（ID、形式、サイズ、URL）
│           └── manager.py     # ダウンロード・検証・削除
│   └── utteran_gui/           # core非依存のGUI process／WebView層
│       ├── app.py             # loopback serverとpywebviewの起動
│       ├── api.py             # session認証付きlocal API
│       ├── cli.py             # profile CLI subprocess adapter
│       ├── jobs.py            # 単一job、JSONL進捗、process tree中断
│       └── web/               # HTML/CSS/JS、日本語／English
├── tests/
└── docs/
```

### 2.3 処理パイプライン

```
入力ファイル（音声 / 動画）
  │
  ├─[1] 音声抽出・正規化 ─────→ audio.wav (16kHz / mono / PCM 16bit)
  │                              ffmpeg 使用
  ├─[2] ASR ────────────────→ asr.json
  │                              単語レベルタイムスタンプ付き
  ├─[3] 話者分離 ───────────→ diarization.json
  │                              話者区間 + exclusive 区間
  ├─[4] 突き合わせ・話者割当 → merged.json
  │
  └─[5] 出力生成 ───────────→ *.srt / *.vtt / *.json / *.txt / *.md
```

各ステージは独立して完了状態を持ち、レジューム時は未完了のステージから再開する。

---

## 3. データモデル

`src/utteran/types.py` に定義する。バックエンド固有の構造を上位層に漏らさないこと。

```python
@dataclass(frozen=True)
class Word:
    start: float          # 秒
    end: float            # 秒
    text: str
    probability: float | None = None

@dataclass
class Segment:
    start: float
    end: float
    text: str
    words: list[Word] = field(default_factory=list)
    speaker: str | None = None      # 話者割当後に設定される

@dataclass
class TranscriptionResult:
    segments: list[Segment]
    language: str
    duration: float
    backend: str                    # 例: "faster-whisper"
    model_id: str
    device: str

@dataclass(frozen=True)
class SpeakerTurn:
    start: float
    end: float
    speaker: str                    # 例: "SPEAKER_00"

@dataclass
class DiarizationResult:
    turns: list[SpeakerTurn]
    exclusive_turns: list[SpeakerTurn] | None   # 対応バックエンドのみ
    num_speakers: int
    backend: str
    model_id: str
    device: str
```

**重要**: `TranscriptionResult` は Whisper 固有の構造を含めない。将来 Whisper 以外の
ASR モデル（Voxtral、Qwen3-ASR 等）を追加できるようにするため。

---

## 4. バックエンド

### 4.1 対応方針

| 用途 | 実装 | 対象ハードウェア | 導入フェーズ |
|---|---|---|---|
| ASR | faster-whisper (CTranslate2) | CPU / NVIDIA (CUDA) | Phase 1 |
| ASR | OpenVINO GenAI | Intel CPU / GPU / NPU | Phase 6b |
| ASR | whisper.cpp | CPU / OpenVINO / Vulkan / OpenVINO+Vulkan | Phase 3 |
| 話者分離 | pyannote.audio 4.x | CPU / NVIDIA (CUDA) / Intel XPU | Phase 1、XPUはPhase 3 |
| 話者分離 | sherpa-onnx | CPU / CUDA / OpenVINO / DirectML | 将来（未実装） |

### 4.2 ASRBackend インターフェース

```python
class ASRBackend(ABC):
    name: ClassVar[str]

    @classmethod
    @abstractmethod
    def is_available(cls) -> bool:
        """必要な依存パッケージが導入済みか。import 失敗を握りつぶして False を返す。"""

    @classmethod
    @abstractmethod
    def available_devices(cls) -> list[DeviceInfo]:
        """このバックエンドで利用可能なデバイスの一覧。"""

    @abstractmethod
    def load(self, model_id: str, device: str, compute_type: str) -> None: ...

    @abstractmethod
    def transcribe(
        self,
        audio_path: Path,
        options: ASROptions,
        progress: ProgressCallback | None = None,
        cancel: CancelToken | None = None,
    ) -> TranscriptionResult: ...

    @abstractmethod
    def unload(self) -> None: ...
```

`ASROptions` には最低限、`language`、`initial_prompt`、`vad_filter`、`beam_size`、
`condition_on_previous_text`、`word_timestamps` を含める。

### 4.2.1 OpenVINO GenAIの制約（Phase 6b）

- backend名は`openvino-genai`、deviceは`cpu | gpu | npu`。モデルは明示取得した
  `OpenVINO/whisper-large-v3-turbo-int8-ov`をローカルパスから読み、暗黙downloadしない。
- `auto`の選択順序には加えず、明示指定とbenchmarkだけで利用する。明示指定の初期化失敗時に
  別backend/deviceへfallbackしない。
- 話者分離が有効な場合、言語`auto`および本家Whisperが非スペース言語として扱う
  `ja, zh, th, lo, my, yue`との組み合わせを設定エラーにする。単語時刻がスペース分割へ融合し、
  話者割当精度を保証できないためで、代替としてwhisper.cppを案内する。話者分離なしなら全言語、
  明示したその他の言語なら話者分離ありでも利用できる。
- 30文字以上のwordおよびsegment全体が1 wordへ融合した状態を退化として検出する。wordは捨てず、
  件数・平均文字数・破棄0件を本文なしで記録し、話者分離ありなら警告する。
- OpenVINO compiled model cacheは`platformdirs.user_cache_dir("utteran")`配下の
  `openvino-genai-compiled`へ置く。`models genai-cache`でパスと使用量を確認できる。
- 構成は汎用の`recommended`属性と理由を持つ。GUIは非推奨を既定非表示にし、利用者が
  「非推奨の構成を表示する」を有効にした場合だけNPUを理由付きで表示する。CLIとbenchmarkでは
  NPUを常に明示指定できる。動作しないdeviceは非推奨ではなく非表示とする。

### 4.3 DiarizationBackend インターフェース

```python
class DiarizationBackend(ABC):
    name: ClassVar[str]

    @classmethod
    @abstractmethod
    def is_available(cls) -> bool: ...

    @classmethod
    @abstractmethod
    def available_devices(cls) -> list[DeviceInfo]: ...

    @abstractmethod
    def load(self, model_id: str, device: str) -> None: ...

    @abstractmethod
    def diarize(
        self,
        audio_path: Path,
        options: DiarizationOptions,
        progress: ProgressCallback | None = None,
        cancel: CancelToken | None = None,
    ) -> DiarizationResult: ...

    @abstractmethod
    def unload(self) -> None: ...
```

`DiarizationOptions` には `num_speakers`、`min_speakers`、`max_speakers` を含める。

### 4.4 pyannote バックエンドの仕様

- 既定モデル: `pyannote/speaker-diarization-community-1`
- Hugging Face のアクセストークンが必要。モデル利用条件への同意も必要。
- pyannote.audio 4.x の出力から `speaker_diarization` と `exclusive_speaker_diarization`
  の両方を取得し、後者を `DiarizationResult.exclusive_turns` に格納する。
- トークン未設定時は、取得手順を案内する明確なエラーメッセージを出す。
  スタックトレースをそのまま見せない。
- ローカルパス指定によるオフライン読み込みに対応する。

### 4.5 デバイス検出

`utteran devices` で、利用可能なバックエンドとデバイスを一覧表示する。

検出項目:
- CPU（コア数、命令セット）
- NVIDIA GPU（CUDA 有無、VRAM、cuDNN/cuBLAS の DLL 解決可否）
- Intel GPU / NPU（OpenVINO の利用可能デバイス）
- 各バックエンドの `is_available()` 結果

自動選択のロジック:
1. ASR: CUDA が使えれば faster-whisper + CUDA、Vulkan が使えれば whisper.cpp + vulkan、
   Intel GPU（OpenVINO）があれば whisper.cpp + openvino 系、いずれもなければ
   faster-whisper + CPU（`src/utteran/devices.py::_auto_selection`）
2. 話者分離: CUDA が使えれば pyannote + CUDA、Intel XPU（torch.xpu）が使えれば
   pyannote + XPU、なければ pyannote + CPU
3. Intel 環境では「ASR は GPU/NPU、話者分離は CPU」という組み合わせになりうる点を
   警告として明示する（torch XPU が判定できない、または利用不可の場合）

#### 4.5.1 プローブの分離とタイムアウト（Phase 5k）

CTranslate2・PyTorch CUDA/XPU・OpenVINO・ONNX Runtime・Vulkan のネイティブ呼び出しは、
NVIDIA GPU を持たない環境で無限にハングしうる（`ctranslate2.get_cuda_device_count()` が
`nvcuda.dll` の解決でネイティブ層ブロックする、など。Ctrl+C でも復帰しない）。
同一プロセス内で呼ぶ限りこのハングから回復する手段がないため、各プローブは
`utteran._device_probe` を `python -m` で起動した**別プロセス**として実行し
（`utteran.devices.run_isolated_probe`）、応答がなければタイムアウトで
プロセスツリーごと強制終了する（Windows は `taskkill /T /F`、POSIX は
`killpg` + `SIGTERM`→`SIGKILL`）。

- 既定タイムアウトは**プローブ1件あたり20秒**（`config.general.device_probe_timeout_seconds`、
  `utteran devices --probe-timeout` で上書き可）。20秒とした根拠は、実機
  （Core i7-1165G7 / Iris Xe / NVIDIA なし）でハングするプローブが実際に
  20秒程度で確実にkillでき、かつ正常に完了するプローブ（数秒未満）を
  待たせすぎない値として採用した。
- 1つのプローブがタイムアウトしても他のプローブは続行する。
- 表示状態は、`started`を「実行中」、`completed`と`cached`を「成功」、`error`を「失敗」、
  `timeout`を「タイムアウト」、それ以外を「不明」とする。未知値を失敗へ倒さずlogへ記録する。
  正常に「未導入」と判定したprobeは利用不可だが、判定処理自体は成功である。
- タイムアウトは`ProbeState = "timeout"`として明示的に扱い、**「利用可能」とは
  絶対に誤認しない**（判定不能として扱う）。
- 対象環境（NVIDIA GPU なし）では CTranslate2 CPU/CUDA・PyTorch CUDA/XPU の
  4プローブが典型的にすべてタイムアウトし、`utteran devices --json`
  全体で**約90〜100秒**かかることを実機で確認済み（2026-08-26、Intel
  profile、キャッシュなし）。原因はCTranslate2/PyTorchが呼び出し関数に
  関わらずネイティブ層でCUDAドライバの列挙を試みるためと推測されるが、
  根本原因の完全な特定はしていない（回避策がなくてもタイムアウトによる
  復帰は機能するため、Phase 5k/5lの完了条件は満たす）。

#### 4.5.2 結果のキャッシュ

全probeが`completed`になった成功結果だけを
`platformdirs.user_cache_dir("utteran")/device-probes-v1.json`へキャッシュする。
timeout/errorを含む結果は保存せず、0.1.20以前に保存された同種cacheも再利用しない。
キーはハードウェア・ドライバ・インタプリタ・対象パッケージ
バージョンから作る SHA-256 フィンガープリント（`device_probe_fingerprint()`）で、
これが変わると自動的に無効化される。個人を特定する情報（デバイスのシリアル等）は
含めない。`utteran devices --refresh`はcacheを破棄して再取得する。GUIの「再検出」も同じ
`--refresh`経路を使う。キャッシュ命中時は
実機で約5秒まで短縮されることを確認済み（上記4.5.1と同一機、同一条件）。

#### 4.5.3 GUI からの呼び出し（Phase 5l）

GUI（`src/utteran_gui`）は`utteran`パッケージを一切importせず（`packaging/gui.spec`が
ビルド時にも禁止する）、プロファイル venv の`utteran.exe devices --json`を
サブプロセスとして起動し結果を読む（`utteran_gui.environment.EnvironmentService`）。
この外側の呼び出しにも別途タイムアウトが必要で、4.5.1のプローブ合計の最悪値
（7プローブ×20秒＋kill/再試行のオーバーヘッド）を下回らないよう
`_DEVICES_PROBE_TIMEOUT_SECONDS = 200.0`秒としている
（`src/utteran_gui/environment.py`）。この外側の呼び出しがタイムアウトまたは
失敗した場合も`EnvironmentService.snapshot()`は例外を外へ伝播させず、
`errors`配列へ記録して安全側の応答を返す。

初回wizardの既存profile runtime検出も同じcommandを呼ぶため、外側上限を200秒へ統一する。
Core i7機の未cache実測最大105.7秒と、7件×20秒＋process tree終了overheadを根拠とする。
OpenVINO GenAI NPUの306.103秒はASR model loadの値でdevice列挙ではないため、この上限へ混ぜない。

GUI起動シーケンス（`app.js`の`boot()`）は、デバイス検出（初回・未キャッシュ時は
数十秒〜100秒超かかりうる）を**待たずに**ジョブキュー確認・ウィザード再開判定へ
進む。デバイス検出が完了・失敗するまでの間は`#environment-alert`で状態を示すのみで、
他の操作をブロックしない。この非同期化以前は、デバイス検出がGUI側の
固定60秒タイムアウトを超えて例外が起動処理全体を中断させ、ウィンドウが
「detecting...」表示のまま復帰しないという不具合があった（Phase 5l、
AISTATE.md参照）。

#### 4.5.4 CTranslate2インポート時のPyTorch巻き込みハング回避（Phase 5m）

CTranslate2 4.8.1の`ctranslate2.specs.model_spec`は、実際には使われない
PyTorchチェックポイント変換ヘルパーのためだけに、モジュールレベルで無条件に
`try: import torch except ImportError: ...`を実行する。CTranslate2自体の
CPU/CUDA推論（モデル読み込み・`transcribe()`）はtorchを一切必要としないが、
`import ctranslate2`（および`from faster_whisper import WhisperModel`）を
呼ぶだけでこの巻き込みimportが発生する。

Intel profileが導入するPyTorch（XPUビルド、`torch==2.11.0+xpu`）は
`torch/lib/torch_xpu.dll`という800 MiB超のネイティブDLLを含み、
`torch/__init__.py::_load_dll_libraries()`が`LoadLibraryW`でこれを
ロードする際、特定のIntel iGPU/ドライバ構成（Core i7-1165G7 / Iris Xe /
NVIDIAなし機で実機確認）ではこの初期化だけで実CPU時間を大量に消費し、
完了しないことがある（実機実測: `import torch`単体が1000 CPU秒超を消費しても
完了せず。`faulthandler.dump_traceback_later()`で採取したスタックは一貫して
`torch/__init__.py::_load_dll_libraries`を指す）。**Phase 5kが特定した
`ctranslate2.get_cuda_device_count()`のハングとは別の、より根本的な原因で
ある。CUDA/XPUデバイスを明示的に問い合わせる関数呼び出しではなく、
`import ctranslate2`そのものが引き金になる。** Phase 5kの隔離プローブ
（`ctranslate2_cpu`・`ctranslate2_cuda_count`）がCPU専用の問い合わせでも
毎回20秒タイムアウトしていたのは、この巻き込みimportが原因だったと判明した。

`utteran.devices.suppress_torch_import()`（contextmanager）は、`sys.modules`に
`torch`が未登録の場合だけ軽量な代替モジュールを一時的に登録し、`import ctranslate2`
完了後に直ちに取り除く。これにより`model_spec`の`try: import torch`は代替
モジュールを即座に取得して完了し（推論経路が使わない変換ヘルパー内でのみ
参照されるため実害がない）、後続で話者分離（pyannote）が本物のtorchを必要とする
場合は`sys.modules`から代替モジュールが既に除去されているため、通常どおり
実importが行われる。同一プロセス内で2回目以降に`import ctranslate2`する場合は
Pythonの通常のモジュールキャッシュにより既に完了済みのため、この処理自体
発生しない。

適用箇所は、`asr/faster_whisper.py`の`FasterWhisperBackend.available_devices()`・
`load()`（実推論経路、無制限にハングしていた箇所）と、`_device_probe.py`の
`ctranslate2_cpu`・`ctranslate2_cuda_count`・`ctranslate2_cuda`プローブ
（Phase 5kの隔離プロセス内、副次的に高速化）。`torch_cuda`・`torch_xpu`プローブは
実際にPyTorch CUDA/XPUを検証する必要があるため対象外とし、Phase 5kの20秒
タイムアウトに委ねる（この2プローブは本機では引き続きタイムアウトする）。

実機検証（Core i7-1165G7 / Iris Xe / NVIDIAなし、2026-08-26）: 修正前は
`utteran transcribe`（faster-whisper・CPU・small・無音3秒）が300秒
タイムアウトまでASRステージから進まなかった。修正後は同一条件で
ASRステージ合計6.48秒（import 0.4秒 + モデル読み込み4.36秒 + 推論0.69秒、
VADが無音を正しく除去しsegment数0）で完了した。`utteran devices --json --refresh`
の所要時間も約90〜100秒（Phase 5k/5l実測）から約49秒へ改善した
（`ctranslate2_cpu`・`ctranslate2_cuda_count`プローブが20秒タイムアウトから
0.4〜0.5秒へ短縮。`torch_cuda`・`torch_xpu`は対象外のため引き続き20秒ずつ
タイムアウトし、これが残り約40秒の内訳）。

---

## 5. 設定

### 5.1 優先順位

```
CLI 引数  >  環境変数  >  .env  >  config.toml  >  既定値
```

Hugging Face トークンのみ、さらに OS キーリングを参照先に加える（優先度は .env の次）。

### 5.2 config.toml

配置場所は `platformdirs.user_config_dir("utteran")`。`utteran config init` で雛形を生成する。

```toml
[general]
output_dir = "./output"
job_dir = ""              # 空文字ならユーザーキャッシュ配下
log_level = "info"
log_dir = ""             # 空文字ならインストール先 logs/（書込不可時はuser logへ退避）
raw_subprocess_logs = false
log_retention_days = 30
log_max_mib = 100
raw_log_max_mib = 1024
venv_dir = ""             # 空文字ならリポジトリ直下 .venvs
native_dir = ""           # 空文字なら ~/.utteran/native
default_profile = ""      # cpu | cuda | intel | vulkan、空文字なら自動解決
device_probe_timeout_seconds = 20.0 # Phase 5kの隔離probe 1件あたりの上限

[asr]
backend = "auto"          # auto | faster-whisper | openvino
model = "large-v3-turbo"
device = "auto"
compute_type = "auto"     # float16 | int8_float16 | int8 など
language = "ja"
vad_filter = true
condition_on_previous_text = false
beam_size = 5
initial_prompt = ""
word_timestamps = "auto"  # auto | always | never（whisper.cpp経路）

[asr.whisper_cpp]
variant = "auto"          # auto | cpu | openvino | vulkan | openvino_vulkan
dtw = "auto"              # auto またはwhisper.cppのDTW preset
threads = 0                # 0なら自動
max_word_duration_seconds = 3.0 # 異常word時刻をsegment単位fallbackへ切り替える閾値

[diarization]
enabled = true
backend = "pyannote"      # pyannote | sherpa-onnx
model = "pyannote/speaker-diarization-community-1"
device = "auto"
num_speakers = 0          # 0 なら自動推定
min_speakers = 0
max_speakers = 0
memory_guard = "auto"      # auto | warn | off
memory_safety_margin = 0.0 # 0ならdevice種別の既定値

[output]
formats = ["srt", "json", "md"]
srt_bom = false           # Windows の字幕ソフト向けに UTF-8 BOM を付与
newline = "lf"            # lf | crlf
show_speaker = true       # 字幕・テキストへ話者名を表示
speaker_labels = {}       # 例: { SPEAKER_00 = "田中", SPEAKER_01 = "佐藤" }

[ffmpeg]
path = ""                 # 空文字なら PATH から探索し、なければ同梱先を参照

[alignment]
max_nearest_distance = 2.0
min_segment_duration = 0.3
min_segment_words = 2
speaker_switch_penalty = 0.75
silence_switch_threshold = 0.3
min_clear_turn_duration = 0.5
max_same_speaker_bridge_gap = 0.3
unknown_emission_score = 0.35
min_unknown_duration = 1.0
min_unknown_characters = 2
max_unsupported_fragment_duration = 0.5
max_unsupported_fragment_characters = 3
min_fragment_speaker_overlap = 0.05
merge_gap = 0.5
renumber_speakers = true
boundary_snap_enabled = true
boundary_snap_unit = "A"                 # SudachiPyの分割単位 A | B | C
boundary_snap_max_characters = 4
boundary_snap_max_gap = 0.1
fallback_characters_per_second = 4.0
fallback_duration_padding = 1.0
fallback_min_duration = 1.0
```

### 5.3 トークンの取り扱い

- `config.toml` にトークンを書かせない。書かれていても読み取らず、警告を出す。
- ログ出力時、トークンらしき文字列は常にマスクする（`hf_****`）。
- 例外メッセージやスタックトレースにトークンが混入しないようにする。
- 将来の GUI が利用できるよう、`TokenProvider` として抽象化する
  （環境変数 / .env / キーリングの各実装を持つ）。

---

## 6. ジョブ管理とレジューム

### 6.1 ジョブディレクトリ

```
<job_dir>/<job_id>/
├── manifest.json       # 入力情報、設定ハッシュ、各ステージの状態
├── audio.wav           # 抽出済み音声（16kHz mono）
├── asr.json            # ASR 結果
├── diarization.json    # 話者分離結果
├── merged.json         # 統合結果
└── utteran.log         # このジョブのログ
```

### 6.2 job_id の決定

```
job_id = sha256(入力ファイルのハッシュ)[:16]
```

入力ファイルのハッシュは、全体を読むと遅いため
「ファイルサイズ + 更新時刻 + 先頭 1MB + 末尾 1MB」から算出する。

設定は `job_id` に含めない。再実行の要否は、ステージごとに保存した
`config_hash` と現在の設定から算出した値を比較して判定する。同一入力に対するジョブは
1つだけ存在し、設定を変えて再実行した場合は影響のあるステージ以降のみを再計算する。

### 6.3 ステージ依存関係

設定変更時に再実行が必要なステージを明示する。

| ステージ | config_hash の算出対象 |
|---|---|
| audio | 入力ファイルハッシュ、サンプリングレート、チャンネル数、サンプル形式 |
| asr | audio の config_hash、`[asr]` セクション全体 |
| diarization | audio の config_hash、`[diarization]` セクション全体 |
| merge | asr / diarization の config_hash、`[alignment]` セクション全体 |
| export | merge の config_hash、`[output]` セクション全体、実効出力先ディレクトリ |

算出対象はキー順にソートした正規形 JSON とし、浮動小数は固定桁の文字列へ変換してから
SHA-256 を算出する。Python の `dict` の順序やロケールに依存してはならない。
ASR backendを明示した場合、モデルカタログの`backend:model-id`形式は同じbackendの
`model-id`へ正規化してから保存・hash計算する。同一モデルを表す2つの記法だけでASRを
再実行してはならない。実体が異なるモデルIDへ変更した場合はASR、merge、exportを再実行する。

出力形式や話者名だけを変更した再実行は、export のみが走ること。
同一入力・同一設定・同一出力先でexport成果物も存在する場合は全stageを再利用し、既存出力を
上書きせず、連番fileも増やさない。manifestが参照するexport成果物が欠落している場合はexportだけを
再実行する。出力先を変更した場合もexportだけを再実行する。

### 6.4 manifest.json

```json
{
  "version": 1,
  "job_id": "a1b2c3d4e5f6a7b8",
  "input": { "path": "...", "size": 123456, "mtime": 1700000000, "hash": "..." },
  "created_at": "2026-01-01T00:00:00+09:00",
  "updated_at": "2026-01-01T00:00:00+09:00",
  "stages": {
    "audio":       { "status": "done", "config_hash": "...", "finished_at": "..." },
    "asr":         { "status": "done", "config_hash": "...", "finished_at": "..." },
    "diarization": { "status": "running", "config_hash": "...", "finished_at": null },
    "merge":       { "status": "pending", "config_hash": null, "finished_at": null },
    "export":      { "status": "pending", "config_hash": null, "finished_at": null }
  }
}
```

`status` は `pending | running | done | failed` の4値。
異常終了で `running` のまま残った場合、次回実行時は `pending` として扱う。

### 6.5 ジョブディレクトリのロック

同一ジョブに対する多重実行を防ぐため、ジョブディレクトリにロックファイルを置く。
ロックファイルには PID と開始時刻を記録する。既存ロックがある場合は当該プロセスの
生存を確認し、存在しなければ古いロックとして除去して取得する。プロセスが存在する場合は
一般エラー（終了コード 1）とし、`--force-unlock` で強制解除できるようにする。

---

## 7. 突き合わせ（align.py）

ASR の単語タイムスタンプと話者区間を突き合わせ、各セグメントに話者を割り当てる。

### 7.1 アルゴリズム

- 同一ASR segment内で同じ`(start, end)`を共有する連続token群は、単一の観測時刻しか持たないため
  話者境界で分割しない。この保護は日本語の形態素・文節境界スナップより優先する。
- 日本語境界スナップはword gapが厳密に0.10秒未満の場合だけ行う。Sudachiの形態素境界に加え、
  副詞・連体詞の後を文節候補として優先する。0.10秒以上の境界は変更しない。
- 長いsegment末尾の短い別話者応答は、長さ0.2〜1.5秒、先行発話2秒以上、別話者turnとの
  重なりが現話者より0.15秒超強い場合だけ再分離する。
- A/B/Aの短断片はBが2秒以下、両gapが0.01秒未満で、Aの重なりがBより0.15秒以上強く、
  B自身の重なりが区間長の25%未満の場合だけAへ再判定する。音響支持のある短い応答を優先し、
  この条件に合わない断片は機械的に吸収しない。

1. `exclusive_turns` が存在すればそちらを優先して使用する。
   存在しない場合は `turns` を使い、重なり区間は開始が早い話者を優先する。
2. 全ASR単語を時系列として扱い、話者区間との重なり秒数をemission scoreへ変換する。
   重なりがない単語は`UNKNOWN`を優先する。ただし単語の両側に同じ話者があり、両側gapが
   `max_same_speaker_bridge_gap`以下の場合だけ、その話者による短い橋渡し候補を与える。
3. 話者切替に`speaker_switch_penalty`を課し、Viterbi動的計画法で系列全体のscoreが最大になる
   話者列を求める。単語間無音が`silence_switch_threshold`以上ならpenaltyを弱める。また、切替
   前後の話者区間がともに`min_clear_turn_duration`以上で、両単語の重なり差が明確な場合も弱める。
   `UNKNOWN`も正式な状態として通常話者と同じ切替penaltyを受ける。連続発話中の短い話者区間、
   `A→UNKNOWN→A`、重なりscoreが拮抗する場合はpenaltyを弱めない。
4. 求めた話者列の切り替わり位置でセグメントを分割する。日本語のCJK単位は文字数を品質根拠に
   せず、重なり秒数、gap、話者区間長、系列全体の切替回数で判断する。
   その後、実効ASR言語が`ja`で単語間gapが`boundary_snap_max_gap`以下の場合、連結したword本文の
   **文字位置**をSudachiPy／sudachidict_coreで解析する。分割単位A（最小）を既定とし、助詞・
   助動詞・接尾辞・非自立動詞の直前、および接頭辞直後をsegment開始候補から除く。現在位置から
   `boundary_snap_max_characters`以内の最近傍候補へ移し、同距離は早い側を選ぶ。移動後wordの
   start/endからsegment時刻も再構成する。`auto`はASRが確定した結果言語で判定し、英語その他は
   解析しない。Sudachiが未導入・初期化不能なら警告し、補正なしで続行する。
5. `UNKNOWN`が`min_unknown_duration`未満または`min_unknown_characters`未満なら、前後の長い既知
   話者側へ吸収する。また、既知話者labelでも`max_unsupported_fragment_duration`未満かつ
   `max_unsupported_fragment_characters`以下で、当該話者turnとの重なりが
   `min_fragment_speaker_overlap`未満のfragmentだけを長い隣接segmentへ吸収する。音響的な話者turnに
   支持された短い相槌は時間・文字数だけで吸収しない。
6. 従来の「0.3秒未満または2単語未満のA→B→Aを吸収」は廃止する。Viterbiと二重に平滑化され、
   無音で区切られた本物の短い相槌を消すためである。旧設定`max_nearest_distance`、
   `min_segment_duration`、`min_segment_words`はconfig互換のため受理するが、系列割当には使わない。
7. 連続する同一話者のセグメントは、間隔が閾値（既定 0.5 秒）以下なら結合する。ただし異常な
   word時刻を破棄して単語なしfallbackとなったsegmentはASR offsetが唯一の信頼できる境界なので、
   その前後では結合しない。
   結合回数に固定上限は設けない。同一話者判定が正しい限り音声長による閾値変更は不要であり、
   実測でも3/10/30/60/116分で最長区間比が増加しなかったためである。
8. 単語時刻なしsegmentは本文と推定話者を保持し、`speaker_confidence=low`とする。pyannoteの
   検出発話turnが元segmentと重なる場合は、その最初のstartから最後のendまでの包絡と交差して
   先頭・末尾の無音伸長を除く。重なるturnがない場合だけ、非空白文字数÷4文字/秒＋1秒、最小1秒を
   時刻上限にする。削減秒数、方式別件数、検出発話coverageを本文なしで記録する。

### 7.2 パラメータ

系列最適化の主要閾値は設定で変更可能にする。既定値は合成正解fixtureのpenalty sweepに基づく。
`speaker_switch_penalty=0.35`では語中境界2回・confusion 0.4秒が残り、0.50〜1.10では語中境界0、
confusion 0、短い相槌保持100%で安定したため、その中央寄りの0.75を採用した。merge stage hashには
alignment policy versionを含め、割当規則の変更時は既存merge/exportを自動再計算する。

`diarization_statistics`は通常/exclusiveそれぞれの区間数、話者交代数、話者数、最長区間、
話者別合計秒を記録する。`alignment_statistics`はASR segment/単語数、単語話者交代数、
分割・吸収・結合の各件数、結合gapのmin/median/p95/max、最終最長区間、話者別合計秒を記録する。
加えて境界補正の候補・移動件数、移動文字数・時刻距離、A/B比較、fallbackの元／保持／削減秒数と
検出発話coverageを記録する。
いずれも本文を含めない。既存中間JSONには`tools/diarization_stats.py`で同じ統計を適用できる。

### 7.3 話者ラベル

- 内部表現は `SPEAKER_00` 形式を維持する。
- 出力時に `[output].speaker_labels` によるリネームを適用する。
- 話者の登場順に `SPEAKER_00` から振り直すオプションを持つ
  （バックエンドが返す番号は登場順とは限らないため）。

### 7.4 正解付き品質評価

合成音声または利用者が管理する正解timelineと、話者付き出力を`utteran eval`で比較できること。
評価時は話者label名の一致を要求せず、正解／推定話者間の重なり時間が最大になる1対1対応を求める。
出力する指標は、DER（miss、false alarm、speaker confusionの秒数を含む）、正解話者境界から
最寄り推定境界までの誤差、単語区間内の話者境界数、0.5秒未満の話者島数、1単語だけの話者島数、
UNKNOWNの時間割合、正解で短い相槌と指定した単語の話者が保持された割合とする。指標出力には
文字起こし本文を含めない。

リポジトリには外部録音を使わない決定的生成の3話者fixture、正解timeline、報告症状を模した
修正前仮説、期待仮説を含める。モデル不要testは、話者labelを入れ替えてもDER 0となること、
語中境界と無音への誤割当を検出すること、短い相槌の保持を判定できることを保証する。

---

## 8. 入出力

### 8.1 入力

- 音声: wav, mp3, m4a, flac, ogg, aac, wma
- 動画: mp4, mkv, mov, avi, webm, ts
- ffmpeg でデコードできるものはすべて受け付ける方針とし、上記は明示的にテストする対象。
- 複数チャンネルはモノラルにダウンミックスする。
- サンプリングレートは 16kHz に変換する。

### 8.2 出力

| 形式 | 拡張子 | 内容 |
|---|---|---|
| SRT | .srt | 字幕。話者名を行頭に付与するオプションあり |
| WebVTT | .vtt | 字幕 |
| JSON | .json | 単語レベルを含む全情報。再利用・二次加工用 |
| テキスト | .txt | 話者名付きの本文 |
| Markdown | .md | 話者名付き本文＋メタ情報（処理条件、日時、モデル） |

- 文字コードは UTF-8。SRT のみ BOM 付与オプションを持つ。
- 改行コードは LF（SRT/VTT は CRLF も選べるようにする）。
- 出力ファイル名は既定で `<入力ファイル名>.<拡張子>`。exportを実行するときに同名fileが既にあり、
  そのfileを今回の再利用成果物として扱えない場合は連番を付与する。全stage再利用時は新規fileを
  作らず、既存成果物のpathを返す。

### 8.3 JSON 出力のスキーマ

バージョンフィールドを持たせ、後方互換性を管理する。

```json
{
  "schema_version": 1,
  "input": { "path": "...", "duration": 3600.0 },
  "processing": {
    "asr": { "backend": "faster-whisper", "model": "large-v3-turbo", "device": "cuda" },
    "diarization": { "backend": "pyannote", "model": "...", "device": "cuda" },
    "created_at": "..."
  },
  "speakers": ["SPEAKER_00", "SPEAKER_01"],
  "segments": [
    {
      "start": 0.5, "end": 3.2, "speaker": "SPEAKER_00", "speaker_confidence": "high", "text": "...",
      "words": [ { "start": 0.5, "end": 0.8, "text": "...", "probability": 0.98 } ]
    }
  ]
}
```

`speaker_confidence`は`high`または`low`で、単語時刻なしsegmentを`low`とする。これは任意fieldの
後方互換な追加であり`schema_version`は1を維持する。SRT/VTT/TXT/Markdownは話者名を維持する。

---

## 9. CLI 仕様

```
utteran transcribe <入力パス> [オプション]
    --format srt,json,md          出力形式（カンマ区切り）
    --output-dir <パス>
    --asr-backend <名前>
    --asr-model <ID>
    --diarization-backend <名前>
    --diarization-model <IDまたはパス>
    --asr-device <名前>           ASR専用のデバイス／native構成
    --diarization-device <名前>   話者分離専用のデバイス
    --device <名前>               両方へ適用する従来互換指定
    --language <コード|auto>     `auto` は言語自動判定
    --num-speakers <N>
    --min-speakers <N>
    --max-speakers <N>
    --no-diarization
    --recursive                   ディレクトリを再帰的に処理
    --include <glob>              一括処理に含めるパターン（複数指定可）
    --exclude <glob>              一括処理から除外するパターン（複数指定可）
    --resume / --no-resume        既存ジョブから再開（既定で有効）
    --force                       すべてのステージを再実行
    --force-unlock                既存のジョブロックを強制解除
    --dry-run                     処理内容の表示のみ
    --yes                         モデル取得などの確認を省略
    --progress-json               UTF-8 JSONL進捗をstderrへ出力
    --config <パス>
    --verbose / --quiet

utteran devices [--json]          利用可能なバックエンドとデバイスを表示（JSON 出力対応）
utteran profiles list [--json]    profileの一覧と導入状況
utteran models list [--json]      モデルの一覧と導入状況
utteran models download [<ID>]    ID指定、または番号付き対話選択によるモデル取得
utteran models remove <ID>
utteran models verify             導入済みモデルの整合性検証
utteran models path               モデル保存先の表示
utteran jobs list [--json]        ジョブの一覧（GUI向けJSON対応）
utteran jobs show <job_id> [--json]
utteran jobs export <job_id>      merged.jsonから出力だけを再生成
    --format srt,json,md
    --output-dir <パス>
    --speaker-label SPEAKER_00=表示名  複数指定可
    [--json]
utteran jobs clean [--all|--failed|--older-than <日数>|--job-id <job_id>]
utteran config init               設定ファイルの雛形生成
utteran config show               現在の有効な設定（トークンはマスク）
utteran config path
utteran native status [--json]    native構成の実行可否
utteran eval <正解ディレクトリ>    DER / WER の算出（Phase 4）
```

終了コード:

| コード | 意味 |
|---|---|
| 0 | 正常終了 |
| 1 | 一般エラー |
| 2 | 設定エラー（トークン未設定、不正な設定値など） |
| 3 | 依存エラー（ffmpeg 未検出、バックエンド未導入など） |
| 4 | 入力エラー（ファイル未検出、デコード失敗など） |
| 5 | 一括処理で一部のファイルが失敗した（全滅ではない） |
| 130 | ユーザーによる中断 |

一括処理ですべての対象ファイルが失敗した場合は終了コード 1 とする。
バッチ中のデコード失敗は「失敗」として集計し、他ファイルの処理を継続する。完了済みジョブや
dry-run対象は「スキップ」とする。

### 9.1 Windows対話フロント

- リポジトリ直下に `start.ps1` を置き、PowerShellから番号メニューだけで主要操作へ到達できること。
- メインメニューから文字起こし、モデル管理、デバイス確認、ジョブ管理、設定管理、setup profile
  切替、入力／出力フォルダを開く操作を選べること。
- 文字起こしウィザードは、`input` 内の単一ファイル／フォルダ一括／任意パス、`output` または
  任意出力先、ASRモデル、話者分離、ASRと話者分離それぞれのデバイス、言語、話者数、出力形式、再帰・glob、resume／force、
  config、ログ詳細度を対話的に設定し、実行前に要約とコマンドを表示すること。
- `devices --json`と`models list --json`から現在のprofileで利用可能なbackend、device/native構成、
  導入済みモデルだけを動的表示する。空ならnative buildまたはモデル取得を案内する。
- 入力パスやモデルIDなどはコマンド文字列として評価せず、PowerShellの引数配列としてCLIへ渡す。
- `start.ps1` はPhase 3aのprofile別`.venvs/win-<profile>`を解決し、未構築時は`setup.ps1`を
  案内すること。
- ASRのnative構成（`openvino`／`vulkan`／`openvino_vulkan`）はpyannoteへ渡さない。
  `--asr-device`と`--diarization-device`を別々に生成し、後者はPyTorchで利用可能な
  `auto`／CPU／CUDA／XPUだけを`devices --json`から提示すること。
- リポジトリ直下に `input` と `output` を作り、それぞれ `.gitkeep` だけを追跡する。利用者が置く
  音声・動画と生成された文字起こしは拡張子を問わずGit対象外にする。

---

## 10. モデル管理

### 10.1 方針

- ユーザーが明示的に管理できること。暗黙のダウンロードで数GBを消費させない。
- 未取得のモデルが指定された場合、自動ダウンロードするか確認プロンプトを出す。
  `--yes` で省略可能。非対話環境では明確なエラーとする。
- `setup.ps1` はPython依存、ffmpeg、秘密設定補助、デバイス診断までを担当し、モデルの一覧・
  選択・取得は行わない。セットアップ完了後に専用の `utteran models` コマンドを案内する。
- `utteran models download` のIDを省略した対話実行では、表示名、用途、backend、概算サイズ、
  ライセンス、gated状態、技術IDを持つ番号付き一覧から1件以上を選択できること。
- 番号の代わりにカタログのモデルIDまたは `<backend>:<model-id>` も指定でき、日本語向けなど
  特殊用途のモデルを明示選択できること。非対話環境でIDを省略した場合は取得せず、一覧表示と
  明示IDの指定方法を案内する。
- 完全オフライン運用が可能なこと（事前取得のみで動作する）。
- モデル形式ごとの必須ファイルがすべて存在する場合だけ「導入済み」と判定すること。部分取得は
  「不完全」と表示し、`models verify` で欠落ファイルを列挙し、`models download` で既存ファイルを
  保持したまま取得を再開できること。
- pyannote community-1は `config.yaml` だけで正常と判定せず、embedding／segmentationの各
  `pytorch_model.bin` と、PLDAの `plda.npz`／`xvec_transform.npz` も必須とすること。
- Windowsのモデル保存先では、Hugging Faceが生成する一時ファイル名を含むパスが260文字以上でも
  取得できるよう、snapshot downloadへextended-length pathを渡すこと。利用者向け表示とモデル
  保存先の設定値は通常のパス表記を維持すること。
- CTranslate2形式モデルの取得完了時と`models verify`実行時、`config.json`の`alignment_heads`が
  実際の decoder 層数を超えて参照していないかを、隔離subprocessで単語タイムスタンプ計算を
  1回試行して検証すること。ネイティブクラッシュ（アクセス違反等、Pythonから捕捉不能な
  異常終了）を検知した場合のみ、安全な既定値（decoder layer 0 / head 0）へ自動修正すること。
  Python例外による検証不能時は誤って書き換えないこと。第三者配布の蒸留系モデル
  （decoder層数を削減したモデル）が、蒸留前の深いdecoder向け`alignment_heads`をそのまま
  配布している既知の不具合（`kotoba-whisper-v2.0`で確認）から利用者を保護するための対策。

### 10.2 モデルカタログ

`models/catalog.py` に、モデル ID・表示名・用途説明・バックエンド・形式・概算サイズ・取得元を
定義する。
同じ Whisper large-v3-turbo でも、バックエンドごとに必要な形式が異なる点に注意する。

| モデル ID | バックエンド | 形式 |
|---|---|---|
| large-v3-turbo | faster-whisper | CTranslate2 |
| large-v3-turbo | openvino | OpenVINO IR |
| large-v3 | faster-whisper | CTranslate2 |
| kotoba-whisper-v2.0 | faster-whisper | CTranslate2 |
| pyannote/speaker-diarization-community-1 | pyannote | pyannote pipeline |

pyannote community-1の現行スナップショットは約32 MiB（カタログ概算34 MiB）。約1 MiBの設定・
文書ファイルだけが存在する状態は部分取得として扱う。

保存先は `platformdirs.user_cache_dir("utteran") / "models"`。
環境変数 `UTTERAN_MODEL_DIR` で上書き可能。

---

## 11. ffmpeg

- 起動時に以下の順で探索する。
  1. 設定 `[ffmpeg].path`
  2. `PATH` 環境変数
  3. アプリケーションデータ配下の同梱先（`<user_data_dir>/bin/ffmpeg.exe`）
- いずれにも見つからない場合、`setup.ps1` での取得を案内するエラーを出す。
- リポジトリには ffmpeg のバイナリを含めない（ライセンス上の理由）。
- `setup.ps1` は公式配布物を取得し、上記の同梱先に配置する。

---

## 12. ログ・エラー処理

- 標準出力は人間向けの進捗表示（rich 等）とし、アプリログと実行事実の構造化ログを分離する。
- 既定rootはインストール先の`logs/`。書込probeに失敗した場合は
  `platformdirs.user_log_dir("utteran")`へ退避し、退避元・実効先をイベントへ残す。
  `log_dir`設定と`UTTERAN_GENERAL__LOG_DIR`で上書きでき、`utteran logs path`とGUIで確認できる。
- `app.log`は10 MiB×5世代のローテーション、`cli/<timestamp>-<command>.jsonl`はCLI実行ごとの
  明示イベントだけ、`diagnostics/<timestamp>-devices.json`は環境snapshotとする。ジョブごとの
  `utteran.log`は互換性・resume診断のため残す。
- 構造化イベントは解決backend／variant／実行ファイル／model、OpenVINO IR pathとload成否、
  variant fallback元・先・理由、ASR／話者分離device、stage所要時間、ASR実時間比、model取得、
  IR生成、error分類を記録する。ASR segment、word、文字起こし本文は記録しない。
- whisper.cpp stderrのOpenVINO診断判定は単一関数へ集約する。既知failureを優先し、load開始を検出し
  processが正常終了した場合だけ`openvino_ir_loaded.success=true`とする。判定文言を呼出側へ散在させない。
- 生stderr保存は`raw_subprocess_logs=false`を既定とする。trueを明示した場合だけ秘密値mask後に
  `raw/<job_id>/<process>.stderr.log`へ隔離する。stderrには文字起こし本文が残りうるため、起動時に
  警告しGUIにも常時表示する。この条件付きopt-in以外では本文をログへ残してはならない。
- 起動時に保持日数と容量の両方を適用し、削除件数・bytesを記録する。既定30日は障害の事後調査期間、
  通常100 MiBは小さな構造化イベントの十分な余裕、raw 1 GiBは本文混入リスクと肥大化を抑える上限
  とする。`log_retention_days`、`log_max_mib`、`raw_log_max_mib`で変更でき、`utteran logs clean`は
  全ログを明示削除する。
- 正常完了時は、今回実行した各ステージの実測所要時間と実行ステージ合計を
  `HH:MM:SS.mmm`形式で最終結果の後に表示する。resumeで再利用したステージは今回の集計へ
  含めない。フォルダ一括処理では成功ジョブのステージ時間を同名ステージごとに合算する。
- 想定される失敗は専用の例外型を定義し、ユーザー向けの対処法を含むメッセージを出す。
  トークン未設定、モデル未取得、ffmpeg 未検出、VRAM 不足、対応外の入力形式など。
- 想定外の例外のみスタックトレースを表示し、`--verbose` で詳細を出す。
- 一括処理中に個別ファイルが失敗しても全体を止めない。最後に失敗一覧を表示する。

---

## 13. 長時間音声への対応

### 13.1 実測と推定式

Phase 3d R-5では、Windows 11 / Core Ultra 7 255H / Arc 140Tの単一環境、同じ素材を連結した
25/50/100分音声、process tree working setの100 ms samplingで測定した。CPUは時間制約により
25/50分だけである。音声長を4倍にしてもXPU peakは約13%増に留まり、固定的な基礎量が支配する。
したがって分割で減らせるのは長さ係数だけで、基礎量が予算を超える環境は救えない。

推定peak bytesは`(base_GiB + coefficient_GiB_per_minute * audio_minutes) * 2^30`とする。
同梱値は測定JSONのbyte値を最小二乗回帰した値で、単一環境・少数点の観測にすぎない。

| stage / backend / device種別 | base (GiB) | GiB/分 | 根拠 |
|---|---:|---:|---|
| diarization / pyannote / XPU | 4.79694 | 0.00870553 | 25/50/100分の3点 |
| diarization / pyannote / CPU | 2.42100 | 0.00571777 | 25/50分の2点 |
| diarization / pyannote / CUDA | 7.31000 | 0 | GTX 1070 Ti、139分の1点だけ。傾向は不明 |
| ASR / whisper.cpp / Vulkan | 1.11794 | 0.01002353 | 25/50/100分の3点 |

ASRとdiarizationは同時にpeakを迎えないため別の式・実測点として扱う。Phase 4bの事前guardと
CPU退避はメモリ問題が顕在化したpyannote stageへ適用し、ASR点も別系列で蓄積・表示する。

### 13.2 予算

| device種別 | raw予算 | 既定安全率 |
|---|---|---:|
| CUDA専用VRAM | `torch.cuda.mem_get_info`の空きVRAM | 10% |
| XPU共有メモリ | GPU報告上限とsystem空きRAMの小さい方 | 30% |
| CPU system RAM | OS報告の空き物理RAM | 20% |

`memory_safety_margin`が0以外なら既定安全率を置換する。片方でも必要な取得APIが使えないXPU、
または対象値を取得できないCUDA/CPUはunknownとし、十分とはみなさない。検証専用の
`UTTERAN_DEBUG_MEMORY_BUDGET_GIB`はraw予算を上書きするが、利用者向け機能とはしない。

### 13.3 キャリブレーション

成功したASR/diarization実行のbackend、device種別、音声長（分）、process tree peak bytes、日時を
`platformdirs.user_data_dir("utteran")/memory-calibration.json`へ保存する。音声名、path、hash、
認識内容、話者情報は保存しないため、profileをまたいで共有できる。

同一(stage, backend, device種別)で3点以上かつ音声長spanが5分以上ある場合、pairwise slopeの
中央値を基準にresidualを求め、`max(3 * MAD, median peakの10%, 0.05 GiB)`を超える点を除外して
OLSを再計算する。2変数（base/slope）に加えてresidualを評価できる最小数が3点であるため、
それ未満は同梱値を使う。ほぼ同じ長さの点だけでは微小な長さ差で傾きが暴走するためfitしない。
OOM後のCPU再試行はGPU working setが混ざるため学習点へ入れない。
`utteran memory show`で式・保存点数・保存先を表示し、`memory reset`で全点を削除する。

### 13.4 判定、退避、OOM

安全率適用後の予算に対し、推定peakが90%未満ならsafe、90%以上または超過してもbaseが入るなら
danger、baseが予算を超えるならimpossible、式または予算がなければunknownとする。

- `memory_guard=auto`（既定）: danger/impossibleのCUDA/XPUかつdeviceもautoのとき、CPUがsafeと
  確認できた場合だけ事前退避する。退避不能なimpossibleは開始前にerrorとする。
- `memory_guard=warn`: 警告だけで退避しない。ただしimpossibleは開始前にerrorとする。
- `memory_guard=off`: 事前判定をしない。
- 明示deviceは事前退避しない。impossibleならerror、それ以外は警告して続行する。
- errorはCPU等への切替、`--no-diarization`、入力fileの事前分割を提示する。

実行時のCUDA/XPU OOM、system `MemoryError`、bad allocation等を捕捉する。deviceがautoでguardが
autoまたはoffなら、backendをunloadしてCPUへ1回だけ再試行する。warnまたは明示deviceでは
再試行しない。2回目の失敗はそのまま返す。diarization stage内で再試行するため、完了済みaudioと
ASR artifactは保持される。判定、予算、推定、実効device、退避理由、OOM再試行、成功試行peakを
job log、diarization中間結果、merged.json、最終JSONへ記録する。

### 13.5 分割処理の判定（Phase 4b Step 0）

pyannote.audio 4.0.7では`get_segmentations`、`get_embeddings`、`clustering`、`reconstruct`を
個別に呼べ、exclusive diarizationもclustering後に再構成できる。しかしチャンク全体clusterには
`SpeakerDiarization.apply`の主要ロジック複製、`_segmentation.model.receptive_field`等の非公開属性、
重複チャンクの時刻格子統合が必要で、安定した分割APIとは判断できない。このため分岐Bとし、
Phase 4bでは自動分割を実装しない。詳細は`docs/調査結果_Phase4b.md`を参照する。

---

## 14. 依存関係とインストールプロファイル

Phase 3aで、PyTorchのビルド種別を選ぶextrasと機能を追加するextrasを分離した
（15章参照）。

```
utteran                # 軽量コア（推論backendは含めない）
utteran[cpu]           # faster-whisper + pyannote.audio + CPU版torch
utteran[cuda]          # faster-whisper + pyannote.audio + CUDA 12.6版torch
utteran[xpu]           # faster-whisper + pyannote.audio + XPU版torch（Windowsのみ）
utteran[whisper-cpp]   # cmake（whisper.cppネイティブビルド用）
utteran[openvino]      # openvino, openai-whisper, onnxscript
utteran[openvino-genai] # OpenVINO GenAI（Intel CPU/GPU/NPU ASR）
utteran[onnx]          # onnxruntime（sherpa-onnx話者分離はPhase 3以降）
utteran[gui]           # FastAPI + Uvicorn + pywebview（推論依存なし）
utteran[japanese]      # SudachiPy + sudachidict_core（日本語境界補正）
utteran[dev]           # 開発・core test用（faster-whisperを含む）
```

- torchとfaster-whisperはhardware extrasにのみ入れ、Sudachiは全推論profileが組み合わせる
  `japanese` extraに入れる。GUI環境と軽量コアを推論依存・辞書から分離する。
- `intel` profileは`openvino-genai` extraを含める。GUI専用venvには含めない。
- `cpu` / `cuda` / `xpu` は `[tool.uv] conflicts` に登録した排他的extrasで、同一venvに同時
  導入できない。`whisper-cpp` / `openvino` / `onnx` / `dev` はこれらと自由に組み合わせられる
  非排他extras。
- `cpu` はPyTorch公式CPU index、`cuda` はPascal世代を含む実機互換を確認したPyTorch CUDA 12.6
  index、`xpu` はPyTorch公式XPU indexを使用し、`uv.lock` でバージョンを固定する。
- `xpu` extraのtorchは`triton-xpu`パッケージに推移的に依存するが、uvの`explicit = true`
  indexは対象extraの`project.optional-dependencies`に直接列挙されたパッケージにしか
  適用されない。そのため`triton-xpu`を`xpu` extraへ`sys_platform == 'win32'`条件付きで
  直接列挙し、同じ`tool.uv.sources`ルーティングを与えている。torch、torchaudio、pyannote、
  faster-whisperもWindows条件付きとし、非Windows XPU profileは未検証（AISTATE.md I-4参照）。
- CTranslate2 の CUDA 実行に必要な cuDNN / cuBLAS は、CUDA版PyTorchが仮想環境内へ配置した
  DLLディレクトリも探索・登録対象にする。
- PyTorch CUDAの実可用性は、デバイス列挙やメモリ確保だけでなく、最小CUDAカーネルの実行、
  CPUへの結果転送、同期の成功で判定する。
- 旧`.venv` / `.venv-windows`単一環境モデルはPhase 3aでプロファイル別venv
  （`.venvs/<os>-<profile>`、15章）へ置き換えた。既存の`.venv-windows`によるWSL/Windows回避は
  OS識別子を含むディレクトリ名で恒久化された。
- 依存同期または選択profileのprobeが失敗したsetupは成功表示にせず、残りの依存処理を安全に
  スキップして終了コード1を返す。

---

## 15. ライセンス表記

コードは MIT だが、依存モデルのライセンスは異なる。`THIRD_PARTY_NOTICES.md` に記載する。

| 対象 | ライセンス | 注意点 |
|---|---|---|
| Whisper | MIT | |
| pyannote community-1 | CC-BY-4.0 | 利用条件への同意が必要 |
| sherpa-onnx | Apache-2.0 | モデルごとに個別のライセンスあり |
| ffmpeg | GPL/LGPL | 同梱しないため配布物には影響しない |

README にも、モデル利用条件への同意が必要である旨を明記する。

---

## 16. 開発規約

- 型ヒントを全関数に付与する。`mypy` で検査する。
- `ruff` で lint とフォーマットを行う。
- パスは `pathlib.Path` を使用する。文字列連結でパスを組み立てない。
- 外部プロセス呼び出しは `subprocess` のリスト形式で行う。`shell=True` を使わない。
- Windows と Linux の差異はコード内で分岐せず、可能な限り標準ライブラリで吸収する。
- テストは pytest。モデルを必要としないテストと、必要とするテストを
  マーカーで分離する（`@pytest.mark.requires_model`）。
- CI ではモデル不要のテストのみ実行する。

---

## 17. フェーズ計画

### Phase 1 — 骨格と最小動作

プロジェクト構成、Config、CLI 骨格、ffmpeg 音声抽出、faster-whisper（CPU/CUDA）、
pyannote、突き合わせ、SRT/VTT/JSON/TXT/MD 出力、単一ファイル処理。

### Phase 2 — 実運用機能

ジョブ管理とレジューム、フォルダ一括・キュー処理、モデル管理コマンド、
`setup.ps1`、`devices` コマンド、デバイス自動判定。

### Phase 3 — バックエンド拡張

OpenVINO ASR バックエンド、sherpa-onnx 話者分離バックエンド、
バックエンド切り替えの設定と検証。

### Phase 4 — 長時間対応と公開準備

分割処理＋全体クラスタリング、メモリ監視、`eval` コマンド、CI、ドキュメント整備。

### Phase 5a — GUI基盤

推論coreから独立したWindows desktop GUI、CLI JSONL進捗契約、profile／model／deviceの動的選択、
安全なloopback API、process tree中断、theme／多言語設定。

### Phase 5b — GUI結果ビューア

文字起こし結果の仮想スクロール閲覧、検索と話者／時間フィルタ、ジョブ履歴、個別削除、
形式・話者表示名・出力先を変更するexport-only再生成。

### Phase 5c 以降

初回setup wizard、AMD追加検証、exe化、GUI編集機能の検討。

---

## 18. ドキュメント運用ルール

以下の4文書はリポジトリ直下に常に存在し、内容と実装が一致していなければならない。

| ファイル | 内容 | 更新義務 |
|---|---|---|
| `README.md` | 概要、インストール、使い方、対応バックエンド、ライセンス | CLI・インストール手順・対応機能に変更があるたび |
| `変更履歴.md` | Keep a Changelog 形式の変更履歴 | すべての作業単位で |
| `要件定義.md` | 本書。仕様の唯一の情報源 | 仕様変更があるたび |
| `AISTATE.md` | AI 作業者向けの状態記録 | すべての作業単位で |

**更新は実装と同じ作業単位で行うこと。実装だけ進めてドキュメントを後回しにしてはならない。**

### AISTATE.md の役割

AI がセッションをまたいで作業する際のコンテキスト置き場。
新しいセッションを開始した AI が、これを読むだけで現在地を把握できる状態を維持する。
形式は自由だが、以下を含めること。

- 現在のフェーズと進捗
- 直近の作業内容と、その結果
- 未解決の課題、判断を保留している事項
- 設計上の判断とその理由（なぜその選択をしたか）
- 次に着手すべきこと
- 既知の落とし穴、ハマった箇所とその回避方法

人間向けの体裁は不要。AI が読んで作業を再開できることを最優先する。

---

## 19. 実行環境の分離（Phase 3a）

> 章番号について: Phase 3a指示書は本章を「15.」として追記するよう指定していたが、
> 本書には既に15〜18章が存在する。「既存の章番号は変更せず、末尾に追加する」という
> 指示書の原則を優先し、既存章と衝突しない19章として追加した。判断の記録はAISTATE.mdを参照。

### 19.1 背景

PyTorchはCPU版・CUDA版・XPU版が同一パッケージ名の別ビルドであり、1つの仮想環境には
1種類しか導入できない。Phase 2までの単一 `.venv` / `.venv-windows` モデルでは、
プロファイルを切り替えるたびに数GBの再ダウンロードが発生し、CUDA環境とIntel環境を
同時に保持できなかった。Phase 3aはこれをプロファイルごとに独立したvenvを持つ構成へ変更する。

### 19.2 プロファイル定義

| プロファイル | extras | 想定環境 |
|---|---|---|
| `cpu` | `cpu` | GPU なし |
| `cuda` | `cuda` | NVIDIA |
| `intel` | `xpu`, `whisper-cpp`, `openvino` | Intel CPU / Arc / NPU |
| `vulkan` | `cpu`, `whisper-cpp` | AMD その他（OpenVINO なし） |
| `gui` | `gui` | Windows desktop UI（推論しない） |

`vulkan` プロファイルは、AMD GPU 環境を OpenVINO なしで対象に含めるために設ける。
extrasの排他関係（`cpu`/`cuda`/`xpu`）とそれぞれの内容は14章を参照。

### 19.3 venv のレイアウトと設定

```text
<venv_root>/
  win-cpu/
  win-cuda/
  win-intel/
  win-vulkan/
  win-gui/
  linux-cpu/            # WSL等、非Windows環境
```

- ディレクトリ名は `<OS識別子>-<プロファイル名>`。OS識別子は Windows が `win`、
  それ以外（WSLを含む）が `linux`。同一チェックアウトをWSLとWindowsで共有した場合の
  相互干渉を防ぐため（旧 `.venv-windows` 特例の恒久化）。
- `<venv_root>` の既定値はリポジトリ直下の `.venvs`。設定 `[general].venv_dir`
  （空文字は既定値扱い）、環境変数 `UTTERAN_VENV_DIR` の順で上書きできる。
  `UTTERAN_VENV_DIR` は `setup.ps1` / `run.ps1` が Python 側の Config を経由せず
  直接参照できるよう、`UTTERAN_MODEL_DIR` と同じ独立した環境変数として扱う
  （`UTTERAN_GENERAL__VENV_DIR` のようなネスト形式ではない）。
- `.gitignore` は `.venvs/` を除外する。既存の `.venv` / `.venv-windows` は
  Phase 3a移行後も削除・変更しない（利用者が新方式の動作を確認した後、手動で削除する）。
- 既定プロファイルは `[general].default_profile`（空文字は未設定）に保存する。未設定時は
  作成済みプロファイルが1つならそれを使い、複数あれば明示指定を要求する。

### 19.4 共有するもの・分離するもの

| 区分 | 対象 |
|---|---|
| 分離（プロファイルごとに独立） | Python パッケージ一式（venv自体） |
| 共有（全プロファイルで単一） | モデルキャッシュ（`UTTERAN_MODEL_DIR`）、ジョブ（`job_dir`）、`config.toml`、ジョブごとの `utteran.log`、ネイティブビルド成果物（`native_dir`、20章） |

ネイティブビルド成果物（whisper.cppバイナリ）はtorchに依存しないため、プロファイル間で
共有する。ただしOpenVINOランタイムDLLのようにプロファイル固有のライブラリへ依存する構成は、
実行時に「現在アクティブな」venvから動的に解決し、ビルド成果物側にパスを焼き込まない
（20.4参照）。

### 19.5 プロファイル切り替えとジョブの再計算

`[asr]` / `[diarization]` セクションの `backend` / `device` は既に6.3の `config_hash`
算出対象に含まれる。したがって、利用者が `device` をプロファイルごとに明示的な値
（例: `cpu` プロファイルでは `cpu`、`cuda` プロファイルでは `cuda`）へ設定していれば、
プロファイル切り替え時に値が変わり、影響するステージ以降が正しく再計算される。

**既知の制約**: `device = "auto"`（既定値）のまま複数プロファイルで同一ジョブを共有すると、
config.toml上の値は`"auto"`のまま変化しないため、実際に使用されるハードウェアが
プロファイルによって変わっていても `config_hash` は変化せず、レジューム判定は
古いステージ結果を再利用してしまう場合がある。これはconfig_hashが実行時に解決された
具体的なdevice/backendではなく、設定ファイル上の文字列を対象にしているために生じる。
Phase 3aでは解決手段としてハッシュ計算ロジックの変更は行わず（既存のレジューム機構への
影響範囲が大きいため）、`device`/`backend`を明示指定するか、プロファイルごとに別々の
`job_dir`を使うことを利用者に推奨する。この制約はドキュメントに明記した上で残す。

---

## 20. ネイティブビルド（whisper.cpp、Phase 3a基盤のみ）

Phase 3aはwhisper.cppの取得・複数構成ビルド・manifest記録までを実装する。実際の
文字起こし利用（`--asr-backend whisper-cpp`相当の実行経路）はPhase 3bで実装する。

### 20.1 バージョン

- タグ `v1.9.2`、コミット `306c88f4d1286aec1bf96e544632897886af5501` に固定する。
  VAD token時刻を元音声timelineへ戻すgetterを含み、bugfix-cでCPU/OpenVINO/Vulkan/
  OpenVINO+Vulkanの4構成すべてのbuildと起動に成功したため採用する。
- 公式`whisper-cli -ojf`はv1.9.2でもraw `whisper_token_data.t0/t1`を使い続けるため、build前に
  full JSON出力の2取得箇所だけを`whisper_full_get_token_t0/t1()`へ置換する。
  patch適用位置が固定sourceと一致しない場合はbuildを中止し、無理に続行しない。

### 20.2 ビルド構成

| 名称 | CMake フラグ | 前提 |
|---|---|---|
| `cpu` | `-DWHISPER_OPENVINO=OFF -DGGML_VULKAN=OFF` | なし |
| `openvino` | `-DWHISPER_OPENVINO=ON -DGGML_VULKAN=OFF -DOpenVINO_DIR=...` | openvino パッケージ、OpenVINO GPU |
| `vulkan` | `-DWHISPER_OPENVINO=OFF -DGGML_VULKAN=ON` | Vulkan SDK（`glslc`） |
| `openvino_vulkan` | 上記両方 | 両方 |

`openvino_vulkan`はエンコーダをOpenVINO、デコーダをVulkanにオフロードする構成。実測で
最速だった（AISTATE.md参照）。各構成は前提条件（`glslc`の有無、OpenVINO GPUの認識）を
個別に判定し、満たさない構成はエラー理由を記録してスキップする。全体のビルドを失敗させない。

Windowsのビルドには、CMakeのマルチコンフィグ生成器 `Visual Studio 17 2022`
（`-G "Visual Studio 17 2022" -A x64`）を使用する。Ninja生成器と異なりMSVCツールチェインを
自力で検出できるため、`vcvars64.bat`を事前に読み込んでいない通常のPowerShell/cmdセッションから
`utteran native build`を実行できることを実機で確認した。

### 20.3 配置

```text
<native_dir>/
  <platform>/
    src/            whisper.cppのソース
    cpu/
    ov/
    vk/
    ovvk/           openvino_vulkanの短縮ディレクトリ名
    manifest.json
```

- `<native_dir>` の既定値は `~/.utteran/native`。`platformdirs.user_data_dir`配下は
  Windowsで長くなりがちなため、より短い固定パスを既定とした。設定
  `[general].native_dir`（空文字は既定値扱い）、環境変数 `UTTERAN_NATIVE_DIR` で
  上書きできる（`venv_dir`と同じ独立した環境変数扱い）。
- ビルドディレクトリ名は短縮する（`openvino_vulkan`→`ovvk`等）。ggmlのVulkanシェーダー生成
  サブビルドが深くネストし、MSVCのFileTrackerがMAX_PATHで失敗するため。
- ビルド成果物は全プロファイルで共有する（19.4参照）。

### 20.4 manifest

```json
{
  "schema_version": 1,
  "platform": "win-amd64",
  "whisper_cpp": {
    "tag": "v1.9.2",
    "commit": "306c88f4...",
    "patch_level": "utteran-token-json-original-timeline-v1"
  },
  "built_at": "...",
  "backends": {
    "openvino_vulkan": {
      "executable": "...",
      "cmake_flags": ["..."],
      "requires": ["openvino", "vulkan"]
    }
  },
  "errors": { "vulkan": "..." }
}
```

**manifestにはOpenVINOランタイムDLLディレクトリなど実行時ライブラリの絶対パスを
一切記録しない。** ビルド成果物は全プロファイルで共有するのに対し、OpenVINOの
DLLは各プロファイルvenvの`site-packages/openvino/libs`にあり、プロファイルごとに
異なるパスになるため、manifestへ焼き込むと他プロファイルから実行できなくなる。
実行時ライブラリのディレクトリは、実行時に現在アクティブな環境から動的に解決する
（`openvino`パッケージがimport可能かどうかで判定）。

### 20.5 CLI

```text
utteran native build [--variant cpu,vulkan,...] [--force]
utteran native status
utteran native clean [--all | --variant <名前>]
```

`build`は前提条件を満たす構成のみをビルドし、満たさないものは理由を記録してスキップする
（要求された構成のうち1つもビルドできなかった場合のみ終了コード3）。`status`はmanifestの
内容と、現在の環境で各構成が実行可能かを表示する。

### 20.6 devices への統合

`utteran devices` は以下を追加表示する（既存のJSON構造は変更せず追加のみ）。

- 現在のプロファイル名と、作成済みの他プロファイルの一覧（存在・最終更新のみ。
  他プロファイルのPythonは起動しない）
- Vulkanのビルド前提（`glslc`）とランタイム（`vulkaninfo`）を区別した検出結果
  （一方だけが利用可能な場合があるため、単一のavailable/unavailableに集約しない）
- ネイティブビルドの状態（manifestの有無、whisper.cppタグ、各構成の実行可否）

---

## 21. whisper.cpp ASR（Phase 3b）

### 21.1 単語タイムスタンプ

- 3秒を超える単語時刻を含む場合、segment全体の単語を破棄せず、異常単語だけを除外する。
  正常単語を部分保持したsegmentは本文全体の位置を捏造せず、`speaker_confidence=low`として
  単一話者へ割り当て、前後と結合しない。全単語が異常な場合だけwordless fallbackとする。
- 単語群がsegment先頭または末尾から3秒超離れているだけで、各単語長が正常なら、単語時刻を
  保持してsegment範囲を観測済み単語の最小開始・最大終了へ補正する。
- `t_dtw`が全て`-1`でもsegment内の正のtoken offsetは破棄せず、低信頼として保持する。
  文字数按分など、観測されていない単語精度の時刻は生成しない。
- 時刻異常eventには本文を含めず、各segmentの区間、word数、3秒超word数、最大word長、
  segment先頭・末尾の未被覆秒数、該当条件を記録する。

`[asr].word_timestamps` は `auto`（既定）/`always`/`never` とする。`auto` は話者分離が
有効な場合だけ、`always` は常時、`never` は取得しない。これはwhisper.cpp経路に適用し、
既存のfaster-whisper経路は従来どおり単語時刻を取得する。

whisper.cppで取得する場合は、モデルカタログのDTWプリセットを解決し、`--dtw`と
`--no-flash-attn`を必ず併用する。`dtw = "auto"`でプリセットのないモデルを`auto`モードで
使う場合は警告して単語なしで続行し、`always`では設定エラーとする。通常トークンの`t_dtw`が
すべて`-1`ならDTW失敗として警告し、壊れたDTW時刻は返さずセグメント単位の話者割当に
フォールバックする。

whisper.cpp v1.9.2の`whisper_full_get_token_t0/t1()`は、内蔵VAD使用時にtoken時刻を元音声
timelineへ戻す。一方、公式CLIのfull JSONはraw `whisper_token_data.t0/t1`を使うため、上記patchで
JSONのtoken offsetsだけをgetter由来にする。`t_dtw`はVAD圧縮timelineのままだが、Python側は
segment内の正のoffsetsを優先する。これにより単語時刻要求時もVADを有効にする。既知時刻の合成音声
による回帰試験では、全wordがsegment範囲内、最長wordが数秒未満、発話前無音による時刻shiftなしを
確認する。ASR stage hashにはpolicy versionを含め、この変更前の中間結果を再利用しない。

文単位のsegmentは維持し、Python側で特殊トークン除外、UTF-8バイト断片の復元、空白言語の
先頭空白境界、日中韓言語の文字境界、句読点の直前語への結合を行う。時刻は構成トークンの
正のoffsets（ミリ秒）を優先し、利用できない場合だけ`t_dtw`（whisper.cppの10ミリ秒tick）へ
退避する。DTWが局所的に逆行してもsegment順序へ伝播させないためである。
確率は構成トークンの`p`の平均とし、時刻は所属segment内へclampする。単語がないsegmentは
既存align.pyのsegment単位割当にフォールバックする。

変換後のwordが`max_word_duration_seconds`（既定3.0秒）を1件でも超える、またはword群がsegmentの
片端へ潰れて同閾値を超える無語範囲を残す場合、壊れたword時刻だけを破棄する。segment本文とoffsetは
保持し、segment単位の話者割当へfallbackする。fallbackしたword数、segment数、segment span合計秒、
率、開始・終了時刻を本文なしeventへ記録し、segment率5%以上はwarningにする。本文を異常wordと一緒に
捨ててはならない。この閾値とASR policy versionをASR stage hashへ含める。

### 21.2 GGMLモデルカタログ

`ggerganov/whisper.cpp`で実在を確認したGGMLファイルをサイズ・量子化単位で登録する。
通常の`models list --available`は多言語の推奨6件だけを表示し、`--all`併用時は英語専用を
含む全件を表示する。各項目は実ファイル名、実測ファイルサイズ、モデルサイズ、量子化、
DTWプリセット、ライセンスを保持する。共有リポジトリ全体ではなく選択した単一GGMLファイル
だけを取得する。

whisper.cpp v1.9.2のCLIはOpenVINO IRパスの明示指定を公開しておらず、GGMLファイル名の
`.bin`を`-encoder-openvino.xml`へ置換して導出する。このため量子化違いでは、サイズごとの
共通IRを各GGML名に対応する規約名へリンクし、リンク非対応時だけコピーする。

### 21.3 OpenVINO encoder IR管理

`models prepare-openvino <whisper-cppモデルID> [--device GPU] [--purge-cache] [--yes]`で
OpenAI公式PyTorch重みからモデルサイズ単位のIRを生成する。実行前に最大約3GBの別取得を
明示して確認し、`--yes`だけが確認を省略する。openvino extraがなければsetup方法を案内する。
生成物は`<model_dir>/openvino-encoder/<size>/`へXML/BIN一組で保存し、GGMLの量子化ごとの
規約名へハードリンク（非対応環境ではコピー）する。`list-openvino`で一覧、
`remove-openvino`でIRとaliasを削除し、`models verify`は片方欠落やalias欠落を不整合とする。

### 21.4 whisper.cppバックエンド

`load()`はmanifest、構成、GGMLファイルを検証するだけでモデルを常駐させず、`transcribe()`が
正規化済み16kHz mono WAVごとに`whisper-cli`を起動する。出力は一時ディレクトリのfull JSONへ
書き、読取後に削除する。進捗stderrだけを解析し、表示・エラー詳細は秘密値マスクを通す。
キャンセルはWindowsでは`taskkill /T /F`でprocess treeを止める。OpenVINO DLLは現在のvenvから
動的にPATHへ追加しmanifestへ記録しない。バッチではbackend解決を再利用するが、GGML自体は
ファイルごとに再ロードされる。

### 21.5 auto選択とフォールバック

autoは、(1)利用可能なCUDA→faster-whisper/CUDA、(2)Vulkan+OpenVINO GPU+build→
whisper-cpp/openvino_vulkan、(3)Vulkan+build→whisper-cpp/vulkan、(4)OpenVINO GPU+build→
whisper-cpp/openvino、(5)faster-whisper/CPUの順で選び、理由をdevicesのnotesへ記録する。
CPUではwhisper.cppをauto選択しない。

variantがautoでGPU初期化に失敗した場合だけ、v1.9.2で確認した初期化エラーを判定し、次の
利用可能なGPU構成へ1回だけ退避する。退避元、先、秘密値mask済み理由を警告・ジョブログへ
残す。backendまたはvariantの明示指定時、モデル形式エラー時、2回目の失敗時は退避しない。
faster-whisperとpyannoteも同じ原則とし、明示deviceでは退避せず、`device="auto"`でaccelerator初期化に
失敗した場合だけCPUへ1回退避する。

---

## 22. torch XPU 話者分離（Phase 3c）

`[diarization].device` は `auto | cpu | cuda | cuda:N | xpu | xpu:N` とする。autoは
PyTorchの実カーネルprobeに成功したCUDA、XPU、CPUの順に選択する。XPUは`intel`プロファイル
専用であり、他プロファイルからの明示指定は`setup.ps1 -Profile intel`を案内して拒否する。

pyannote community-1はArc 140T実機で`Pipeline.to(xpu:0)`、segmentationとspeaker embeddingを
含む実推論が成功したため分岐Aとして対応する。autoで選んだXPUの初期化に失敗した場合だけCPUへ
一度退避し、明示指定では退避しない。XPUのメモリ不足は共有システムRAMの不足として説明し、
`unload()`でXPU allocatorを明示解放する。deviceは話者分離stageのconfig hashに含め、変更時は
再計算する。
# Phase 3d 是正仕様

## whisper.cpp ハルシネーション対策と機能パリティ

| 目的 | faster-whisper | whisper.cpp v1.9.2+patch | whisper.cpp既定 |
|---|---|---|---|
| 前文脈遮断 | `condition_on_previous_text=false` | `--max-context 0` | 有効 |
| 無音除去 | `vad_filter=true` | `--vad --vad-model PATH`（Silero GGML必須） | 無効 |
| decoder失敗判定 | backend既定 | entropy 2.4 / logprob -1.0 | 有効 |
| 無音閾値 | backend既定 | `--no-speech-thold 0.6` | 有効 |
| 温度fallback | backend既定 | temperature 0.0 / increment 0.2 | 有効 |

`[asr.whisper_cpp]`は`no_context`、`vad`、`vad_model`、`vad_threshold`、各decoder閾値、温度、
`repetition_limit`を持つ。Silero v6.2.0は`whisper-cpp-vad`カタログからdownload/verifyでき、VAD
有効時に未取得なら取得コマンド付きで停止する。同一segmentは既定10件まで残し、以降の抑制件数を
warningへ記録する。0は無効。会議の相槌など正当な反復も削る可能性があるため根本対策後の保険とする。

24分46秒実測で、flash有効/DTWなしは324 segments・5559文字・最大反復3、flash無効/DTWなしと
flash無効/DTWありはともに297 segments・5606文字・最大反復2だった。この素材の結果変化はDTWで
なくflash attention無効化に帰属する。利用者向け設定は増やさず、切り分け専用環境変数だけを残す。

対策なしは最大150連続、前文脈遮断のみ3、VADのみ2、entropyのみ31、logprobのみ150、no-speech
のみ150だった。よって`no_context=true`とwhisper.cpp既定decoder閾値・温度fallbackを既定維持し、
追加モデルとsegment構成変化を伴うVADはopt-inとする。これらはwhisper.cpp設定hashへ含めるが、
faster-whisper設定・実行経路には適用しない。

## benchmark とauto選択

`utteran benchmark --audio PATH [--variants ...] [--word-timestamps auto|always|never]
[--repeat N] [--warmup N] [--durations 180,900,full] [--json PATH] [--apply]`を提供する。
実データを暗黙選択しないためaudioは必須。既定測定長は指定WAV全体（`full`）とし、短いfixtureへ
暗黙に切り詰めない。複数長を指定した場合は入力WAVのprefixを一時生成し、長さごとに全構成を測る。
入力より長い指定は設定エラーとする。一時WAV、認識本文、ジョブは保存しない。

長時間用途を判断する推奨最小長は900秒（15分）とする。主用途の下限30分より短いが、CPU構成を
含む測定時間との妥協であり、OpenVINO初期化コストの償却を観測する最低限の長さとして扱う。
900秒未満には、Phase 3dで180秒と24分46秒のVulkan/ovvk順位逆転が観測された事実を含む警告を
人間向け表示とJSONへ記録する。既定warmup 1回・計測3回の中央値を報告する。
`--apply`は最長の測定における最速whisper.cpp構成を`[asr.whisper_cpp].variant`へ保存し、同sectionの
`benchmark_duration_seconds`へ測定長をmetadataとして記録する。このmetadataはConfigでは無視し、
ASR stage config hashへ含めない。JSONは複数長を保持するschema version 2とする。話者分離比較は対象外。

### Phase 6a バックエンド横断benchmark

測定対象は`backend / device / model / quantization / compute_type`の組として表し、backend registryが
有効なdeviceとfactoryを解決する。registryはfaster-whisper（cpu/cuda）、whisper.cpp
（cpu/openvino/vulkan/openvino_vulkan）、OpenVINO GenAI（cpu/gpu/npu）を持つ。
旧`--variants`はこの組への互換aliasとして維持し、新規指定は`--targets backend/device/model`を使う。

`--targets`、`--variants`、modeによるmatrix列挙は、経路ごとの個別実装を持たず、同じtarget/device
解決を通す。明示deviceは変更せず、利用不能なら理由付きerrorで停止して別deviceへfallbackしない。
`device=auto`の場合だけPhase 5kの検出結果から解決し、faster-whisperは検出済みCTranslate2 reportで
device indexとcompute typeを確定する。結果JSONのtargetには要求device、解決device、決定種別
（明示 / auto / matrix列挙）と根拠を保存し、記録deviceはbackendへ渡した実値と一致させる。
benchmarkはASRだけを対象とし、話者分離deviceを受け取らない。

Phase 5kの`detect_devices()`とそのfingerprint cacheだけを可用性判定に使う。状態は`runnable`、
`preparation`、`hidden`、`unknown`の4種とし、GPUが物理的にない`hidden`は候補へ表示しない。
モデル、native build、OpenVINO runtime/encoder IRが不足する場合は`preparation`として具体的な準備
コマンドを示す。timeout/errorは`unknown`であり、実行可能として扱わない。CUDA判定はモデル有無より
先に行い、CUDAなし機で未取得CUDAモデルを「準備すれば可能」と誤表示しない。

速度スコアは**実時間比 × 100を整数丸め**し、基準モデルは**large-v3-turbo**固定とする。他モデルは
参考値。測定条件としてbackend、device、モデル、量子化、音声長、単語timestamp有無を保存・表示し、
`1時間の音声 → 約 round(60 / 実時間比) 分`も表示する。日本語精度は分かち書きに依存するWERでなく
CERを採用する。NFKC＋casefold後、空白・句読点を除くが英数字・仮名・漢字は保持し、Levenshtein距離を
正解文字数で除した値をCERとする。**精度スコア = round(max(0, 1 - CER) × 100)**（0〜100）である。

モードはquick（最大60秒、warmup 0、1回、速度のみ、10〜20分）、standard（最大180秒、warmup 1、
2回、速度＋精度、30〜60分）、detailed（60/180/900/full、warmup 1、3回、複数モデル、速度＋精度、
数時間）とする。入力が規定長より短ければfullを使う。精度はWAV同名`.txt`または`--reference-text`の
正解がある場合だけ算出し、不在時に値を捏造しない。同梱fixtureは合成音声と正解textで、実音声は
ライセンス・容量上、既定素材にしない。

結果はPhase 5jの解決済みlog dir配下`benchmarks/benchmark-*.json`へschema v3で測定対象ごとに
atomic replaceし、`status=running/interrupted/completed`と途中結果を保持する。環境にはutteran、OS、
Python、CPU、GPU/runtime、whisper.cpp、driver変更検出用の非可逆fingerprintを保存し、認識本文、
入力path、job、usernameは保存しない。人間向けtable、`--json`、`--markdown`を提供する。前回結果を
参照し、utteran versionが違えば再測定を促す。最長のlarge-v3-turbo測定から推奨を選び、`--apply`は
backend/device/modelと測定日時・音声長を記録する。auto順位自体はPhase 6cまで変更しない。

**このスコアは目安である。音声の内容、長さ、同時に動作する他の処理により変動する。
スコアが2倍でも、処理時間が半分になることを保証しない。**

### R-7: 反復対策適用後の再測定とauto順序の再確認

R-1の反復対策（`no_context=true`等）適用後、whisper-cliへ渡すコマンドライン引数はR-2時点の
測定（Vulkan 20.488秒/ovvk 32.564秒）から変化していないことをコード差分で確認したうえで、
`utteran benchmark`を使い全構成・単語TSあり/なし・VAD有効時・実音声長の4条件で再測定した。

Intel Core Ultra 7 255H / Arc 140T、large-v3-turbo-q5_0、180秒WAV、warmup 1・3回中央値。

| 構成 | TSなし | TSあり |
|---|---:|---:|
| whisper-cpp / cpu | 148.460秒 (1.213x) | 183.135秒 (0.983x) |
| whisper-cpp / openvino | 30.587秒 (5.886x) | 34.138秒 (5.273x) |
| whisper-cpp / vulkan | 15.363秒 (11.718x) | 22.310秒 (8.069x) |
| whisper-cpp / openvino_vulkan | 19.638秒 (9.167x) | 24.086秒 (7.474x) |
| faster-whisper / cpu | 79.551秒 (2.263x) | 74.672秒 (2.411x) |

いずれもR-2時点の測定と同程度かやや高速（cpu/openvino/vulkanで約10〜13%）で、
whisper-cliへの引数が不変であることと合わせ、通常の測定ばらつき（熱・電源状態等）による差と判断する。
反復対策そのものによる速度改善ではない。VAD有効時のVulkan（TSなし）は14.322秒（12.570x）で、
無効時（15.363秒）と有意差はなく、VAD自体は処理時間にほぼ影響しない
（既定opt-inのままとする判断はR-1の記録どおり変更しない）。

**180秒fixtureではVulkanがopenvino_vulkanより高速という順序は変わらなかった。**
一方、24分46秒の実会議WAV（単語TSなし、1点測定）では次のとおり、**順序が逆転した**。

| 構成 | 実時間比 | 秒 |
|---|---:|---:|
| whisper-cpp / vulkan | 13.420x | 110.730秒 |
| whisper-cpp / openvino_vulkan | 22.811x | 65.141秒 |

180秒の測定値から音声長の比（約8.255倍）で単純に外挿するとVulkanは126.8秒、ovvkは162.1秒の
見込みになるが、実測はVulkan 110.7秒、ovvk 65.1秒であり、**ovvkはこの実音声で
Vulkanより約41%高速**という、外挿からは予測できない逆転が生じた。原因はOpenVINOエンコーダの
初期化・パイプライン確立コストが短い音声では相対的に大きく、長い音声で償却されると
考えられるが、単一環境・単一素材の1点測定であり確定的な原因究明は行っていない。

**決定**: `auto`の既定順序（`vulkan` → `openvino_vulkan` → `openvino` → `cpu`）は変更しない。
理由:

1. `vulkan`はOpenVINO IRの事前変換（`models prepare-openvino`、モデルサイズにより最大約3GB相当の
   追加取得と変換時間）を必要とせず、未変換環境でも`auto`が確実に動作する
2. 現在の`_choose_variant`／auto fallbackはnative buildの有無だけを見ており、
   モデルごとのIR生成状態までは判定しない。`auto`が`openvino_vulkan`を既定で選ぶよう変更すると、
   IR未生成環境で`auto`実行時に初期化失敗（IRファイル不在）を誘発しうる。これを安全にするには
   IR生成状態を`auto`選択に組み込む追加実装が必要で、本ステップ（再測定・再確認）の範囲を超える
3. 短い素材でも長い素材でも「動く」ことを優先し、利用者の追加作業（IR変換）なしに
   既定で確実に高速な構成を選ぶ方針（R-2の判断根拠）を継続する

ただし、**実際の主用途（30分〜3時間の会議、1.3節）ではovvkが大きく有利な場合がある**ため、
この非自明な逆転をREADMEで目立つ形で明記し、長時間音声を扱う利用者には`utteran benchmark`を
自分の素材で実行するか、IR変換済みなら`[asr.whisper_cpp].variant = "openvino_vulkan"`を
明示指定するよう案内する。`devices`のauto表示・理由文言は`vulkan`優先のままで変更しない
（4の判断により順序自体は変えていないため追従の必要なし）。

## 受入試験ハーネスの位置づけ（R-4）

`tools/acceptance/`のハーネス（`harness.py`/`cases.json`）は、Phase 1〜3の受入試験を実行する
コマンド実行器であると同時に、**将来GUIが「この環境で何が実行できるか」を自動判断するための
基盤**として設計する。各ケースは`requires`（プロファイル・backend・ネイティブビルド構成・
導入済みモデル・CUDA/XPUの有無）を持ち、実行前に`devices --json`／`models list --json`の
スナップショットと突き合わせて実行可否を判定する。判定不能な場合は失敗ではなく理由付きの
スキップとして記録する。`destructive`（環境を変更するケース）は既定で実行せず、
明示指定時のみ実行する。結果は`results.jsonl`（1ケース1行）に加え、実行全体のサマリーを
JSONで出力できる。CLIから起動するだけでなく`run_selected()`としてPython APIからも呼び出せ、
将来GUIがサブプロセスを介さず直接利用できる。詳細なスキーマは
`tools/acceptance/README.md`を参照。

全ケースは`id`/`group`/`description`に加えて`requires`/`destructive`/`estimated_seconds`を
宣言し、実行時に選択した`results.jsonl`は`{results}`プレースホルダで集計ケースへ引き渡す。
環境要件による`skip`は受入失敗に含めず、理由をサマリーへ残す。2026-08-05のIntel実機で
長時間・破壊的ケースを含む統合192ケースを実行し、ID別最新結果177合格・15 CUDA理由付きskip・
0失敗を確認した。P系は77/77合格。詳細は`docs/受入試験統合結果_Phase3d.md`に記録する。

話者分離の`--require-quality`は従来の平均exclusive turn 0.5秒以上、UNKNOWN 20%未満、
dominant speaker 99%以下に加え、複数話者出力で**1.0 segment/分以上**かつ
**最長出力segmentが音声長の25%以下**を要求する。3分実測25〜31件、修正前116.2分18件・
最長52.66%、修正後859件・最長1.00%を根拠とする。前者は細分化過多、後二者は粗すぎる
出力を互いに独立に検出する。

---

## 23. 継続的インテグレーションと品質保証（Phase 4a）

GitHub Actionsは次を自動検査する。

| 検査 | OS |
|---|---|
| `ruff check` / `ruff format --check` / `mypy` / `uv lock --check` | Linux |
| `pytest -m "not requires_model"` | Linux / Windows |
| 全追跡`.ps1`のUTF-8 BOMバイト検査 | Linux |
| 全追跡`.ps1`のParser API構文検査とWindows PowerShell 5.1実起動 | Windows |
| 現在treeのprivate data blocking走査と全到達可能履歴の監査 | Linux |

BOMはParser APIでは検出できないため、`tools/check_powershell_bom.py`が先頭3 byteを直接検査する。
Windows側では静的parseに加え、`setup.ps1 -List`、`start.ps1`の終了選択、`run.ps1`のread-only起動を
`powershell.exe`から実行し、起動時のencoding解決も通す。

CIは、Python 3.12でのimport、lint、型、モデル不要試験、PowerShell起動、公開treeの衛生を担保する
**最低限の回帰ゲート**である。Vulkan SDKやOpenVINOを使うnative build、実モデル、GPU、長時間音声、
性能、デバイス固有fallback、破壊後の復元はCIで検証しない。これらを含む実質的な品質保証と
release判定は`tools/acceptance/`の統合受入試験ハーネスが担う。LinuxはCIにより最低限のimportと
モデル不要テストを継続確認するが、GPU/native backendの動作保証対象ではない。

---

## 24. Versioningとrelease（Phase 4a）

Semantic Versioningを採用する。1.0以降の互換性を壊すCLI/config/job/output schema変更はMAJOR、
後方互換機能はMINOR、後方互換修正はPATCHとする。1.0未満は開発版で、breaking changeはMINORを
上げて変更履歴へ明記する。

`v0.0.1`は2026-08-02の初期開発snapshotであり、変更履歴の未release節を切り出さず公開されたため、
最初の正式なsupport対象releaseとは扱わない。Phase 4受入後の`v0.1.0`を最初の正式なpre-1.0
release候補とする。release時は`pyproject.toml`と`utteran.__version__`を一致させ、未release節を
日付付きversion節へ移し、新しい未release節を作る。既存tagは移動・上書きしない。

CI合格に加え、公開履歴／GitHub面の監査、対象hardwareの統合受入試験、license/model条件の確認を
release gateとする。tag作成、push、GitHub Release公開は人間の承認後に行い、model、実録音、
transcript、秘密設定、job/log、benchmark元音声をrelease assetへ含めない。詳細手順は
`docs/リリース手順.md`を正とする。

### 24.1 作業単位ごとのバージョン運用

上記のrelease gate（tag作成・push・GitHub Release公開）とは別に、日常の開発作業でも
「どの版で試したか」を常に追跡できるよう、以下を運用規約とする。

- 変更を加えた作業単位ごとに、パッチバージョンを1つ上げる。`v0.1.9`の次は`v0.1.10`であり
  `v0.2.0`ではない。パッチ番号は9で止めず、10を超えて増え続ける。
- マイナーバージョン（`0.MINOR.x`のMINOR）の繰り上げは、利用者が明示的に指示したときのみ行う。
  AIが独断で繰り上げてはならない。
- `変更履歴.md`に、そのバージョンの節をKeep a Changelog形式（追加・変更・修正・削除）で作成し、
  実施した作業単位ごとに追記する。最後にまとめて書かない。既定値の変更、ジョブの再計算を伴う
  変更、設定の互換性が失われる変更など破壊的変更は明示する。
- バージョン更新時に揃えるべき箇所の一覧と手順は`docs/リリース手順.md`を正とする。

---

## 25. 公開履歴の照合監査（Phase 4a）

公開CIは、現在の追跡fileを対象にemail形式、user絶対path、秘密値形式、
media拡張子、環境設定fileを汎用検査する。利用者固有の組織名、domain、email local部、
入力file名、Windows user名は公開repositoryに保存できない。これらは
`tools/private_history_match.py`で`input/`のfile名だけからhash patternを作成し、
Git対象外の`output/`に置いてローカルで全ref・object・message・差分・現在treeを照合する。
一致値とdigestは報告へ出力せず、分類、件数、commit、pathだけを保存する。

Git repository内の文字起こし出力先は、Gitが無視するdirectoryでなければならない。
既定の`output/`に加え、`transcripts/`と`utteran-output/`を明示的な安全出力先とする。
repository内のその他のdirectoryは、SRT/VTT以外のJSON/TXT/Markdownが追跡されるため
export開始前に終了code 2で拒否する。repository外の出力先は制限しない。

commit messageにemail形式を持つtrailerは追加しない。contributorのcreditはPRまたは
release noteに記載し、非公開addressを転記しない。

---

## 26. WindowsデスクトップGUI（Phase 5a）

### 26.1 構成と実行環境

- GUIは独立package `utteran_gui`とし、`utteran`／推論backendを直接importしてはならない。
- `.venvs/win-gui`には`gui` extraだけを導入し、PyTorchとfaster-whisperを含めない。
  `setup.ps1 -Profile gui`で構築し、`gui.ps1`から`utteran-gui`を起動する。
- 推論は`.venvs/win-<cpu|cuda|intel|vulkan>/Scripts/utteran.exe`をshellを介さない引数配列で
  子process起動する。GUIと推論profileは別々に更新・診断できること。
- GUIは`profiles list --json`、`devices --json`、`models list --json`、
  `native status --json`だけを環境情報の正とし、GUI側にbackendやhardwareの可用性を固定しない。
  現在profileで導入済みかつ利用可能なmodel、device、native構成だけを選択肢へ出す。
  実行開始直前にも同じ情報で再検証する。

### 26.2 CLI進捗JSONL契約

`transcribe --progress-json`は、UTF-8のJSON objectを1 event 1行でstderrへ逐次flushする。
標準のRich表示と併用でき、`--quiet`でもJSONLは抑止しない。GUIは完全なJSON行だけをeventとして
解釈し、不完全行、非JSON行、stdoutはマスク後の詳細logとして保持する。

全eventの共通fieldは次のとおり。

| field | 型 | 意味 |
|---|---|---|
| `schema_version` | integer | Phase 5aでは`1` |
| `timestamp` | string | UTCのISO 8601日時 |
| `event` | string | event種別 |

event別fieldは次のとおり。

| event | field |
|---|---|
| `job_resolved` | `job_id`, `resumed`, `stages` |
| `file_start` | `file_index`, `file_total`, `input_path` |
| `file_done` | `file_index`, `file_total`, `input_path`, `status`, `reason`, `job_id` |
| `stage_start` | `stage` |
| `progress` | `stage`, `completed`, `total`, `ratio`, `message` |
| `stage_done` | `stage`, `duration_seconds`, `skipped` |
| `output_written` | `path`, `format` |
| `run_summary` | `job_id`, `asr_backend`, `asr_model`, `asr_device`, `executed_stages`, `reused_stages` |
| `warning` | `stage`, `message` |
| `error` | `error_type`, `message`, `exit_code` |
| `done` | `duration_seconds`, `exit_code` |

pathとmessageを含む全stringは既存の秘密maskを再帰適用する。eventにはASR segment、word、
文字起こし本文、tokenを含めてはならない。正常・失敗・中断のいずれも最後に`done`を1回だけ出し、
失敗時はその前に`error`を出す。batchは各入力を`file_start`／`file_done`で囲む。

### 26.3 local serverの防御境界

- OSにIPv4 `127.0.0.1:0`をbindさせ、割り当てられたloopback portだけでUvicornを起動する。
  LAN addressや`0.0.0.0`へbindしてはならない。
- process起動時に暗号学的random session keyを生成する。全`/api/*` requestは
  `x-utteran-session` headerまたは`HttpOnly; SameSite=Strict` cookieの一致を必須とする。
  初期`/launch`だけがkeyを検証してcookieを設定する。key自体をlogへ残さない。
- CORSを有効にしない。CSP、`X-Content-Type-Options`、`Referrer-Policy`等のsecurity headerを
  付け、WebViewから同一origin APIだけを利用する。
- serverは事前bind済みsocketをUvicornへ渡し、port確認とlisten開始の間に差替え余地を作らない。

### 26.4 job、進捗、中断

- 同時実行は1 jobに限定する。保持状態は`starting | running | completed | failed | cancelled`の5値。
  `starting/running`は進行中、`completed`だけ成功、`failed`だけ失敗、`cancelled`は中断とする。
  未知値は失敗へ倒さず不明としてlogへ記録する。2件目は拒否する。
- stderr JSONLをbackground threadで読み、SSEでstage、経過時間、推定残時間、生成file、詳細logを
  WebViewへ通知する。一定時間eventがない場合は停止と断定せず「応答待ち」と表示する。
- CLIの`done` JSONLはprocess終了直前に届き得るため、GUIは`done`だけで結果を確定しない。
  API保持状態が終端値になり、終了codeが確定するまでpollとSSEを維持する。
- 通常処理に固定timeoutを設けない。中断時はWindowsの`taskkill /T /F`、POSIXではprocess groupへ
  signalを送り、子孫processを含めて終了する。終了code 130を中断として扱う。
- GUIが起動するprofile CLI、setup、model操作、runtime probeは1つのsupervisorへ登録する。
  ウィンドウの正常終了では新規起動を止め、稼働中の子process treeを先に終了し、最大5秒待ってから
  GUI serverを終了する。待機中は失敗とせず終了処理中として扱い、終了できなかった場合だけ診断する。
  Windowsでは全ての直接の子processを`JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`付きJob Objectへ所属させ、
  GUI本体が強制終了されてもOSによるhandle closeで子孫を残さない。Job Objectへ所属させられない
  processは未管理のまま実行せず、そのprocess treeを終了して起動失敗にする。
- キャンセル要求後は「終了処理中」であることと、OpenVINO GenAIのnative推論は15分音声の実測で
  GPU約1分、NPU約3分、CPU約5分（音声が長いほど延び得る）応答に時間がかかる旨を表示する。
  この待機を失敗表示へ変換しない。推論内部のキャンセル粒度自体は変更しない。
- CLIの終了codeとerror typeから、設定、token、model、ffmpeg、memory、入力、一部失敗の
  復旧案内を表示する。raw logも必ず秘密mask後に保持・表示する。
- 正常終了時は実際に結果へ記録されたASR backend／model／deviceと、今回実行したstage／cacheから
  再利用したstageをCLIとGUIの完了表示へ明示する。

### 26.5 画面、設定、privacy

- themeは`system`／`light`／`dark`の3値とし、既定の`system`は
  `prefers-color-scheme`でOS設定へ追従する。既存settingsに`dark`または`light`が記録済みなら
  その値を尊重し、theme fieldがない場合だけ`system`へ移行する。日本語を既定としEnglishを
  選択でき、表示文言は外部dictionaryへ分離する。
- profile／backend／model／ASR device／話者分離device、話者分離ON/OFF、話者数制約、言語、
  出力形式、resume／fresh／force、file／folder入力、再帰・include・excludeを設定できる。
- stage進捗、経過時間、ETA、応答待ち、詳細log、中断、生成file一覧、出力folderを開く操作を
  提供する。WebView標準のdrag-and-dropは絶対pathを保証できないため提供せず、file／folder選択dialogと
  path手入力を使う。ファイル名だけをpath欄へ設定しない。文字起こし本文をGUIの永続設定へ保存しない。
- theme、language、既定profile、既定入出力directoryだけを`platformdirs`のuser config directoryに
  ある`settings.json`へ原子的に保存する。更新は変更fieldだけをmergeする`PATCH /api/settings`
  とし、process内lockの下でread-modify-writeして、連続／並行操作で他fieldを巻き戻さない。
  選択した入力fileの履歴は保存しない。
- Hugging Face tokenはservice `utteran`、user `huggingface`としてOS keyringだけへ保存・削除する。
  保存直後に同じslotから再取得して一致を確認する。APIと画面へtoken値を返さず、設定済み、未設定、
  keyring利用不能を区別する。利用不能時はWindows資格情報マネージャーと、CLIが優先参照する
  `HF_TOKEN`環境変数／`.env`を案内する。

---

## 27. GUI結果閲覧・検索・ジョブ履歴（Phase 5b）

### 27.1 結果の正本とJSON契約

- GUIはPhase 5aと同じく`utteran`をimportせず、profile CLIの`jobs list --json`／
  `jobs show --json`／`jobs export --json`／`jobs clean --job-id --json`だけを呼び出す。
- 表示と再出力の正本はジョブdirectoryのschema version付き`merged.json`とする。利用者指定先の
  出力JSONは移動・削除され得るうえ、話者表示名適用後は内部`SPEAKER_00`との対応を失うため、
  正本やfallbackにはしない。`merged.json`はresumeと同じ寿命を持ち、内部話者ラベル、単語時刻、
  処理backend／model／deviceを保持する。
- `merged.json`の`schema_version`が対応version 1と異なる、欠落する、または内容を復元できない場合、
  jobを`corrupt`として表示し、対応versionと検出versionを含むエラーを返す。壊れた本文表示や
  推測による変換を行わない。
- `jobs list --json`は入力file名、作成／更新日時、状態、容量、ASR／話者分離のmodelとdevice、
  話者数、音声長、結果可否、検証済みresult path、出力pathを返す。GUIは一度このCLI契約でjob
  directoryを特定した後、core processを再起動せず標準JSONとして直接読む。`jobs show --json`も
  自動化向けにviewer schema version 1のsegment／word情報、内部話者labelと表示名を返す。

### 27.2 閲覧、検索、統計

- segment listは固定行高の仮想スクロールとし、viewport周辺とoverscan分だけをDOMへ生成する。
  2時間級1,300 segmentを一括DOM展開しない。各行は開始／終了時刻、話者、本文を表示する。
- 話者色はdark／light双方のCSS変数8色から登場順に割り当てる。componentへ色値を直書きしない。
- ASRと話者分離それぞれのbackend、model、device、生成日時、音声長、segment数、話者数は
  transcript上部の強調領域へ常時表示する。
- 話者別の発話時間／割合と全segmentの平均turn長を表示する。word時刻／確度は情報過多と初期転送量を
  避けるためGUI viewer payloadへ複製せず、segmentごとのword数だけを返す。正本`merged.json`と
  `jobs show --json`には保持するため、将来の詳細表示は別の遅延読込APIとして追加できる。
- 全文検索は部分一致、case-insensitiveとし、一致箇所のhighlight、件数、前後移動を提供する。
  IMEの`compositionstart`から`compositionend`までは検索を実行せず、確定後も短いdebounceを置く。
- 話者複数選択と分単位の開始／終了範囲をsegmentに適用する。検索語は絞り込み後segmentを対象とする。

### 27.3 履歴、削除、再生成

- 履歴は状態filter（完了／失敗／破損）と、更新日時、file名、容量による並び替えを持つ。
  結果を開く、出力folderを開く、個別jobを削除する操作を提供する。
- 個別削除前に入力file名とjob directoryの合計byte数を表示して確認する。削除対象は検証済みの
  16桁job ID直下だけとし、生存processがlockを所有するjobは拒否する。job外の出力fileは削除しない。
- `jobs export`は対応`merged.json`を直接共通`PipelineResult`へ復元し、exporterだけを呼ぶ。
  指定形式、話者表示名、出力先を適用してexport stage recordだけを更新し、audio／ASR／
  diarization／mergeのstatusとconfig hashを変更しない。
- 話者表示名、直近の形式／出力先はGUI全体設定でなく、文字起こし本文と同じjob directoryの
  `presentation.json`（schema version 1）へ保存する。参加者名を別の利用者設定領域へ複製せず、
  job削除時に同時削除できるためである。

### 27.4 privacyと将来拡張

- transcript本文と検索語をGUI log、`settings.json`、追加cache、Web Storage、IndexedDBへ保存しない。
  API応答には`Cache-Control: no-store`を付け、画面を離れる際に検索語とviewerのin-memory状態を破棄する。
- エラーはschema、path、状態などの診断だけを含み、segment本文を含めない。テストfixtureは手書きの
  合成JSON／共通modelだけを使用する。
- API payloadはsegmentとwordを分離して保持し、row rendererを独立させる。本文編集、音声同期、
  話者再割当を追加する場合も、履歴・検索・仮想化層を置き換えず拡張できる構造とする。

---

## 28. 初回セットアップウィザード（Phase 5c）

### 28.1 目的と初回起動判定

`setup.ps1`はuv未導入時に案内だけ表示して終了し、READMEのprofile表は用途がハードウェア名
主体で「話者分離がGPUで動くか」という最大の性能差を伝えていなかった。GUIに初回セットアップ
ウィザードを追加し、ハードウェア検出からモデル取得・実動作確認までを一本化する。

初回起動判定（`SetupWizardService.status()`）は次のいずれかを満たす場合に`first_run=true`
を返す。

- プロファイルvenv（`cpu`/`cuda`/`intel`/`vulkan`）が1つも存在しない
- GUI設定ファイル（`settings.json`）自体が存在しない
- `setup_wizard_started_at`があり、`setup_wizard_completed_at`がない（中断後の再開）

「設定完了の記録（`setup_wizard_completed_at`）がない」ことだけを単独の判定基準にはしない。
Phase 5c以前からGUIを使っている利用者はこのフィールドを持たない`settings.json`を既に持って
いるため、字面どおりに判定すると既存利用者全員に毎回ウィザードが表示され、既に使っている
利用者を煩わせない、という要件に反する。設定ファイル自体の有無、プロファイルvenvの有無、
明示的な開始／完了時刻を組み合わせる。profileが存在し、開始／完了fieldがどちらもない旧版環境は
自動表示しない。

判定はGUI設定画面の「ウィザードを開く」ボタンからいつでも手動で再実行できる。

### 28.2 ハードウェア検出と推奨profile（`utteran_gui/hardware.py`）

プロファイルvenvが1つも存在しない時点では、そのprofileの`utteran devices --json`は実行
できない。GUI環境（`.venvs/win-gui`）にはPyTorchやOpenVINOなどの推論依存を追加しないため、
以下はOS標準機能とstdlibだけで検出する。

- GPUベンダー: `powershell.exe`経由の`Get-CimInstance Win32_VideoController`（Pythonパッケージ
  を追加しない、`utteran/devices.py`が`nvidia-smi`／`vulkaninfo`をsubprocessで呼ぶのと同じ作法）。
- メモリ: `ctypes`の`GlobalMemoryStatusEx`（Windows専用）。
- ディスク空き容量: `shutil.disk_usage`。

検出結果から次の対応表で推奨profileと、選択可能な代替を算出する。**選べない構成
（対応するハードウェアがない構成）は候補に出さない**。非Windowsまたは検出失敗時は`cpu`
だけを提示し、`detection_confident=false`とする。

| 検出結果 | 推奨 | 文字起こし高速化 | 話者分離のGPU実行 |
|---|---|---|---|
| NVIDIA GPU | `cuda` | ○ | ○ |
| Intel GPU（Arc／内蔵GPU） | `intel` | ○ | ○（XPU） |
| AMD等（NVIDIA/Intel以外） | `vulkan` | ○ | ×（CPU） |
| GPUなし／不明 | `cpu` | × | × |

複数GPU搭載機（例: Intel内蔵GPU＋NVIDIA dGPU）では、`utteran/devices.py`の自動選択優先順位
（CUDA→Vulkan/OpenVINO→CPU）と同じ優先度でNVIDIAを優先する。

各profileの概算ディスク使用量は実機計測値を使用する（2026-08-17、Windows 11 / Intel Arc 140T機、
`setup.ps1 -List`実測）: `cpu`=1.0 GiB、`intel`=5.3 GiB、`vulkan`=1.1 GiB。`cuda`はNVIDIA GPU
不在のため未測定であり、gyan.dev ffmpegと同種の公開情報に基づく概算値「約2.4GB」を使用する。

### 28.3 uvの自動導入（`setup.ps1`の`Install-Uv`）

`setup.ps1`は既存の`Install-Ffmpeg`と同じ型でuvを自動導入する。

- 既にuvが導入済みなら何もしない。
- 未導入の場合、ダウンロード前に取得元・概算容量を表示し、`-Yes`未指定なら確認を求める。
- astral-sh/uvのGitHub最新release（`https://api.github.com/repos/astral-sh/uv/releases/latest`）
  から`uv-x86_64-pc-windows-msvc.zip`と、公開されている`<同名>.sha256`（`<hash> *<filename>`形式、
  ffmpegの`.sha256`と同じ検証手順）を取得し、SHA-256を検証してから展開する。公式インストール
  スクリプト（`irm .../install.ps1 | iex`）を未検証のまま実行する方式は、ffmpegに適用している
  「取得前確認・チェックサム必須」という自己の基準と一貫しないため採用しない。
- `uv.exe`／`uvx.exe`は既存の`$BinDir`（`%LOCALAPPDATA%\utteran\bin`、ffmpegと共有）へ配置し、
  現在プロセスのPATHへ前置、未登録ならユーザーPATHへ永続化する（管理者権限不要）。
- 失敗時はffmpegと同じ形でオフライン時の手動導入手順を案内し、既存の依存関係同期を安全に
  スキップする。
- Linuxでのuv自動導入は対象外（GUI／ウィザードはWindows限定のため）。

`setup.ps1`の既存トップレベル引数は変更しない。`Write-Step`関数へ任意の`-Stage <slug>`
パラメータを追加し、指定時は`##UTTERAN-WIZARD## stage=<slug>`という機械可読マーカー行を
人間向け出力に加えて出す。主要ステップ（`python_check`/`uv_install`/`venv_sync`/`ffmpeg`/
`env_setup`/`verify`）にこのマーカーを付与し、GUIが具体的な進捗ステージ名を文字列の曖昧
マッチではなく正確なプレフィックス一致でパースできるようにする。

profile検証の`utteran devices --json`はPyTorchとOpenVINOを同時に初期化するため、実行前に
「初回は時間がかかる」旨と`verify_devices` stageを出し、終了後に実測秒数を表示する。
JSONの`pytorch.xpu_available`とXPU device一覧は、従来別processで実行していた
`python -c "import torch; ..."`と同じ判定を含むため、Intel profileではJSON結果を再利用する。
人間向け`utteran devices`の2回目起動も行わず、同じJSONから要約を表示して重い初期化を1回にする。

### 28.4 ウィザードのジョブオーケストレーション（`utteran_gui/setup_wizard.py`）

`SetupWizardService`は`JobManager`（26章）と同型の1操作制限・event cursor・process tree
終了機構を持つ。共有プロセス管理（Popen kwargs構築、`kill_process_tree`）は
`utteran_gui/processes.py`へ切り出し、`JobManager`と`SetupWizardService`の両方が使用する。

ウィザードは入力フェーズと無人実行フェーズを明確に分ける。入力フェーズは
`welcome → profile → 話者分離 → Token（ON時）→ model → confirm`とし、venv構築を始める前に
全入力を終える。話者分離は既定ONの独立ステップで、OFFの場合に話者ラベルが得られないことと、
設定画面から後で有効化できることを表示する。confirmには実行内容、10～45分の所要時間目安、
profileとモデルを合わせた概算ダウンロード量、開始後は席を外せることを表示する。

無人実行フェーズは`venv → Token preflight → pyannote model → ASR model → smoke test`とする。
preflight後もgated file取得時に失敗する可能性を最短で顕在化させるため、話者分離ON時は34MiB級の
pyannoteを1.6GiB級のASRより先に取得する。ウィザードは3種類のsubprocess jobを使用する。

1. **venv構築**（`venv_build`）: `powershell.exe -File setup.ps1 -Profile <p> -Yes`を
   shellを介さない引数配列で起動し、標準出力・標準エラーを行単位で読み、`##UTTERAN-WIZARD##`
   マーカー行はステージ切替イベント、それ以外はログ行としてSSEへ流す。一定時間イベントが
   なければ既存の`stalled`判定（26章と同じ仕組み）で「応答待ち」と表示する。
2. **モデル取得**（`model_download`）: 選択したprofileの`utteran.exe models download <ref>`を
   起動する。既定は`large-v3-turbo`。話者分離を使う場合はpyannote community-1
   （`pyannote:pyannote/speaker-diarization-community-1`）をASRより先に取得する。
3. **実動作確認**（`smoke_test`）: `wave`標準ライブラリで生成した無音の合成WAV
   （実データは使用しない）に対し、直前に取得した実モデル参照（`--asr-backend`/`--asr-model`）
   を明示して`transcribe`を実行し、終了code 0と出力file存在を確認する。**auto選択に任せると、
   実機検証でprofileの自動選択（例: `intel`のwhisper-cpp/vulkan）と実際に導入済みのモデル
   （faster-whisper）が食い違い失敗する実例を確認した**ため、smoke testは常に直前の
   モデル取得ステップで実際に取得した`model_ref`を明示指定する。

venv、preflight、pyannote model、ASR model、smoke testが完了するたびに完了段階を原子的に
永続化する。再起動後は未完了段階から自動再開し、model cacheと既存venvを再利用する。
`profile=null`の状態で`diarization`以降を永続化してはならない。旧版や中断競合によりこの不変条件を
満たさない状態を読み込んだ場合は、完了段階を実行に使わず、OSハードウェアを再検出して従来の
CPU/GPUおすすめprofile選択画面へ戻す。profileを変更した場合も、旧profileの完了段階は破棄する。
smoke testがexit code 0で終了した時点で`setup_wizard_completed_at`を原子的に永続化する。
`POST /api/wizard/complete`は永続化済みの値を返す冪等APIとし、未完了かつ現在processで成功した
`smoke_test`もない場合だけ409を返す。

### 28.5 API

既存の`/api/jobs/*`と同じ形で追加する（26.2〜26.4章の契約を流用し、新しいSSE形式は
定義しない）。

| method / path | 内容 |
|---|---|
| `GET /api/wizard/status` | 初回／再開判定、入力値、現在step、完了済み実行段階、Token error分類 |
| `POST /api/wizard/start` | `setup_wizard_started_at`を永続化 |
| `PUT /api/wizard/state` | 非機密の入力値と現在stepを原子的に永続化 |
| `GET /api/wizard/hardware` | ハードウェア検出＋推奨profile（venv不要） |
| `POST /api/wizard/token-preflight` | 選択profile CLIによるToken解決・gated modelアクセス確認 |
| `POST /api/wizard/jobs` | `{kind, profile, model_ref?, diarization_enabled?}` → 202 |
| `GET /api/wizard/jobs/{id}` | スナップショット |
| `POST /api/wizard/jobs/{id}/cancel` | 202 |
| `GET /api/wizard/jobs/{id}/events` | SSE（`/api/jobs/{id}/events`と同型） |
| `POST /api/wizard/complete` | 完了マーク（smoke_test未成功時409） |

Token保存には既存の`/api/token`・`/api/settings`をそのまま流用する。
HFトークンは既存`TokenStore`（OS keyring、service `utteran`／user `huggingface`）へのみ保存する。
ウィザードと設定画面は同じ保存関数とAPIを使う。ウィザードはアカウント作成、モデル利用条件への
同意、read token発行へのリンク、マスク入力、保存後の再取得確認、「話者分離なしで進める」を持つ。
無効token（`token`）と利用条件未同意（`license`）はモデル取得／smoke testの既存CLIエラー分類で
区別する。

Token画面は話者分離ONの場合だけ入力フェーズに表示する。keyring保存と再取得まではvenv不要のGUIで
確認し、既に設定済みなら再入力を求めない。keyringが使えない場合は`HF_TOKEN`／`.env`を案内する。
「後で行う」は話者分離OFFへ変更してmodel選択へ進む。venv構築直後、
`utteran config token-status --json --check-model <ref>`を自動実行し、実効Token
（`HF_TOKEN` > `.env` > keyring）とgated model fileへのアクセスを確認できるまで取得へ進めない。
応答は`configured`、`source`、`keyring_available`、`access`だけとし、Token本文を含めない。
preflightは`whoami`でToken自体を検証した後、pyannoteの`config.yaml`を`dry_run=True`でHEADする。
repository metadataだけではgated fileへの権限を保証できず、無効Tokenでも取得可能なため使用しない。
失敗時は`token_missing`／`token_invalid`／`agreement_required`／`network_error`を永続化してToken画面へ
戻し、修正後はvenv構築をやり直さず実行フェーズを再開する。モデル取得／smoke testで
`guide_token`／`guide_license`となった場合も汎用error画面ではなく同じToken画面へ戻す。

### 28.6 エラー分類の拡張

26.4章の`guidance_for`が持つ復旧案内カテゴリに`license`を追加する。`ModelAgreementError`
（pyannote modelの利用条件未同意、HTTP 403／`GatedRepoError`）と、既存の`token`カテゴリが
指す`HuggingFaceAuthenticationError`（トークン自体が無効、HTTP 401）は、Phase 1で既に別の
例外クラスとして区別されていたが、GUI側の`guidance_for`はどちらも`token`という1カテゴリに
畳んでいた。ウィザードは「トークンは有効だが利用条件に同意していない」場合と「トークンが
無効」の場合を利用者に区別して案内する必要があるため、`guidance_for`へ`license`カテゴリを
追加した（この修正は主な文字起こしフローの案内表示にも適用される）。

### 28.7 設定の永続化

`GuiSettings`（26.5章）に`setup_wizard_started_at: str | None`と
`setup_wizard_completed_at: str | None`、現在step、選択profile、話者分離ON/OFF、model参照、
完了済み実行段階、Token error分類を追加する。Token本文は保存しない。ウィザード開始時に開始時刻を、
smoke test成功時に完了時刻をUTC ISO 8601で設定する。
既存フィールドと同じくGUI専用の`settings.json`へ原子的に保存し、CLIの`config.toml`とは
分離する。

### 28.8 対象外

- インストーラー化（Phase 5d）。
- `setup.ps1`の既存トップレベル引数の変更（`Write-Step`への任意パラメータ追加のみ）。
- Linuxでのuv自動導入。
- `gui`profile自体のウィザード対応（ウィザード実行時点でGUIは既に動作しているため対象外）。
- 実行中subprocessをGUI再起動後に再attachすること（現在段階は冪等に再実行し、完了済み段階と
  model cacheは再利用する）。

---

## 29. Windowsインストーラー（Phase 5d）

### 29.1 目的と対象外

Python や uv を知らない利用者が、インストーラーを実行するだけで utteran を使い始められる
状態にする。推論コア（`utteran`パッケージ、torch等）とプロファイルvenvはインストーラーに
含めない（数百MB〜数GB規模になり配布物として非現実的なため）。署名は行わない（29.6章）。
CIでのインストーラービルドは対象外（29.9章）。

### 29.2 配布物の構成と配置レイアウト

インストーラーは次の3層で構成する。

1. **GUI本体**: `packaging/gui.spec`でPyInstaller onedirビルドした`utteran-gui.exe`と
   その支援ファイル（`_internal/`）。`src/utteran_gui`とFastAPI／uvicorn／pywebviewだけを
   含み、`utteran`（推論コア）・torch・faster-whisper・pyannote・ctranslate2は
   ビルド時点で二重に検査して混入を防ぐ（29.3章）。
2. **プロファイルvenv構築に必要な最小限のソース**: `pyproject.toml`、`uv.lock`、
   `src/utteran/`、`src/utteran_gui/`（ソース）、`setup.ps1`、`run.ps1`。プロファイルvenvは
   ローカルpackageとして`uv sync --extra <profile>`でビルドされるため、事前ビルド済み
   wheelが存在しない現状の運用では、実行時に`setup.ps1`がこれらのソースを必要とする。
   ソース自体は1.5MB程度で、この構成でも「推論コアを含めない」という要件と両立する
   （含めないのは依存関係が解決した実行環境であり、ソースコードそのものではない）。
3. **付随文書**: `README.md`、`README.en.md`、`LICENSE`、`THIRD_PARTY_NOTICES.md`、
   `.env.example`。

インストール先ディレクトリ（`{app}`）には、PyInstallerビルドの中身（`utteran-gui.exe`と
`_internal/`）をそのままトップレベルへ展開し、`pyproject.toml`等もその直下へ並べる
（サブフォルダへ分離しない、フラットな構成）。理由は次の29.5章のとおり、
`utteran_gui.app.project_root()`のcwd判定と両立させ、GUI起動を発見するpathを
1つに統一するため。

### 29.3 PyInstaller: onedir採用と推論コア混入防止

**onefileではなくonedirを採用した。** onefileは起動のたびにペイロードを一時ディレクトリへ
展開するため、毎回数秒の起動遅延が発生する。onedirはインストール時に1度その展開コストを
払うだけで、以降の起動は約1秒に収まる。繰り返し起動するデスクトップアプリでは、
「エクスプローラーにファイルが並ぶ」ことより起動速度を優先した。

推論コア（`utteran`パッケージ、torch、faster-whisper、pyannote、ctranslate2）を
誤って含めないよう、二重の検査を設けた。

- `packaging/gui.spec`のビルド時点で、`Analysis`が収集した純粋Pythonモジュール一覧
  （`a.pure`）を走査し、禁止モジュール名が1つでも見つかればビルド自体を`SystemExit`で
  中断する。
- `build.ps1`が、PyInstaller完了後の実際の出力ディレクトリを走査し、禁止モジュール名の
  ディレクトリが存在しないことを確認する（spec側の検査が依存解析の結果を見るのに対し、
  こちらは実際にディスクへ書き出された内容を見る、独立した検査）。

Phase 5aで既に存在する「`utteran_gui`が`utteran`をimportしない」というソースコードレベルの
AST回帰試験（`tests/test_gui.py`）に対し、この2つはビルド成果物レベルの保証を追加する。

keyringは`keyring.backends` entry pointsからWindows backendを動的に選ぶため、GUI specは
`collect_submodules("keyring")`と`copy_metadata("keyring")`を明示する。PyInstaller 6.22.2の
標準hookも同じ収集を行うことを旧配布物のarchiveで確認したが、hook実装への暗黙依存を避け、
specの必須条件として回帰試験する。配布版は`--diagnose-keyring <json-path>`で、実token slotを
変更せず診断専用usernameに対するimport/backend/get/set/delete結果と例外型・メッセージを出せる。
token値は診断結果へ含めない。

#### WebView2ランタイム依存について（判断と理由）

pywebviewのWindows既定backend（edgechromium）はMicrosoft Edge WebView2ランタイムに依存する。
Windows 11は標準搭載だが、Windows 10では未導入の場合がある。検出・案内の要否を検討し、
**インストーラー側でのレジストリ検出やインストールブロックは実装しない**と判断した。理由:

- Windows 10でもEdgeが既定ブラウザとして存在する構成（2022年以降のWindows Update経由）では
  WebView2ランタイムを既に満たしており、未導入なのはごく限られた構成（Edgeを手動削除した
  環境等）に限られる。
- レジストリベースの検出をInno Setupへ追加することは可能だが、対象が限定的な構成である
  一方でスクリプトの複雑度が増し、誤検出（検出ロジック自体の不具合）のリスクを新たに
  背負うことになる。
- pywebviewはWebView2が利用できない場合、例外を送出してGUIが起動しないという明確な失敗
  モードになる（無言でハングしたり誤動作したりはしない）ため、案内さえあれば利用者が
  自力で解決できる。

そのため、**文書による案内のみ**で対応する。インストール先選択直後の追加ダウンロード告知
page（29.4章）にWebView2ランタイムへの言及と、未導入の場合の公式入手先
（<https://developer.microsoft.com/microsoft-edge/webview2/>）を追記した。
GUI起動失敗時にPythonコード側で個別のエラーメッセージへ変換することは、Phase 5dの
「推論／GUIコードを変更しない」という設計方針（29.5章）を優先し、見送った
（起動失敗時に表示される既定の例外情報からでも、利用者がREADMEの当該記述へ辿り着ける
限りは対応可能と判断した）。

アイコンは用意していない（既定のPyInstaller/Inno Setupアイコンを使用）。将来デザイン資産が
用意できた場合に追加する。

### 29.4 Inno Setup: 管理者権限不要のユーザー単位インストール

- `PrivilegesRequired=lowest`、`DefaultDirName={localappdata}\Programs\utteran`とし、
  管理者権限を要求しない。VSCodeのユーザーインストーラー等と同じ、ユーザー単位の
  `%LocalAppData%\Programs\<name>`規約に倣った（Inno Setup固有の`{userpf}`的な省略記法に
  依存せず、`{localappdata}`という曖昧さのない定数を直接使用）。
- 日本語・英語に対応（`compiler:Languages\Japanese.isl`、`compiler:Default.isl`）。
- スタートメニュー項目は必須、デスクトップアイコンは`[Tasks]`で任意選択（既定オフ）。
- ライセンスページ（MIT、`LICENSE`をそのまま表示）の直後に、依存ライブラリ・モデルの
  ライセンスがutteran自体とは異なる旨（ffmpegはGPLv3、pyannoteモデルは別途同意が必要）を
  示す情報ページを追加。さらにインストール先選択の直後に、初回起動時に追加ダウンロードが
  発生すること（uv、プロファイル環境、モデルで合計数GB規模）を示す情報ページを追加した。
  どちらも`CreateOutputMsgPage`による静的な告知ページで、ユーザー入力は求めない。

### 29.5 GUI起動時のプロジェクトルート解決（コード変更なし）

`utteran_gui.app.project_root()`は、`UTTERAN_PROJECT_ROOT`環境変数が無ければ
`Path.cwd()`に`pyproject.toml`があるかを見る（無ければ`Path(__file__).resolve().parents[2]`
にfallbackするが、これはPyInstallerでフリーズしたビルドでは無意味なpathになる）。

29.2章のとおり`pyproject.toml`を`utteran-gui.exe`と同じ`{app}`直下に配置し、
スタートメニュー／デスクトップショートカットの`WorkingDir`を`{app}`に設定することで、
ショートカット経由の起動、エクスプローラーでの`utteran-gui.exe`直接ダブルクリックの
いずれでもcwdが`{app}`になり、`Path.cwd()/pyproject.toml`判定が成立する。
**この設計により、Phase 5dはGUI／推論コアのPythonコードを一切変更していない。**
`UTTERAN_PROJECT_ROOT`を明示設定する経路は温存されているため、開発時の挙動にも影響しない。

### 29.6 署名について（判断と理由）

自己署名証明書の導入を検討したが、**採用しなかった**。SmartScreenの評価は署名の有無だけでなく
発行者の配布実績に基づくため、自己署名では実績が蓄積せず、警告回避効果を得られない。
利用者が信頼済みルートへ自己手動登録した場合にのみ発行者名は変わるが、その手間自体が
警告と同程度に利用者を止める。

そのため今回は**署名を実行せず**、次の対策で代替する。

- README／`docs/リリース手順.md`へ、SmartScreen警告の内容と回避手順（「詳細情報」→
  「実行」）、警告が出る理由（署名していないため）、警告を無視することの意味を正直に記載する。
- リリースごとにSHA-256を算出し、GitHub Releasesへ公開する。利用者がPowerShellの
  `Get-FileHash`で検証できる手順もREADMEへ記載する。
- 将来Azure Trusted Signing等のCAベース署名を採用する場合に備え、`packaging/installer.iss`へ
  `#ifdef SignInstaller`で囲んだ`SignTool`/`SignedUninstaller`宣言を用意し、
  `build.ps1 -SignCommand <signtool呼び出し>`を渡すと`/DSignInstaller=1 /Sutteran=<command>`が
  ISCCへ渡る構造にした。署名しないビルドでは`SignTool`自体が定義されないため、
  署名ツール未登録でもISCCのcompileが失敗しない。

### 29.7 アンインストール

#### 削除対象の分類

| 対象 | 場所 | 既定 |
|---|---|---|
| アプリ本体（`utteran-gui.exe`、`_internal/`、ソース等） | `{app}` | 常に削除 |
| プロファイル実行環境 | `{app}\.venvs` | 常に削除 |
| ダウンロード済みモデル／OpenVINO IR | `{localappdata}\utteran\utteran\Cache\models` | 常に削除 |
| OpenVINO GenAI compiled cache | `{localappdata}\utteran\utteran\Cache\openvino-genai-compiled` | 常に削除（旧runtime blobを含む） |
| device probe cache | `{localappdata}\utteran\utteran\Cache\device-probes-v1.json` | 常に削除 |
| native build | `{userprofile}\.utteran\native` | 常に削除 |
| ffmpeg | `{localappdata}\utteran\bin`内の`ffmpeg.exe`/`ffprobe.exe`のみ | 常に削除 |
| ジョブ履歴・文字起こし結果 | `{localappdata}\utteran\utteran\Cache\jobs` | 選択（既定オフ、サイズ表示、**本文を含む旨明示**） |
| GUI設定 | `{localappdata}\utteran-gui\utteran-gui` | 選択（既定オフ） |
| CLI設定／memory calibration | `{localappdata}\utteran\utteran`内の`config.toml`／`memory-calibration.json` | 選択（既定オフ） |
| log／diagnostics／benchmark | `{localappdata}\utteran\utteran\Logs`（旧版は`{app}\logs`もあり得る） | 選択（既定オフ、本文を含み得る旨明示） |
| Hugging Face token | OS keyringのservice `utteran`、user `huggingface` | 選択（既定オフ）。同梱GUIの専用非対話commandで削除 |
| 利用者指定の出力 | installer管理外 | 常に保持し、走査もしない |
| uv | 同上ディレクトリ内の`uv.exe`/`uvx.exe` | **削除しない（選択肢に出さない）** |

再取得・再構築できるruntime/cacheは製品の残骸なので、対話／silentを問わず削除する。利用者が作った
結果、設定、認証情報、本文を含み得るlogは同意なしに削除しない。silent uninstallは同意を取得できない
ため後者を全て保持する。配布GUIとそこから起動するprofile CLIの既定log先は0.1.22からuser log領域へ
置き、clean install後に`{app}\logs`が生成されてinstall directoryだけ残ることを防ぐ。明示log設定は尊重する。

#### ffmpegとuvが同じディレクトリを共有する問題（判断と理由）

`setup.ps1`はffmpegとuvの両方を`%LocalAppData%\utteran\bin`という同一ディレクトリへ
展開する。「uvは削除しない」という要件と「ffmpegは削除する」という要件を両立するには、
ディレクトリ単位の削除ではなく、`ffmpeg.exe`／`ffprobe.exe`という**ファイル単位**の削除が
必要になる。アンインストーラーはこの2ファイルだけを`DeleteFile`し、ディレクトリ自体や
`uv.exe`／`uvx.exe`には触れない。

#### アンインストール時UIの実装方法（判断と理由）

当初、単一のチェックリスト画面（`CreateInputOptionPage`が返す`TInputOptionWizardPage`を
`.ShowModal`で単独表示）を計画したが、実際にInno Setup 6.7.3でcompileすると
`Unknown identifier 'SHOWMODAL'`でcompile自体が失敗した。この型はインストーラーの
自動ページ遷移に組み込まれることを前提としており、アンインストール側にはその遷移機構が
存在しないため、単独表示用のメソッドを持たない。

確実に動作するAPI（`MsgBox`によるYes/No確認）を使い、利用者データ4項目を順に確認する。
1画面のチェックリストに比べてクリック数は増えるが、実際にInno Setup 6.7.3でcompileと
実行を確認できる、より低リスクな実装を優先した。

**サイレントアンインストール（`/VERYSILENT`等）時は、5項目の確認を一切行わずすべて
「削除しない」とする。** スクリプトから無人実行されるアンインストールが、対話確認なしに
数GB規模のプロファイル環境や文字起こし結果を消してしまうことを防ぐための安全側デフォルト。
`UninstallSilent()`で判定する。

選択されなかった対象は、アンインストール完了後に`MsgBox`でその場所を一覧表示する。

### 29.8 ビルドの自動化（`build.ps1`）

`build.ps1`を新設し、1コマンドで次を実行する。

1. ビルド専用venv（`.venvs\win-gui-build`、`gui`＋`build`extra）への同期。
   `pyinstaller`を通常の`gui`venv（`gui.ps1`が開発時に使うもの）へ混ぜず、専用venvへ
   分離した。開発時のGUI実行環境をビルドツールで肥大化させないため。
2. `packaging/gui.spec`でのPyInstallerビルド（onedir）。
3. 推論コア非混入の実ファイル走査（29.3章）。
4. `pyproject.toml`から読んだversionを`/DMyAppVersion=`でInno Setupへ渡し、
   Inno Setupでのcompile。**versionの単一の情報源は`pyproject.toml`とし、
   インストーラー名・exe内バージョン情報・releaseタグが個別にずれないようにした。**
5. 生成された`.exe`のSHA-256を算出し、`.sha256`sidecarとして書き出す。

Inno Setup（`ISCC.exe`）が見つからない場合は、`uv sync`やPyInstallerを一切実行せずに
即座にエラーメッセージ（winget導入コマンドと公式ダウンロードURL）を表示して終了する。
未導入環境での実行コスト（数分かかるsyncやビルド）を払わせないための設計。

#### 自動テストを追加しなかった理由（判断と理由）

指示書はversion取得の一貫性、アンインストール削除対象判定、削除対象サイズ算出、
ビルドscript引数解釈について「自動テストで書けるものは積極的に書く」ことを求めていた。
これらの実装は`build.ps1`（PowerShell）と`packaging/installer.iss`（Pascal Script）に
存在し、いずれも本repositoryに既存のtest基盤（pytestはPythonのみ対象）を持たない言語である。
Pester（PowerShell用test framework）を本phaseのためだけに新規導入することは、既存の
`.ps1`群がPester無しで`tools/check_powershell_bom.py`の静的BOM検査と
`[System.Management.Automation.Language.Parser]`による静的構文検査（CI `windows-tests`）
＋実行確認だけで品質保証されてきた既存方針から逸脱し、1つのscriptのためだけに
新しい依存・規約を持ち込むことになる。ロジックをPython側へ切り出して単体testを書く
という選択肢も検討したが、実際の`build.ps1`/`installer.iss`から呼ばれない別実装を
testするだけになり、かえって偽の安心感を生むと判断した。

代わりに、実際のツール（uv、PyInstaller、Inno Setup 6.7.3）に対して本物の入力で
実行し、実際の出力を検証する方針を取った（AISTATE.md Phase 5dの実機検証記録参照）。
この過程で、モックされた単体testでは決して検出できなかった3件の実バグ
（`SPECPATH`の誤解、Pascalコメントの`{app}`によるearly-close、存在しない`ShowModal`
メソッド）を発見・修正しており、実行ベースの検証がこの種のscriptに対しては
単体testより高い実効性を持つと判断した根拠になっている。

### 29.9 CIとの関係（判断と理由）

**インストーラービルドはCIに組み込まない。** 理由は次のとおり。

- Inno Setupは`windows-latest` runnerに既定で入っておらず、追加のインストールstepが必要。
- PyInstaller onedirビルド自体に数分かかり、既存CIの実行時間を押し上げる。
- インストーラーの実質的な品質保証（実際にインストール・アンインストールが動くか）は、
  実機での目視確認を必要とする部分が大きく、CIの自動判定に還元しにくい
  （`docs/Phase5d_インストーラー_手動確認手順書.md`参照）。

一方、`build.ps1`は他の`.ps1`と同じく`tools/check_powershell_bom.py`のBOM検査対象になり、
CIの`windows-tests`ジョブが行う「全`.ps1`をWindows PowerShell 5.1でParseする」静的構文検査の
対象にも自動的に含まれる（`git ls-files '*.ps1'`で列挙するため）。Inno Setup未導入環境
（CI含む）で`build.ps1`を引数なしで実行すると、上記29.8章の「即座にエラー終了」する経路を
通るため、構文検査の対象にはなるが実際のビルドやsyncは一切発生しない。

### 29.10 未検証の項目

- 署名済みビルド（`-SignCommand`経路自体）は、署名証明書を持たないため未検証。
- 実際のSmartScreen警告表示・「詳細情報」からの実行許可は、未署名releaseを実際に
  ダウンロード実行してのみ確認できるため、開発機でのローカルビルド実行では再現条件が異なり
  未検証（Windows Defender SmartScreenはファイルの出所＝ダウンロード履歴のZone
  Identifierに基づいて警告するため、ローカルビルドしたexeをそのまま実行しても警告は出ない）。
- アンインストール時の`MsgBox`によるYes/No確認chain、およびインストール時の情報ページ
  （`CreateOutputMsgPage`）は、実際にダイアログをクリックする対話操作を伴うため、
  自動化ツールからは実行できず目視確認が必要（`docs/Phase5d_インストーラー_手動確認手順書.md`
  へ委譲）。
- uv未導入環境からの`Install-Uv`実PATH書き込み分岐は、Phase 5c・Phase 5d事前準備から
  引き続き実機未検証（既存環境を壊さずに検証する安全な方法が確立できていない）。

## 30. GUI性能構成・モデル管理・ネイティブ統合（Phase 5h）

### 30.1 ASR構成の既定

- GUIはbackend/deviceの独自既定を持たず、active profileの`devices --json`に含まれる
  `auto_selection`を既定とする。
- auto backendに対応する導入済みmodelとrunnable deviceがある場合、その組み合わせを選択する。
  whisper.cppでは推奨量子化modelを優先する。
- native buildまたはGGML modelが不足しauto構成を選べない場合、CPUへ無言で退避せず、モデル管理と
  `native status`／`native build`の具体的手順を画面に表示する。暗黙downloadは行わない。
- 利用者が明示変更するため、検出済みのwhisper.cpp `cpu`／`openvino`／`vulkan`／
  `openvino_vulkan`をGUI選択肢へ含める。

### 30.2 モデル管理

- active profileのCLI JSON/text契約だけを使用し、GUI processは推論coreをimportしない。
- 推奨カタログと`models list --available --all`相当の全カタログを切り替え、backend、format、用途、
  gated、概算size、導入済み実size、保存先を表示する。
- download、remove、verify、prepare-openvino、remove-openvinoを明示操作で実行する。download前は
  概算sizeと回線依存の所要時間、remove前は実size、IR生成前は追加PyTorch重み最大約3GBを表示して
  確認を求める。
- downloadとIR生成を含む操作は追跡可能な子processとし、進捗log、cancel、process tree終了を提供する。
  Token未設定、無効、利用条件未同意は既存CLI例外分類を変更せずGUI案内へ渡す。Token本文は返さない。
- native buildはVulkan SDK等の前提と長時間のbuild診断があるためGUIから自動実行せず、状態とCLI手順を
  案内する。

### 30.3 ウィザード、path選択、privacy

- ウィザードのASR model stepは実際のselectとし、Intel/Vulkan profileではGGML、CPU/CUDA profileでは
  CTranslate2 catalogを提示する。推奨modelを既定にし、選択値をdownloadとsmoke testへ渡す。
- 入力file、入力folder、出力folderはpywebview native dialogから選択できる。jobは単一input rootを
  受け、folder batchが複数fileを扱うためfile dialogのmultiple selectionは採用しない。
- native dialogはPython側の専用APIだけが呼び出し、JavaScriptへpywebview Windowやその`native`を
  公開しない。pywebviewの`js_api`へ渡すobjectは公開methodだけを持ち、Window等の内部object参照は
  再帰列挙されないprivate属性へ隔離する。JavaScript／Pythonのいずれからも`window.native`以下へ
  到達する実装を禁止し、静的testで公開memberと禁止文字列を検査する。
- path手入力を維持する。WebView標準のdrag-and-dropは絶対pathを取得できず、`File.name`だけでは
  実行不能になるため無効とし、利用可能とは案内しない。選択path、入力filename、検索語は
  settings/historyへ保存しない。
- JavaScriptの`error`と`unhandledrejection`は、最大20件を一時queueし、明示公開した診断APIから
  Pythonの構造化logへ転送する。1項目は2000文字に制限し、転送・log失敗はUIへ伝播させない。

### 30.4 Windows表示とレイアウト

- GUI子processは`CREATE_NO_WINDOW`を使用する。cancel対象は`CREATE_NEW_PROCESS_GROUP`もbit ORで維持し、
  `taskkill /T /F`でtreeを終了する。CLI単体のCtrl+C exit 130契約は変更しない。
- PyInstaller exe、Inno Setup、Start menu／desktop shortcut、GUI brandに`icon/`のutteran iconを使う。
- transcript仮想行はviewportのscrollbarを含むcontent幅内へ収め、左右marginによる横overflowを作らない。
  system/light/dark themeとresizeで同じ幅契約を維持する。

## 31. モデル体系・VAD・共通処理キュー（Phase 5i）

### 31.1 カタログ方針

- カタログへ掲載できるのは、現在のregistryから実行可能な`faster-whisper`、`whisper-cpp`、
  `pyannote`と、補助モデル用`whisper-cpp-vad`だけとする。将来予定または未実装backendは掲載しない。
- OpenVINO GenAI推論経路は未実装のため、`openvino:large-v3-turbo`を除去する。whisper.cppが使用する
  OpenVINO encoder IRは`whisper-cpp`の実行variantであり、独立ASR backend／モデルではない。
- faster-whisperは実在を確認したSystranのtiny／base／small／mediumと、従来のlarge系、
  日本語特化Kotoba-Whisperを提供する。GUIは推奨セットを既定表示し、明示操作で全件を表示する。
- 既に旧OpenVINO GenAIモデルを取得した利用者のデータは自動削除しない。モデル保存先の
  `openvino/large-v3-turbo`は実行に使われないため、容量を回収したい場合だけ手動削除する。

### 31.2 GPU表示と量子化

- GPU可否は`devices --json`と`native status --json`の検出結果からbackend別に算出する。
  CTranslate2は検出済みCUDA、whisper.cppはrunnableなVulkan／OpenVINO variant、pyannoteは
  PyTorch CUDA／XPUが存在する場合にGPU実行可と表示する。
- GGMLのf16／q5／q8はサイズ・速度・精度のトレードオフであり、GPU可否とは独立して表示する。
  推奨モデルには検出環境での推奨理由を併記する。

### 31.3 VAD仕様

- `[asr].vad_filter`はfaster-whisper内部のSilero VAD filter、`[asr.whisper_cpp].vad`は
  whisper.cpp CLIへ渡すSilero GGML VADであり、選択backendに応じて一方だけが作用する。
  両方の既定は`true`とし、どちらも設定から無効化できる。
- ウィザードはASRモデルとは別に`whisper-cpp-vad:silero-v6.2.0`を取得する。モデル管理からも
  download／verify／removeできる。
- whisper.cpp VADが既定ONでモデル未取得の旧環境では、警告のうえ当該実行だけVADを無効化する。
  明示した`vad_model` pathが不正な場合は設定誤りとしてエラーにする。
- VAD既定変更はASR `config_hash`を変更し、既存ジョブのASR以降を仕様どおり再計算する。

### 31.4 取得進捗と共通キュー

- Hugging Faceのdry-runメタデータとfile単位progress callbackから、取得済みbytes、total bytes、割合、
  転送速度、残り時間をJSONL進捗イベントとしてGUIへ渡す。完了／失敗時は一覧を自動更新する。
- 文字起こし、モデル取得／検証／削除、OpenVINO IR生成／削除、ウィザード処理はアプリ内の共通
  FIFOキューへ入れ、同時実行は1件とする。失敗後も次の待機項目を続行し、待機中／実行中を個別に
  キャンセルできる。並べ替えは本Phaseの必須要件外として実装しない。
- キューはGUI process内だけに保持し永続化しない。外部processの途中状態を安全に再開できないためで、
  再起動後は文字起こしの既存resumeとモデルdownloadの再試行を使用する。
- 文字起こし設定はキュー投入時に不変snapshotへ変換する。実行中のフォーム変更は現在の処理へ影響せず、
  次に追加する項目だけへ反映される旨を画面に表示する。

### 31.5 Windows shell identity

- PyInstaller／Inno Setup／shortcutのicon指定に加え、window作成前に
  `SetCurrentProcessExplicitAppUserModelID("Utteran.Utteran")`を呼び、pywebviewへ`.ico` pathを渡す。
  これによりshortcut、taskbar、title bar、Alt+Tabで同じアプリidentityとiconを使用する。
