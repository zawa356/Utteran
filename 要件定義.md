# utteran 設計書 / 要件定義

> このドキュメントは utteran リポジトリの `要件定義.md` として配置する。
> 仕様に変更が生じた場合、実装と同じ作業単位で必ず本書を更新すること。

---

## 1. 概要

### 1.1 目的

音声・動画ファイルから、話者を区別した文字起こしを生成するローカル実行ツール。
会議・インタビュー・講演の記録作成を主用途とする。

### 1.2 スコープ

**対象とするもの**

- ファイル入力によるバッチ処理（単一ファイル / フォルダ再帰 / キュー）
- 文字起こし（ASR）と話者分離（Speaker Diarization）の統合
- 複数の推論バックエンド（CPU / NVIDIA / Intel、将来 AMD）の切り替え
- CLI インターフェース

**対象としないもの（現時点）**

- リアルタイム（マイク入力）処理
- 文字起こし結果の編集エディタ
- クラウド API への送信（完全ローカル実行を前提とする）
- GUI（後続フェーズで追加可能な設計にとどめる）

### 1.3 想定音源

| 種別 | 話者数 | 想定長 |
|---|---|---|
| 会議・打ち合わせ | 2〜6人 | 30分〜3時間 |
| インタビュー・対談 | 2人 | 30分〜2時間 |
| 講演・セミナー | 1人＋質疑 | 1〜3時間 |

3時間を超える音声も処理可能であること。

### 1.4 プロジェクト情報

| 項目 | 値 |
|---|---|
| 名称 | utteran |
| 言語 | Python 3.11 / 3.12 |
| パッケージ管理 | uv（`pyproject.toml`） |
| ライセンス | MIT |
| 主対象 OS | Windows 10/11 |
| 副対象 OS | Linux（動作可能な実装とする） |

---

## 2. アーキテクチャ

### 2.1 設計原則

1. **コアはライブラリとして実装し、CLI はその薄いラッパーとする。**
   将来 GUI を追加する際に、コアへの変更を不要にするため。
2. **すべての長時間処理は進捗コールバックとキャンセルに対応する。**
   GUI のプログレスバーと中断ボタンに直結できるようにするため。
3. **設定は単一の Config オブジェクトに集約する。**
   GUI の設定画面がこれをバインドするだけで済むようにするため。
4. **バックエンドはプラグイン的なインターフェースで抽象化する。**
   モデル固有の構造を上位層に漏らさない。
5. **重い処理の結果は必ず中間ファイルに保存する。**
   再実行時に不要な再計算を発生させない。

### 2.2 ディレクトリ構成

```
utteran/
├── README.md                  # 必須ドキュメント
├── 変更履歴.md                # 必須ドキュメント
├── 要件定義.md                # 必須ドキュメント（本書）
├── AISTATE.md                 # 必須ドキュメント
├── LICENSE                    # MIT
├── THIRD_PARTY_NOTICES.md     # 依存ライブラリ・モデルのライセンス表記
├── pyproject.toml
├── setup.ps1                  # Windows 向けセットアップスクリプト
├── start.ps1                  # Windows 向け対話フロント
├── input/
│   └── .gitkeep               # 利用者入力は除外し、フォルダだけ維持
├── output/
│   └── .gitkeep               # 生成出力は除外し、フォルダだけ維持
├── .env.example
├── src/
│   └── utteran/
│       ├── __init__.py
│       ├── cli.py             # typer によるCLI定義
│       ├── config.py          # pydantic-settings による設定
│       ├── types.py           # 共通データモデル
│       ├── pipeline.py        # 全体オーケストレーション
│       ├── jobs.py            # ジョブ管理・レジューム
│       ├── audio.py           # ffmpeg 経由の音声抽出・正規化
│       ├── devices.py         # デバイス・バックエンド検出
│       ├── align.py           # ASR結果と話者区間の突き合わせ
│       ├── logging.py         # ログ設定（トークンマスク含む）
│       ├── errors.py          # 例外定義
│       ├── asr/
│       │   ├── base.py        # ASRBackend 抽象基底
│       │   ├── faster_whisper.py
│       │   ├── openvino.py    # Phase 3
│       │   └── registry.py
│       ├── diarization/
│       │   ├── base.py        # DiarizationBackend 抽象基底
│       │   ├── pyannote.py
│       │   ├── sherpa_onnx.py # Phase 3
│       │   └── registry.py
│       ├── exporters/
│       │   ├── base.py
│       │   ├── srt.py
│       │   ├── vtt.py
│       │   ├── json_exporter.py
│       │   ├── text.py
│       │   └── markdown.py
│       └── models/
│           ├── catalog.py     # モデル定義（ID、形式、サイズ、URL）
│           └── manager.py     # ダウンロード・検証・削除
├── tests/
└── docs/
```

### 2.3 処理パイプライン

```
入力ファイル（音声 / 動画）
  │
  ├─[1] 音声抽出・正規化 ─────→ audio.wav (16kHz / mono / PCM 16bit)
  │                              ffmpeg 使用
  ├─[2] ASR ────────────────→ asr.json
  │                              単語レベルタイムスタンプ付き
  ├─[3] 話者分離 ───────────→ diarization.json
  │                              話者区間 + exclusive 区間
  ├─[4] 突き合わせ・話者割当 → merged.json
  │
  └─[5] 出力生成 ───────────→ *.srt / *.vtt / *.json / *.txt / *.md
```

各ステージは独立して完了状態を持ち、レジューム時は未完了のステージから再開する。

---

## 3. データモデル

`src/utteran/types.py` に定義する。バックエンド固有の構造を上位層に漏らさないこと。

```python
@dataclass(frozen=True)
class Word:
    start: float          # 秒
    end: float            # 秒
    text: str
    probability: float | None = None

@dataclass
class Segment:
    start: float
    end: float
    text: str
    words: list[Word] = field(default_factory=list)
    speaker: str | None = None      # 話者割当後に設定される

@dataclass
class TranscriptionResult:
    segments: list[Segment]
    language: str
    duration: float
    backend: str                    # 例: "faster-whisper"
    model_id: str
    device: str

@dataclass(frozen=True)
class SpeakerTurn:
    start: float
    end: float
    speaker: str                    # 例: "SPEAKER_00"

@dataclass
class DiarizationResult:
    turns: list[SpeakerTurn]
    exclusive_turns: list[SpeakerTurn] | None   # 対応バックエンドのみ
    num_speakers: int
    backend: str
    model_id: str
    device: str
```

**重要**: `TranscriptionResult` は Whisper 固有の構造を含めない。将来 Whisper 以外の
ASR モデル（Voxtral、Qwen3-ASR 等）を追加できるようにするため。

---

## 4. バックエンド

### 4.1 対応方針

| 用途 | 実装 | 対象ハードウェア | 導入フェーズ |
|---|---|---|---|
| ASR | faster-whisper (CTranslate2) | CPU / NVIDIA (CUDA) | Phase 1 |
| ASR | OpenVINO GenAI | Intel CPU / GPU / NPU | 将来（未実装） |
| ASR | whisper.cpp | CPU / OpenVINO / Vulkan / OpenVINO+Vulkan | Phase 3 |
| 話者分離 | pyannote.audio 4.x | CPU / NVIDIA (CUDA) / Intel XPU | Phase 1、XPUはPhase 3 |
| 話者分離 | sherpa-onnx | CPU / CUDA / OpenVINO / DirectML | 将来（未実装） |

### 4.2 ASRBackend インターフェース

```python
class ASRBackend(ABC):
    name: ClassVar[str]

    @classmethod
    @abstractmethod
    def is_available(cls) -> bool:
        """必要な依存パッケージが導入済みか。import 失敗を握りつぶして False を返す。"""

    @classmethod
    @abstractmethod
    def available_devices(cls) -> list[DeviceInfo]:
        """このバックエンドで利用可能なデバイスの一覧。"""

    @abstractmethod
    def load(self, model_id: str, device: str, compute_type: str) -> None: ...

    @abstractmethod
    def transcribe(
        self,
        audio_path: Path,
        options: ASROptions,
        progress: ProgressCallback | None = None,
        cancel: CancelToken | None = None,
    ) -> TranscriptionResult: ...

    @abstractmethod
    def unload(self) -> None: ...
```

`ASROptions` には最低限、`language`、`initial_prompt`、`vad_filter`、`beam_size`、
`condition_on_previous_text`、`word_timestamps` を含める。

### 4.3 DiarizationBackend インターフェース

```python
class DiarizationBackend(ABC):
    name: ClassVar[str]

    @classmethod
    @abstractmethod
    def is_available(cls) -> bool: ...

    @classmethod
    @abstractmethod
    def available_devices(cls) -> list[DeviceInfo]: ...

    @abstractmethod
    def load(self, model_id: str, device: str) -> None: ...

    @abstractmethod
    def diarize(
        self,
        audio_path: Path,
        options: DiarizationOptions,
        progress: ProgressCallback | None = None,
        cancel: CancelToken | None = None,
    ) -> DiarizationResult: ...

    @abstractmethod
    def unload(self) -> None: ...
```

`DiarizationOptions` には `num_speakers`、`min_speakers`、`max_speakers` を含める。

### 4.4 pyannote バックエンドの仕様

- 既定モデル: `pyannote/speaker-diarization-community-1`
- Hugging Face のアクセストークンが必要。モデル利用条件への同意も必要。
- pyannote.audio 4.x の出力から `speaker_diarization` と `exclusive_speaker_diarization`
  の両方を取得し、後者を `DiarizationResult.exclusive_turns` に格納する。
- トークン未設定時は、取得手順を案内する明確なエラーメッセージを出す。
  スタックトレースをそのまま見せない。
- ローカルパス指定によるオフライン読み込みに対応する。

### 4.5 デバイス検出

`utteran devices` で、利用可能なバックエンドとデバイスを一覧表示する。

検出項目:
- CPU（コア数、命令セット）
- NVIDIA GPU（CUDA 有無、VRAM、cuDNN/cuBLAS の DLL 解決可否）
- Intel GPU / NPU（OpenVINO の利用可能デバイス）
- 各バックエンドの `is_available()` 結果

自動選択のロジック:
1. ASR: CUDA が使えれば faster-whisper + CUDA、Intel GPU/NPU があれば OpenVINO、
   いずれもなければ faster-whisper + CPU
2. 話者分離: CUDA が使えれば pyannote + CUDA、なければ pyannote + CPU
3. Intel 環境では「ASR は GPU/NPU、話者分離は CPU」という組み合わせになる点を
   警告として明示する（pyannote は Intel GPU で高速化できないため）

---

## 5. 設定

### 5.1 優先順位

```
CLI 引数  >  環境変数  >  .env  >  config.toml  >  既定値
```

Hugging Face トークンのみ、さらに OS キーリングを参照先に加える（優先度は .env の次）。

### 5.2 config.toml

配置場所は `platformdirs.user_config_dir("utteran")`。`utteran config init` で雛形を生成する。

```toml
[general]
output_dir = "./output"
job_dir = ""              # 空文字ならユーザーキャッシュ配下
log_level = "info"
venv_dir = ""             # 空文字ならリポジトリ直下 .venvs
native_dir = ""           # 空文字なら ~/.utteran/native
default_profile = ""      # cpu | cuda | intel | vulkan、空文字なら自動解決

[asr]
backend = "auto"          # auto | faster-whisper | openvino
model = "large-v3-turbo"
device = "auto"
compute_type = "auto"     # float16 | int8_float16 | int8 など
language = "ja"
vad_filter = true
condition_on_previous_text = false
beam_size = 5
initial_prompt = ""
word_timestamps = "auto"  # auto | always | never（whisper.cpp経路）

[asr.whisper_cpp]
variant = "auto"          # auto | cpu | openvino | vulkan | openvino_vulkan
dtw = "auto"              # auto またはwhisper.cppのDTW preset
threads = 0                # 0なら自動

[diarization]
enabled = true
backend = "pyannote"      # pyannote | sherpa-onnx
model = "pyannote/speaker-diarization-community-1"
device = "auto"
num_speakers = 0          # 0 なら自動推定
min_speakers = 0
max_speakers = 0

[output]
formats = ["srt", "json", "md"]
srt_bom = false           # Windows の字幕ソフト向けに UTF-8 BOM を付与
newline = "lf"            # lf | crlf
show_speaker = true       # 字幕・テキストへ話者名を表示
speaker_labels = {}       # 例: { SPEAKER_00 = "田中", SPEAKER_01 = "佐藤" }

[ffmpeg]
path = ""                 # 空文字なら PATH から探索し、なければ同梱先を参照

[alignment]
max_nearest_distance = 2.0
min_segment_duration = 0.3
min_segment_words = 2
merge_gap = 0.5
renumber_speakers = true
```

### 5.3 トークンの取り扱い

- `config.toml` にトークンを書かせない。書かれていても読み取らず、警告を出す。
- ログ出力時、トークンらしき文字列は常にマスクする（`hf_****`）。
- 例外メッセージやスタックトレースにトークンが混入しないようにする。
- 将来の GUI が利用できるよう、`TokenProvider` として抽象化する
  （環境変数 / .env / キーリングの各実装を持つ）。

---

## 6. ジョブ管理とレジューム

### 6.1 ジョブディレクトリ

```
<job_dir>/<job_id>/
├── manifest.json       # 入力情報、設定ハッシュ、各ステージの状態
├── audio.wav           # 抽出済み音声（16kHz mono）
├── asr.json            # ASR 結果
├── diarization.json    # 話者分離結果
├── merged.json         # 統合結果
└── utteran.log         # このジョブのログ
```

### 6.2 job_id の決定

```
job_id = sha256(入力ファイルのハッシュ)[:16]
```

入力ファイルのハッシュは、全体を読むと遅いため
「ファイルサイズ + 更新時刻 + 先頭 1MB + 末尾 1MB」から算出する。

設定は `job_id` に含めない。再実行の要否は、ステージごとに保存した
`config_hash` と現在の設定から算出した値を比較して判定する。同一入力に対するジョブは
1つだけ存在し、設定を変えて再実行した場合は影響のあるステージ以降のみを再計算する。

### 6.3 ステージ依存関係

設定変更時に再実行が必要なステージを明示する。

| ステージ | config_hash の算出対象 |
|---|---|
| audio | 入力ファイルハッシュ、サンプリングレート、チャンネル数、サンプル形式 |
| asr | audio の config_hash、`[asr]` セクション全体 |
| diarization | audio の config_hash、`[diarization]` セクション全体 |
| merge | asr / diarization の config_hash、`[alignment]` セクション全体 |
| export | merge の config_hash、`[output]` セクション全体、実効出力先ディレクトリ |

算出対象はキー順にソートした正規形 JSON とし、浮動小数は固定桁の文字列へ変換してから
SHA-256 を算出する。Python の `dict` の順序やロケールに依存してはならない。

出力形式や話者名だけを変更した再実行は、export のみが走ること。

### 6.4 manifest.json

```json
{
  "version": 1,
  "job_id": "a1b2c3d4e5f6a7b8",
  "input": { "path": "...", "size": 123456, "mtime": 1700000000, "hash": "..." },
  "created_at": "2026-01-01T00:00:00+09:00",
  "updated_at": "2026-01-01T00:00:00+09:00",
  "stages": {
    "audio":       { "status": "done", "config_hash": "...", "finished_at": "..." },
    "asr":         { "status": "done", "config_hash": "...", "finished_at": "..." },
    "diarization": { "status": "running", "config_hash": "...", "finished_at": null },
    "merge":       { "status": "pending", "config_hash": null, "finished_at": null },
    "export":      { "status": "pending", "config_hash": null, "finished_at": null }
  }
}
```

`status` は `pending | running | done | failed` の4値。
異常終了で `running` のまま残った場合、次回実行時は `pending` として扱う。

### 6.5 ジョブディレクトリのロック

同一ジョブに対する多重実行を防ぐため、ジョブディレクトリにロックファイルを置く。
ロックファイルには PID と開始時刻を記録する。既存ロックがある場合は当該プロセスの
生存を確認し、存在しなければ古いロックとして除去して取得する。プロセスが存在する場合は
一般エラー（終了コード 1）とし、`--force-unlock` で強制解除できるようにする。

---

## 7. 突き合わせ（align.py）

ASR の単語タイムスタンプと話者区間を突き合わせ、各セグメントに話者を割り当てる。

### 7.1 アルゴリズム

1. `exclusive_turns` が存在すればそちらを優先して使用する。
   存在しない場合は `turns` を使い、重なり区間は開始が早い話者を優先する。
2. 各単語について、単語の中心時刻を含む話者区間の話者を割り当てる。
3. どの区間にも含まれない場合、重なり時間が最大の区間を採用する。
   重なりがまったくない場合は、最も近い区間を採用する。
   距離が閾値（既定 2.0 秒）を超える場合は `UNKNOWN` とする。
4. セグメント内で話者が混在する場合、話者の切り替わり位置でセグメントを分割する。
5. 分割後、極端に短いセグメント（既定 0.3 秒未満、または 2 単語未満）は
   前後の同一話者セグメントに吸収する。相槌によるラベルの細分化を防ぐ。
6. 連続する同一話者のセグメントは、間隔が閾値（既定 0.5 秒）以下なら結合する。

### 7.2 パラメータ

上記の閾値はすべて設定で変更可能にする。既定値は上記のとおり。

### 7.3 話者ラベル

- 内部表現は `SPEAKER_00` 形式を維持する。
- 出力時に `[output].speaker_labels` によるリネームを適用する。
- 話者の登場順に `SPEAKER_00` から振り直すオプションを持つ
  （バックエンドが返す番号は登場順とは限らないため）。

---

## 8. 入出力

### 8.1 入力

- 音声: wav, mp3, m4a, flac, ogg, aac, wma
- 動画: mp4, mkv, mov, avi, webm, ts
- ffmpeg でデコードできるものはすべて受け付ける方針とし、上記は明示的にテストする対象。
- 複数チャンネルはモノラルにダウンミックスする。
- サンプリングレートは 16kHz に変換する。

### 8.2 出力

| 形式 | 拡張子 | 内容 |
|---|---|---|
| SRT | .srt | 字幕。話者名を行頭に付与するオプションあり |
| WebVTT | .vtt | 字幕 |
| JSON | .json | 単語レベルを含む全情報。再利用・二次加工用 |
| テキスト | .txt | 話者名付きの本文 |
| Markdown | .md | 話者名付き本文＋メタ情報（処理条件、日時、モデル） |

- 文字コードは UTF-8。SRT のみ BOM 付与オプションを持つ。
- 改行コードは LF（SRT/VTT は CRLF も選べるようにする）。
- 出力ファイル名は既定で `<入力ファイル名>.<拡張子>`。衝突時は連番を付与する。

### 8.3 JSON 出力のスキーマ

バージョンフィールドを持たせ、後方互換性を管理する。

```json
{
  "schema_version": 1,
  "input": { "path": "...", "duration": 3600.0 },
  "processing": {
    "asr": { "backend": "faster-whisper", "model": "large-v3-turbo", "device": "cuda" },
    "diarization": { "backend": "pyannote", "model": "...", "device": "cuda" },
    "created_at": "..."
  },
  "speakers": ["SPEAKER_00", "SPEAKER_01"],
  "segments": [
    {
      "start": 0.5, "end": 3.2, "speaker": "SPEAKER_00", "text": "...",
      "words": [ { "start": 0.5, "end": 0.8, "text": "...", "probability": 0.98 } ]
    }
  ]
}
```

---

## 9. CLI 仕様

```
utteran transcribe <入力パス> [オプション]
    --format srt,json,md          出力形式（カンマ区切り）
    --output-dir <パス>
    --asr-backend <名前>
    --asr-model <ID>
    --diarization-backend <名前>
    --diarization-model <IDまたはパス>
    --asr-device <名前>           ASR専用のデバイス／native構成
    --diarization-device <名前>   話者分離専用のデバイス
    --device <名前>               両方へ適用する従来互換指定
    --language <コード|auto>     `auto` は言語自動判定
    --num-speakers <N>
    --min-speakers <N>
    --max-speakers <N>
    --no-diarization
    --recursive                   ディレクトリを再帰的に処理
    --include <glob>              一括処理に含めるパターン（複数指定可）
    --exclude <glob>              一括処理から除外するパターン（複数指定可）
    --resume                      既存ジョブから再開（既定で有効）
    --force                       すべてのステージを再実行
    --force-unlock                既存のジョブロックを強制解除
    --dry-run                     処理内容の表示のみ
    --yes                         モデル取得などの確認を省略
    --config <パス>
    --verbose / --quiet

utteran devices [--json]          利用可能なバックエンドとデバイスを表示（JSON 出力対応）
utteran models list               モデルの一覧と導入状況
utteran models download [<ID>]    ID指定、または番号付き対話選択によるモデル取得
utteran models remove <ID>
utteran models verify             導入済みモデルの整合性検証
utteran models path               モデル保存先の表示
utteran jobs list                 ジョブの一覧
utteran jobs show <job_id>
utteran jobs clean [--all|--failed|--older-than <日数>]
utteran config init               設定ファイルの雛形生成
utteran config show               現在の有効な設定（トークンはマスク）
utteran config path
utteran eval <正解ディレクトリ>    DER / WER の算出（Phase 4）
```

終了コード:

| コード | 意味 |
|---|---|
| 0 | 正常終了 |
| 1 | 一般エラー |
| 2 | 設定エラー（トークン未設定、不正な設定値など） |
| 3 | 依存エラー（ffmpeg 未検出、バックエンド未導入など） |
| 4 | 入力エラー（ファイル未検出、デコード失敗など） |
| 5 | 一括処理で一部のファイルが失敗した（全滅ではない） |
| 130 | ユーザーによる中断 |

一括処理ですべての対象ファイルが失敗した場合は終了コード 1 とする。
バッチ中のデコード失敗は「失敗」として集計し、他ファイルの処理を継続する。完了済みジョブや
dry-run対象は「スキップ」とする。

### 9.1 Windows対話フロント

- リポジトリ直下に `start.ps1` を置き、PowerShellから番号メニューだけで主要操作へ到達できること。
- メインメニューから文字起こし、モデル管理、デバイス確認、ジョブ管理、設定管理、setup profile
  切替、入力／出力フォルダを開く操作を選べること。
- 文字起こしウィザードは、`input` 内の単一ファイル／フォルダ一括／任意パス、`output` または
  任意出力先、ASRモデル、話者分離、ASRと話者分離それぞれのデバイス、言語、話者数、出力形式、再帰・glob、resume／force、
  config、ログ詳細度を対話的に設定し、実行前に要約とコマンドを表示すること。
- `devices --json`と`models list --json`から現在のprofileで利用可能なbackend、device/native構成、
  導入済みモデルだけを動的表示する。空ならnative buildまたはモデル取得を案内する。
- 入力パスやモデルIDなどはコマンド文字列として評価せず、PowerShellの引数配列としてCLIへ渡す。
- `start.ps1` はPhase 3aのprofile別`.venvs/win-<profile>`を解決し、未構築時は`setup.ps1`を
  案内すること。
- ASRのnative構成（`openvino`／`vulkan`／`openvino_vulkan`）はpyannoteへ渡さない。
  `--asr-device`と`--diarization-device`を別々に生成し、後者はPyTorchで利用可能な
  `auto`／CPU／CUDA／XPUだけを`devices --json`から提示すること。
- リポジトリ直下に `input` と `output` を作り、それぞれ `.gitkeep` だけを追跡する。利用者が置く
  音声・動画と生成された文字起こしは拡張子を問わずGit対象外にする。

---

## 10. モデル管理

### 10.1 方針

- ユーザーが明示的に管理できること。暗黙のダウンロードで数GBを消費させない。
- 未取得のモデルが指定された場合、自動ダウンロードするか確認プロンプトを出す。
  `--yes` で省略可能。非対話環境では明確なエラーとする。
- `setup.ps1` はPython依存、ffmpeg、秘密設定補助、デバイス診断までを担当し、モデルの一覧・
  選択・取得は行わない。セットアップ完了後に専用の `utteran models` コマンドを案内する。
- `utteran models download` のIDを省略した対話実行では、表示名、用途、backend、概算サイズ、
  ライセンス、gated状態、技術IDを持つ番号付き一覧から1件以上を選択できること。
- 番号の代わりにカタログのモデルIDまたは `<backend>:<model-id>` も指定でき、日本語向けなど
  特殊用途のモデルを明示選択できること。非対話環境でIDを省略した場合は取得せず、一覧表示と
  明示IDの指定方法を案内する。
- 完全オフライン運用が可能なこと（事前取得のみで動作する）。
- モデル形式ごとの必須ファイルがすべて存在する場合だけ「導入済み」と判定すること。部分取得は
  「不完全」と表示し、`models verify` で欠落ファイルを列挙し、`models download` で既存ファイルを
  保持したまま取得を再開できること。
- pyannote community-1は `config.yaml` だけで正常と判定せず、embedding／segmentationの各
  `pytorch_model.bin` と、PLDAの `plda.npz`／`xvec_transform.npz` も必須とすること。
- Windowsのモデル保存先では、Hugging Faceが生成する一時ファイル名を含むパスが260文字以上でも
  取得できるよう、snapshot downloadへextended-length pathを渡すこと。利用者向け表示とモデル
  保存先の設定値は通常のパス表記を維持すること。
- CTranslate2形式モデルの取得完了時と`models verify`実行時、`config.json`の`alignment_heads`が
  実際の decoder 層数を超えて参照していないかを、隔離subprocessで単語タイムスタンプ計算を
  1回試行して検証すること。ネイティブクラッシュ（アクセス違反等、Pythonから捕捉不能な
  異常終了）を検知した場合のみ、安全な既定値（decoder layer 0 / head 0）へ自動修正すること。
  Python例外による検証不能時は誤って書き換えないこと。第三者配布の蒸留系モデル
  （decoder層数を削減したモデル）が、蒸留前の深いdecoder向け`alignment_heads`をそのまま
  配布している既知の不具合（`kotoba-whisper-v2.0`で確認）から利用者を保護するための対策。

### 10.2 モデルカタログ

`models/catalog.py` に、モデル ID・表示名・用途説明・バックエンド・形式・概算サイズ・取得元を
定義する。
同じ Whisper large-v3-turbo でも、バックエンドごとに必要な形式が異なる点に注意する。

| モデル ID | バックエンド | 形式 |
|---|---|---|
| large-v3-turbo | faster-whisper | CTranslate2 |
| large-v3-turbo | openvino | OpenVINO IR |
| large-v3 | faster-whisper | CTranslate2 |
| kotoba-whisper-v2.0 | faster-whisper | CTranslate2 |
| pyannote/speaker-diarization-community-1 | pyannote | pyannote pipeline |

pyannote community-1の現行スナップショットは約32 MiB（カタログ概算34 MiB）。約1 MiBの設定・
文書ファイルだけが存在する状態は部分取得として扱う。

保存先は `platformdirs.user_cache_dir("utteran") / "models"`。
環境変数 `UTTERAN_MODEL_DIR` で上書き可能。

---

## 11. ffmpeg

- 起動時に以下の順で探索する。
  1. 設定 `[ffmpeg].path`
  2. `PATH` 環境変数
  3. アプリケーションデータ配下の同梱先（`<user_data_dir>/bin/ffmpeg.exe`）
- いずれにも見つからない場合、`setup.ps1` での取得を案内するエラーを出す。
- リポジトリには ffmpeg のバイナリを含めない（ライセンス上の理由）。
- `setup.ps1` は公式配布物を取得し、上記の同梱先に配置する。

---

## 12. ログ・エラー処理

- 標準出力は人間向けの進捗表示（rich 等）、ログファイルは構造化ログとする。
- ジョブごとに `utteran.log` を残す。
- 正常完了時は、今回実行した各ステージの実測所要時間と実行ステージ合計を
  `HH:MM:SS.mmm`形式で最終結果の後に表示する。resumeで再利用したステージは今回の集計へ
  含めない。フォルダ一括処理では成功ジョブのステージ時間を同名ステージごとに合算する。
- 想定される失敗は専用の例外型を定義し、ユーザー向けの対処法を含むメッセージを出す。
  トークン未設定、モデル未取得、ffmpeg 未検出、VRAM 不足、対応外の入力形式など。
- 想定外の例外のみスタックトレースを表示し、`--verbose` で詳細を出す。
- 一括処理中に個別ファイルが失敗しても全体を止めない。最後に失敗一覧を表示する。

---

## 13. 長時間音声への対応

3時間超の音声で問題になるのは話者分離側である。ASR は 30 秒チャンクの逐次処理のため
長さ自体は問題にならない。

pyannote は音声全体を一括処理するため、長時間音声ではメモリ消費が大きい。
分割処理すると、チャンクごとに話者 ID が独立して振られ、同じ話者に別の ID が付く。

対応方針:

1. まず一括処理を試みる。
2. 事前に必要メモリを概算し、閾値を超える場合は警告する。
3. メモリ不足が発生した場合、分割モードにフォールバックする。
4. 分割モードでは、各チャンクの話者埋め込みを保持し、
   全チャンク分をまとめて最終クラスタリングを1回だけ実行する。
   これにより話者 ID の一貫性を保つ。

分割モードの実装は Phase 4 とする。Phase 1 では一括処理のみを実装し、
メモリ不足時は明確なエラーメッセージを出す。

---

## 14. 依存関係とインストールプロファイル

Phase 3aで、PyTorchのビルド種別を選ぶextrasと機能を追加するextrasを分離した
（15章参照）。

```
utteran                # コア + faster-whisper（torch 非依存）
utteran[cpu]           # pyannote.audio + CPU版torch
utteran[cuda]          # pyannote.audio + CUDA 12.6版torch
utteran[xpu]           # pyannote.audio + XPU版torch + triton-xpu（Windowsのみ）
utteran[whisper-cpp]   # cmake（whisper.cppネイティブビルド用）
utteran[openvino]      # openvino, openai-whisper, onnxscript
utteran[onnx]          # onnxruntime（sherpa-onnx話者分離はPhase 3以降）
utteran[dev]           # 開発用
```

- torch は話者分離を含む extras にのみ入れ、コアはtorch非依存を維持する。
- `cpu` / `cuda` / `xpu` は `[tool.uv] conflicts` に登録した排他的extrasで、同一venvに同時
  導入できない。`whisper-cpp` / `openvino` / `onnx` / `dev` はこれらと自由に組み合わせられる
  非排他extras。
- `cpu` はPyTorch公式CPU index、`cuda` はPascal世代を含む実機互換を確認したPyTorch CUDA 12.6
  index、`xpu` はPyTorch公式XPU indexを使用し、`uv.lock` でバージョンを固定する。
- `xpu` extraのtorchは`triton-xpu`パッケージに推移的に依存するが、uvの`explicit = true`
  indexは対象extraの`project.optional-dependencies`に直接列挙されたパッケージにしか
  適用されない。そのため`triton-xpu`を`xpu` extraへ`sys_platform == 'win32'`条件付きで
  直接列挙し、同じ`tool.uv.sources`ルーティングを与えている。Windows以外のtorch+xpu組合せは
  未検証（AISTATE.md I-4参照）。
- CTranslate2 の CUDA 実行に必要な cuDNN / cuBLAS は、CUDA版PyTorchが仮想環境内へ配置した
  DLLディレクトリも探索・登録対象にする。
- PyTorch CUDAの実可用性は、デバイス列挙やメモリ確保だけでなく、最小CUDAカーネルの実行、
  CPUへの結果転送、同期の成功で判定する。
- 旧`.venv` / `.venv-windows`単一環境モデルはPhase 3aでプロファイル別venv
  （`.venvs/<os>-<profile>`、15章）へ置き換えた。既存の`.venv-windows`によるWSL/Windows回避は
  OS識別子を含むディレクトリ名で恒久化された。
- 依存同期または選択profileのprobeが失敗したsetupは成功表示にせず、残りの依存処理を安全に
  スキップして終了コード1を返す。

---

## 15. ライセンス表記

コードは MIT だが、依存モデルのライセンスは異なる。`THIRD_PARTY_NOTICES.md` に記載する。

| 対象 | ライセンス | 注意点 |
|---|---|---|
| Whisper | MIT | |
| pyannote community-1 | CC-BY-4.0 | 利用条件への同意が必要 |
| sherpa-onnx | Apache-2.0 | モデルごとに個別のライセンスあり |
| ffmpeg | GPL/LGPL | 同梱しないため配布物には影響しない |

README にも、モデル利用条件への同意が必要である旨を明記する。

---

## 16. 開発規約

- 型ヒントを全関数に付与する。`mypy` で検査する。
- `ruff` で lint とフォーマットを行う。
- パスは `pathlib.Path` を使用する。文字列連結でパスを組み立てない。
- 外部プロセス呼び出しは `subprocess` のリスト形式で行う。`shell=True` を使わない。
- Windows と Linux の差異はコード内で分岐せず、可能な限り標準ライブラリで吸収する。
- テストは pytest。モデルを必要としないテストと、必要とするテストを
  マーカーで分離する（`@pytest.mark.requires_model`）。
- CI ではモデル不要のテストのみ実行する。

---

## 17. フェーズ計画

### Phase 1 — 骨格と最小動作

プロジェクト構成、Config、CLI 骨格、ffmpeg 音声抽出、faster-whisper（CPU/CUDA）、
pyannote、突き合わせ、SRT/VTT/JSON/TXT/MD 出力、単一ファイル処理。

### Phase 2 — 実運用機能

ジョブ管理とレジューム、フォルダ一括・キュー処理、モデル管理コマンド、
`setup.ps1`、`devices` コマンド、デバイス自動判定。

### Phase 3 — バックエンド拡張

OpenVINO ASR バックエンド、sherpa-onnx 話者分離バックエンド、
バックエンド切り替えの設定と検証。

### Phase 4 — 長時間対応と公開準備

分割処理＋全体クラスタリング、メモリ監視、`eval` コマンド、CI、ドキュメント整備。

### Phase 5 以降

GUI、AMD 対応（whisper.cpp Vulkan）、exe 化。

---

## 18. ドキュメント運用ルール

以下の4文書はリポジトリ直下に常に存在し、内容と実装が一致していなければならない。

| ファイル | 内容 | 更新義務 |
|---|---|---|
| `README.md` | 概要、インストール、使い方、対応バックエンド、ライセンス | CLI・インストール手順・対応機能に変更があるたび |
| `変更履歴.md` | Keep a Changelog 形式の変更履歴 | すべての作業単位で |
| `要件定義.md` | 本書。仕様の唯一の情報源 | 仕様変更があるたび |
| `AISTATE.md` | AI 作業者向けの状態記録 | すべての作業単位で |

**更新は実装と同じ作業単位で行うこと。実装だけ進めてドキュメントを後回しにしてはならない。**

### AISTATE.md の役割

AI がセッションをまたいで作業する際のコンテキスト置き場。
新しいセッションを開始した AI が、これを読むだけで現在地を把握できる状態を維持する。
形式は自由だが、以下を含めること。

- 現在のフェーズと進捗
- 直近の作業内容と、その結果
- 未解決の課題、判断を保留している事項
- 設計上の判断とその理由（なぜその選択をしたか）
- 次に着手すべきこと
- 既知の落とし穴、ハマった箇所とその回避方法

人間向けの体裁は不要。AI が読んで作業を再開できることを最優先する。

---

## 19. 実行環境の分離（Phase 3a）

> 章番号について: Phase 3a指示書は本章を「15.」として追記するよう指定していたが、
> 本書には既に15〜18章が存在する。「既存の章番号は変更せず、末尾に追加する」という
> 指示書の原則を優先し、既存章と衝突しない19章として追加した。判断の記録はAISTATE.mdを参照。

### 19.1 背景

PyTorchはCPU版・CUDA版・XPU版が同一パッケージ名の別ビルドであり、1つの仮想環境には
1種類しか導入できない。Phase 2までの単一 `.venv` / `.venv-windows` モデルでは、
プロファイルを切り替えるたびに数GBの再ダウンロードが発生し、CUDA環境とIntel環境を
同時に保持できなかった。Phase 3aはこれをプロファイルごとに独立したvenvを持つ構成へ変更する。

### 19.2 プロファイル定義

| プロファイル | extras | 想定環境 |
|---|---|---|
| `cpu` | `cpu` | GPU なし |
| `cuda` | `cuda` | NVIDIA |
| `intel` | `xpu`, `whisper-cpp`, `openvino` | Intel CPU / Arc / NPU |
| `vulkan` | `cpu`, `whisper-cpp` | AMD その他（OpenVINO なし） |

`vulkan` プロファイルは、AMD GPU 環境を OpenVINO なしで対象に含めるために設ける。
extrasの排他関係（`cpu`/`cuda`/`xpu`）とそれぞれの内容は14章を参照。

### 19.3 venv のレイアウトと設定

```text
<venv_root>/
  win-cpu/
  win-cuda/
  win-intel/
  win-vulkan/
  linux-cpu/            # WSL等、非Windows環境
```

- ディレクトリ名は `<OS識別子>-<プロファイル名>`。OS識別子は Windows が `win`、
  それ以外（WSLを含む）が `linux`。同一チェックアウトをWSLとWindowsで共有した場合の
  相互干渉を防ぐため（旧 `.venv-windows` 特例の恒久化）。
- `<venv_root>` の既定値はリポジトリ直下の `.venvs`。設定 `[general].venv_dir`
  （空文字は既定値扱い）、環境変数 `UTTERAN_VENV_DIR` の順で上書きできる。
  `UTTERAN_VENV_DIR` は `setup.ps1` / `run.ps1` が Python 側の Config を経由せず
  直接参照できるよう、`UTTERAN_MODEL_DIR` と同じ独立した環境変数として扱う
  （`UTTERAN_GENERAL__VENV_DIR` のようなネスト形式ではない）。
- `.gitignore` は `.venvs/` を除外する。既存の `.venv` / `.venv-windows` は
  Phase 3a移行後も削除・変更しない（利用者が新方式の動作を確認した後、手動で削除する）。
- 既定プロファイルは `[general].default_profile`（空文字は未設定）に保存する。未設定時は
  作成済みプロファイルが1つならそれを使い、複数あれば明示指定を要求する。

### 19.4 共有するもの・分離するもの

| 区分 | 対象 |
|---|---|
| 分離（プロファイルごとに独立） | Python パッケージ一式（venv自体） |
| 共有（全プロファイルで単一） | モデルキャッシュ（`UTTERAN_MODEL_DIR`）、ジョブ（`job_dir`）、`config.toml`、ジョブごとの `utteran.log`、ネイティブビルド成果物（`native_dir`、20章） |

ネイティブビルド成果物（whisper.cppバイナリ）はtorchに依存しないため、プロファイル間で
共有する。ただしOpenVINOランタイムDLLのようにプロファイル固有のライブラリへ依存する構成は、
実行時に「現在アクティブな」venvから動的に解決し、ビルド成果物側にパスを焼き込まない
（20.4参照）。

### 19.5 プロファイル切り替えとジョブの再計算

`[asr]` / `[diarization]` セクションの `backend` / `device` は既に6.3の `config_hash`
算出対象に含まれる。したがって、利用者が `device` をプロファイルごとに明示的な値
（例: `cpu` プロファイルでは `cpu`、`cuda` プロファイルでは `cuda`）へ設定していれば、
プロファイル切り替え時に値が変わり、影響するステージ以降が正しく再計算される。

**既知の制約**: `device = "auto"`（既定値）のまま複数プロファイルで同一ジョブを共有すると、
config.toml上の値は`"auto"`のまま変化しないため、実際に使用されるハードウェアが
プロファイルによって変わっていても `config_hash` は変化せず、レジューム判定は
古いステージ結果を再利用してしまう場合がある。これはconfig_hashが実行時に解決された
具体的なdevice/backendではなく、設定ファイル上の文字列を対象にしているために生じる。
Phase 3aでは解決手段としてハッシュ計算ロジックの変更は行わず（既存のレジューム機構への
影響範囲が大きいため）、`device`/`backend`を明示指定するか、プロファイルごとに別々の
`job_dir`を使うことを利用者に推奨する。この制約はドキュメントに明記した上で残す。

---

## 20. ネイティブビルド（whisper.cpp、Phase 3a基盤のみ）

Phase 3aはwhisper.cppの取得・複数構成ビルド・manifest記録までを実装する。実際の
文字起こし利用（`--asr-backend whisper-cpp`相当の実行経路）はPhase 3bで実装する。

### 20.1 バージョン

- タグ `v1.9.1`、コミット `f049fff95a089aa9969deb009cdd4892b3e74916` に固定する。
  Phase 3a着手時点の最新安定タグであり、実機でCPU/OpenVINO/Vulkan/
  OpenVINO+Vulkanの4構成すべてのビルドと実推論に成功したため、参考実装のv1.8.6より
  新しいこのタグを採用した（判断根拠はAISTATE.md I-1参照）。

### 20.2 ビルド構成

| 名称 | CMake フラグ | 前提 |
|---|---|---|
| `cpu` | `-DWHISPER_OPENVINO=OFF -DGGML_VULKAN=OFF` | なし |
| `openvino` | `-DWHISPER_OPENVINO=ON -DGGML_VULKAN=OFF -DOpenVINO_DIR=...` | openvino パッケージ、OpenVINO GPU |
| `vulkan` | `-DWHISPER_OPENVINO=OFF -DGGML_VULKAN=ON` | Vulkan SDK（`glslc`） |
| `openvino_vulkan` | 上記両方 | 両方 |

`openvino_vulkan`はエンコーダをOpenVINO、デコーダをVulkanにオフロードする構成。実測で
最速だった（AISTATE.md参照）。各構成は前提条件（`glslc`の有無、OpenVINO GPUの認識）を
個別に判定し、満たさない構成はエラー理由を記録してスキップする。全体のビルドを失敗させない。

Windowsのビルドには、CMakeのマルチコンフィグ生成器 `Visual Studio 17 2022`
（`-G "Visual Studio 17 2022" -A x64`）を使用する。Ninja生成器と異なりMSVCツールチェインを
自力で検出できるため、`vcvars64.bat`を事前に読み込んでいない通常のPowerShell/cmdセッションから
`utteran native build`を実行できることを実機で確認した。

### 20.3 配置

```text
<native_dir>/
  <platform>/
    src/            whisper.cppのソース
    cpu/
    ov/
    vk/
    ovvk/           openvino_vulkanの短縮ディレクトリ名
    manifest.json
```

- `<native_dir>` の既定値は `~/.utteran/native`。`platformdirs.user_data_dir`配下は
  Windowsで長くなりがちなため、より短い固定パスを既定とした。設定
  `[general].native_dir`（空文字は既定値扱い）、環境変数 `UTTERAN_NATIVE_DIR` で
  上書きできる（`venv_dir`と同じ独立した環境変数扱い）。
- ビルドディレクトリ名は短縮する（`openvino_vulkan`→`ovvk`等）。ggmlのVulkanシェーダー生成
  サブビルドが深くネストし、MSVCのFileTrackerがMAX_PATHで失敗するため。
- ビルド成果物は全プロファイルで共有する（19.4参照）。

### 20.4 manifest

```json
{
  "schema_version": 1,
  "platform": "win-amd64",
  "whisper_cpp": { "tag": "v1.9.1", "commit": "f049fff9..." },
  "built_at": "...",
  "backends": {
    "openvino_vulkan": {
      "executable": "...",
      "cmake_flags": ["..."],
      "requires": ["openvino", "vulkan"]
    }
  },
  "errors": { "vulkan": "..." }
}
```

**manifestにはOpenVINOランタイムDLLディレクトリなど実行時ライブラリの絶対パスを
一切記録しない。** ビルド成果物は全プロファイルで共有するのに対し、OpenVINOの
DLLは各プロファイルvenvの`site-packages/openvino/libs`にあり、プロファイルごとに
異なるパスになるため、manifestへ焼き込むと他プロファイルから実行できなくなる。
実行時ライブラリのディレクトリは、実行時に現在アクティブな環境から動的に解決する
（`openvino`パッケージがimport可能かどうかで判定）。

### 20.5 CLI

```text
utteran native build [--variant cpu,vulkan,...] [--force]
utteran native status
utteran native clean [--all | --variant <名前>]
```

`build`は前提条件を満たす構成のみをビルドし、満たさないものは理由を記録してスキップする
（要求された構成のうち1つもビルドできなかった場合のみ終了コード3）。`status`はmanifestの
内容と、現在の環境で各構成が実行可能かを表示する。

### 20.6 devices への統合

`utteran devices` は以下を追加表示する（既存のJSON構造は変更せず追加のみ）。

- 現在のプロファイル名と、作成済みの他プロファイルの一覧（存在・最終更新のみ。
  他プロファイルのPythonは起動しない）
- Vulkanのビルド前提（`glslc`）とランタイム（`vulkaninfo`）を区別した検出結果
  （一方だけが利用可能な場合があるため、単一のavailable/unavailableに集約しない）
- ネイティブビルドの状態（manifestの有無、whisper.cppタグ、各構成の実行可否）

---

## 21. whisper.cpp ASR（Phase 3b）

### 21.1 単語タイムスタンプ

`[asr].word_timestamps` は `auto`（既定）/`always`/`never` とする。`auto` は話者分離が
有効な場合だけ、`always` は常時、`never` は取得しない。これはwhisper.cpp経路に適用し、
既存のfaster-whisper経路は従来どおり単語時刻を取得する。

whisper.cppで取得する場合は、モデルカタログのDTWプリセットを解決し、`--dtw`と
`--no-flash-attn`を必ず併用する。`dtw = "auto"`でプリセットのないモデルを`auto`モードで
使う場合は警告して単語なしで続行し、`always`では設定エラーとする。通常トークンの`t_dtw`が
すべて`-1`ならDTW失敗として警告し、壊れたDTW時刻は返さずセグメント単位の話者割当に
フォールバックする。

文単位のsegmentは維持し、Python側で特殊トークン除外、UTF-8バイト断片の復元、空白言語の
先頭空白境界、日中韓言語の文字境界、句読点の直前語への結合を行う。時刻は構成トークンの
`t_dtw`（whisper.cppの10ミリ秒tick）から求め、値が`-1`ならoffsets（ミリ秒）へ退避する。
確率は構成トークンの`p`の平均とし、時刻は所属segment内へclampする。単語がないsegmentは
既存align.pyのsegment単位割当にフォールバックする。

### 21.2 GGMLモデルカタログ

`ggerganov/whisper.cpp`で実在を確認したGGMLファイルをサイズ・量子化単位で登録する。
通常の`models list --available`は多言語の推奨6件だけを表示し、`--all`併用時は英語専用を
含む全件を表示する。各項目は実ファイル名、実測ファイルサイズ、モデルサイズ、量子化、
DTWプリセット、ライセンスを保持する。共有リポジトリ全体ではなく選択した単一GGMLファイル
だけを取得する。

whisper.cpp v1.9.1のCLIはOpenVINO IRパスの明示指定を公開しておらず、GGMLファイル名の
`.bin`を`-encoder-openvino.xml`へ置換して導出する。このため量子化違いでは、サイズごとの
共通IRを各GGML名に対応する規約名へリンクし、リンク非対応時だけコピーする。

### 21.3 OpenVINO encoder IR管理

`models prepare-openvino <whisper-cppモデルID> [--device GPU] [--purge-cache] [--yes]`で
OpenAI公式PyTorch重みからモデルサイズ単位のIRを生成する。実行前に最大約3GBの別取得を
明示して確認し、`--yes`だけが確認を省略する。openvino extraがなければsetup方法を案内する。
生成物は`<model_dir>/openvino-encoder/<size>/`へXML/BIN一組で保存し、GGMLの量子化ごとの
規約名へハードリンク（非対応環境ではコピー）する。`list-openvino`で一覧、
`remove-openvino`でIRとaliasを削除し、`models verify`は片方欠落やalias欠落を不整合とする。

### 21.4 whisper.cppバックエンド

`load()`はmanifest、構成、GGMLファイルを検証するだけでモデルを常駐させず、`transcribe()`が
正規化済み16kHz mono WAVごとに`whisper-cli`を起動する。出力は一時ディレクトリのfull JSONへ
書き、読取後に削除する。進捗stderrだけを解析し、表示・エラー詳細は秘密値マスクを通す。
キャンセルはWindowsでは`taskkill /T /F`でprocess treeを止める。OpenVINO DLLは現在のvenvから
動的にPATHへ追加しmanifestへ記録しない。バッチではbackend解決を再利用するが、GGML自体は
ファイルごとに再ロードされる。

### 21.5 auto選択とフォールバック

autoは、(1)利用可能なCUDA→faster-whisper/CUDA、(2)Vulkan+OpenVINO GPU+build→
whisper-cpp/openvino_vulkan、(3)Vulkan+build→whisper-cpp/vulkan、(4)OpenVINO GPU+build→
whisper-cpp/openvino、(5)faster-whisper/CPUの順で選び、理由をdevicesのnotesへ記録する。
CPUではwhisper.cppをauto選択しない。

variantがautoでGPU初期化に失敗した場合だけ、v1.9.1で確認した初期化エラーを判定し、次の
利用可能なGPU構成へ1回だけ退避する。退避元、先、秘密値mask済み理由を警告・ジョブログへ
残す。backendまたはvariantの明示指定時、モデル形式エラー時、2回目の失敗時は退避しない。

---

## 22. torch XPU 話者分離（Phase 3c）

`[diarization].device` は `auto | cpu | cuda | cuda:N | xpu | xpu:N` とする。autoは
PyTorchの実カーネルprobeに成功したCUDA、XPU、CPUの順に選択する。XPUは`intel`プロファイル
専用であり、他プロファイルからの明示指定は`setup.ps1 -Profile intel`を案内して拒否する。

pyannote community-1はArc 140T実機で`Pipeline.to(xpu:0)`、segmentationとspeaker embeddingを
含む実推論が成功したため分岐Aとして対応する。autoで選んだXPUの初期化に失敗した場合だけCPUへ
一度退避し、明示指定では退避しない。XPUのメモリ不足は共有システムRAMの不足として説明し、
`unload()`でXPU allocatorを明示解放する。deviceは話者分離stageのconfig hashに含め、変更時は
再計算する。
